<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Hello World</title>
    <url>/2026/05/03/hello-world/</url>
    <content><![CDATA[Welcome to Hexo! This is your very first post. Check documentation for more info. If you get any problems when using Hexo, you can find the answer in troubleshooting or you can ask me on GitHub.
Quick StartCreate a new post$ hexo new &quot;My New Post&quot;

More info: Writing
Run server$ hexo server

More info: Server
Generate static files$ hexo generate

More info: Generating
Deploy to remote sites$ hexo deploy

More info: Deployment
]]></content>
  </entry>
  <entry>
    <title>H.264 硬解码播放链路 Bug 修复记录</title>
    <url>/2026/06/22/H264%E7%A1%AC%E8%A7%A3%E7%A0%81%E6%92%AD%E6%94%BE%E9%93%BE%E8%B7%AFBug%E4%BF%AE%E5%A4%8D%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[H.264 硬解码播放链路 Bug 修复记录概述在 TXW82x 平台 H.264 硬解码播放功能开发过程中，定位并修复了 H.264 文件解复用（h264_file_msi）与硬件解码（h264_decode_msi）链路上的 3 个关键 Bug。这些 Bug 共同导致解码输出花屏、解码器崩溃或完全不输出。

涉及文件


文件
路径
角色



h264_file_msi.c
sdk/app/h264_demux/
H.264 文件解复用器，从 SD 卡读取 NAL 流，封装 framebuff 发出


h264_decode_msi.c
sdk/app/decode/
H.264 硬件解码 MSI 组件，消费 framebuff 送入硬件解码


sample_h264_esplayer.c
project/app/
H.264 ES 流播放 Sample，作为参考实现


stream_define.h
sdk/app/algorithm/stream_frame/
fb_h264_s 结构定义



Bug 1：start_len 未动态设置现象解码器在 h264_decode_msi.c 中通过以下代码计算 NAL 有效载荷的偏移和长度：
uint32_t dst = (uint32_t) rfb-&gt;data + h264_priv-&gt;start_len;uint32_t dst_len = rfb-&gt;len - h264_priv-&gt;start_len;

当 start_len 被硬编码为固定值（如 4）时，如果实际文件使用 3 字节起始码（00 00 01），则：

dst 多偏移了 1 字节，指向错误位置
dst_len 少算了 1 字节，长度不正确
后续 h264_rom_memcpy() 拷贝错误数据送入硬件解码器，导致花屏或解码器异常

根因H.264 Annex-B 格式的起始码（start code）有两种长度：

3 字节：0x00 0x00 0x01
4 字节：0x00 0x00 0x00 0x01

fb_h264_s.start_len 字段用于记录起始码长度，告诉解码器需要跳过多少字节才能到达 NAL 单元的真实数据。之前代码中未根据实际扫描到的起始码动态赋值，而是使用了固定值。
修复h264_file_msi.c — 帧发送路径// 旧：start_len 可能为固定值或未正确设置// 新：使用实际扫描到的 start code 长度node-&gt;priv.start_len = frame_info.frame_start_len;

其中 frame_info.frame_start_len 在扫描 NAL 边界时由 h264_find_start_code() 动态确定（返回 3 或 4）。
sample_h264_esplayer.c — Sample 参考实现// 旧：固定值或缺失// 新：从实际 NAL 扫描中获取 start_lenh264_priv-&gt;start_len = code_len;

其中 code_len 在扫描起始码时根据字节序列动态判定。
h264_parse_fb_priv() 中的连带修复该函数在解析内存中的 NAL 缓冲时，原先使用 nal_header_offset 赋值 start_len：
// 旧：nal_header_offset = found_offset + start_len// 当 found_offset != 0 时，start_len 值错误priv-&gt;start_len = (uint8_t)nal_header_offset;// 新：应使用实际的 start code 长度priv-&gt;start_len = (uint8_t)start_len;


注： found_offset 是起始码在缓冲中的偏移，start_len（局部变量）是起始码长度（3 或 4）。当缓冲头部无填充字节时（found_offset == 0），两者数值相同，Bug 不易暴露；但存在前导数据时则计算错误。


Bug 2：fb-&gt;len 未设置导致 memcpy 失败现象解码器在 h264_decode_msi.c 中执行：
h264_rom_memcpy(rom_ptr, (uint8_t *) dst, dst_len);

其中 dst_len = rfb-&gt;len - h264_priv-&gt;start_len。当 fb-&gt;len 为 0 或未赋值时，dst_len 计算为负值（类型隐式转换后为极大值），导致：

h264_rom_memcpy 拷贝超出实际数据范围的无效内存
硬件解码器接收到错误的 NAL 数据
解码超时或崩溃

根因在创建 framebuff 时，fb-&gt;len 字段未被正确设置为 NAL 单元的实际长度。fb-&gt;len 是 MSI 管道中下游组件判断数据有效长度的唯一依据。
修复h264_file_msi.c — 帧发送路径// 旧：fb-&gt;len 未赋值或赋值为 0// 新：设置为 frame node 的实际数据长度fb-&gt;len = node-&gt;len;fb-&gt;data = node-&gt;data;   // data 也需同步指向正确缓存

sample_h264_esplayer.c — Sample 参考实现fb-&gt;data = STREAM_MALLOC(nal_size);fb-&gt;len = nal_size;     // ← 关键修复：必须设置长度


Bug 3：解码端 dst &#x2F; dst_len 计算现象这是 Bug 1 和 Bug 2 的消费端表现。即使前两项已修复，如果 start_len 和 fb-&gt;len 正确，此计算本身是正确的。但该代码对输入的正确性高度敏感：
uint32_t dst     = (uint32_t) rfb-&gt;data + h264_priv-&gt;start_len;uint32_t dst_len = rfb-&gt;len - h264_priv-&gt;start_len;

逻辑说明


变量
含义



rfb-&gt;data
framebuff 数据指针，指向包含起始码的完整 NAL 数据


rfb-&gt;len
NAL 数据总长度（包含起始码）


h264_priv-&gt;start_len
起始码长度（3 或 4），跳过起始码到达 NAL type 字节


dst
指向 NAL type 字节（即有效载荷起点）


dst_len
剩余有效数据长度（NAL type + payload）


数据流校验关系rfb-&gt;data  ──▶ [00 00 00 01][67 XX YY ...]                ├─ start_len=4 ─┤                ├───────── rfb-&gt;len ─────────┤                                  ├── dst_len ──┤                dst ──────────────────▶

修复依赖
✅ Bug 1 修复后 → start_len 正确（3 或 4）
✅ Bug 2 修复后 → rfb-&gt;len 正确
→ 此计算自然正确


修复验证测试条件


项目
值



平台
TXW82x (C-Sky CK804DF)


测试文件
H.264 基线编码文件，包含 I 帧 + P 帧


起始码类型
混合：部分文件使用 3 字节、部分使用 4 字节


输出分辨率
320×240


验证方式
LCD 屏幕目测 + os_printf 日志


验证结果


测试项
修复前
修复后



3 字节起始码文件
花屏&#x2F;无输出
正常播放


4 字节起始码文件
偶发花屏
正常播放


I 帧 + P 帧解码
首帧后卡死
连续播放


文件循环播放
第 2 轮崩溃
稳定循环


快速切换文件
内存拷贝错误
切换正常



经验总结
fb_h264_s.start_len 是起始码长度，不是 NAL header 偏移。 虽然在某些场景下两者数值相同，但语义必须准确，否则遇到变长起始码或缓冲前导数据时会出错。

fb-&gt;len 是 MSI 管道的核心契约。 上游生产者必须准确设置 framebuff 的数据长度，下游消费者依赖此值进行内存操作。遗漏设置会导致消费者读取越界或长度异常。

H.264 Annex-B 起始码长度是动态的（3 或 4 字节）。 所有涉及 NAL 数据扫描和传递的环节都必须感知这一动态性，硬编码 4 字节是最常见的隐蔽 Bug 来源。

端到端校验： 在 h264_file_msi（生产者）→ jpg_decode_msg_msi（帧解析）→ h264_decode_msi（解码器）→ R_VIDEO_P0（LCD 显示）的完整链路中，每个环节都应校验 fb-&gt;len 和 start_len 的合理性，避免异常数据传递到硬件层。


]]></content>
      <categories>
        <category>Bug 修复</category>
      </categories>
      <tags>
        <tag>MSI</tag>
        <tag>H.264</tag>
        <tag>硬件解码</tag>
        <tag>TXW82x</tag>
        <tag>framebuff</tag>
        <tag>BugFix</tag>
      </tags>
  </entry>
  <entry>
    <title>TXW82x 平台 H.264 硬解码播放器 UI 组件开发文档</title>
    <url>/2026/06/22/TXW82x%E5%B9%B3%E5%8F%B0H264%E7%A1%AC%E8%A7%A3%E7%A0%81%E6%92%AD%E6%94%BE%E5%99%A8UI%E7%BB%84%E4%BB%B6%E5%BC%80%E5%8F%91%E6%96%87%E6%A1%A3/</url>
    <content><![CDATA[H264 Player UI 组件开发文档1. 概述h264_player_ui.c 是基于 LVGL 图形框架和 MSI（Media Stream Interface）媒体流管道实现的 H.264 硬解码播放器 UI 组件。运行于 TXW82x WiFi 摄像头 SoC（C-Sky CK804DF 内核）平台。
功能：

在 LCD 上创建 H.264 文件播放器的菜单入口
遍历文件系统中 H264/ 目录下的 *.h264 文件并展示文件列表
选择文件后，通过 MSI 管道完成硬件解码并在 LCD 视频层（R_VIDEO_P0）显示
支持播放&#x2F;暂停控制
支持退出播放并回到主菜单


2. 架构与数据流2.1 MSI 管道拓扑在进入播放界面时，组件建立如下 MSI 数据流管道：
SD 卡 H.264 文件 (*.h264)     │     ▼h264_file_msi (文件解复用器)     │  MSI_CMD_VIDEO_DEMUX_CTRL → START/STOP/PAUSE     │  输出: F_H264 类型 framebuff     ▼p0_decode_msg_msi (SR_OTHER_JPG, 帧信息解析)     │  强制输出类型: FSTYPE_YUV_P0     │  输出: F_JPG_DECODE_MSG     ▼decode_msi (&quot;h264_decode&quot;, H264 硬件解码)     │  H264 硬件解码 + Scale2 缩放     │  输出: F_YUV, stype=FSTYPE_YUV_P0     ▼R_VIDEO_P0 (LCD 视频层 P0)     │  MSI_CMD_LCD_VIDEO → MSI_VIDEO_ENABLE 控制开关     ▼LCD 屏幕显示

2.2 UI 层次结构lv_scr_act (当前活动屏幕)   ├── base_ui (主菜单列表, 由调用方传入)   │     └── &quot;h264_player&quot; 按钮 → 点击进入播放器   │   └── now_ui (播放器界面, 全屏)         ├── 视频显示层 (通过 MSI → R_VIDEO_P0 渲染)         ├── label_time (&quot;00:00&quot; 时间标签, 顶部居中)         └── 文件列表弹窗 (按 &#x27;e&#x27; 键触发)               ├── &quot;exit&quot; 按钮 → 返回播放器               └── 每个 *.h264 文件按钮 → 点击开始播放


3. 关键数据结构3.1 struct h264_player_ui_s — 播放器控制块


字段
类型
说明



last_group
lv_group_t *
上一级界面的 LVGL 组，退出时恢复


base_ui
lv_obj_t *
主菜单列表对象，退出时取消隐藏


w, h
uint16_t
视频解码输出宽高


now_group
lv_group_t *
当前播放器界面的 LVGL 组


now_ui
lv_obj_t *
当前播放器界面对象


timer
lv_timer_t *
100ms 周期定时器（用于更新时间显示）


label_time
lv_obj_t *
时间标签控件


p0_decode_msg_msi
struct msi *
解码帧信息 MSI 组件 (h264_decode_msg_msi)


decode_msi
struct msi *
H264 硬件解码 MSI 组件 (h264_decode_msi)


file_msi
struct msi *
文件解复用 MSI 组件 (h264_file_msi)


play_name
uint8_t *
当前播放流的名称（PSRAM 分配）


playing
uint8_t:1
播放状态标志


3.2 struct h264_list_param — 文件列表参数


字段
类型
说明



group
lv_group_t *
文件列表的 LVGL 组


ui
lv_obj_t *
文件列表的 LVGL list 对象


ui_s
h264_player_ui_s *
指向播放器控制块的指针



4. 外部依赖与声明4.1 外部 MSI 组件函数// 创建解码帧信息 MSI 组件（实现在 jpg_decode_msg_msi.c）extern struct msi *h264_decode_msg_msi(const char *name, uint16_t out_w, uint16_t out_h,                                        uint16_t step_w, uint16_t step_h, uint32_t filter);// 创建 H264 硬件解码 MSI 组件（实现在 h264_decode_msi.c）// only_I_H264: 0=解码 I+P 帧, 1=只解码 I 帧extern struct msi *h264_decode_msi(const char *name, uint8_t only_I_H264);// 创建文件解复用 MSI 组件（实现在 h264_file_msi.c）extern struct msi *h264_file_msi_init(const char *msi_name, const char *filename);// H264 硬件驱动初始化extern void h264_drv_init(void);

4.2 外部全局变量extern lv_style_t  g_style;       // 全局 LVGL 样式extern lv_indev_t *indev_keypad;  // 全局键盘输入设备

4.3 引用的头文件


头文件
路径
用途



lvgl/lvgl.h
SDK 内
LVGL 图形库核心


lvgl_ui.h
sdk/app/ui/
UI 组件声明、MSI 组件 extern 声明


keyWork.h
sdk/lib/key/
按键回调工作队列


keyScan.h
sdk/lib/key/
按键扫描、键值定义（AD_UP, AD_DOWN 等）


av_heap.h
lib/heap/
普通堆内存分配


av_psram_heap.h
lib/heap/
PSRAM 堆内存分配


avi_player_msi.h
sdk/app/playback/
AVI 播放器 MSI 接口


audio_code_ctrl.h
audio_media_ctrl/
音频编解码控制


osal_file.h
fs/fatfs/
文件系统接口


stream_define.h
sdk/app/algorithm/stream_frame/
流名称和 MSI 命令常量定义



5. 宏定义#define CHECK_DIR            &quot;H264&quot;     // 扫描目录名#define EXT_NAME             &quot;*h264&quot;    // 文件名匹配模式#define PLAY_STREAM_NAME_LEN (64)       // 播放流名称缓冲区大小#define STREAM_MALLOC        av_psram_malloc   // 大块内存分配（PSRAM）#define STREAM_FREE          av_psram_free     // 大块内存释放#define STREAM_LIBC_ZALLOC   av_zalloc         // 零初始化分配


6. 按键映射函数 self_key() 将硬件按键事件映射为 LVGL 逻辑按键：



硬件按键
LVGL 映射值
用途



AD_UP
&#39;q&#39;
返回&#x2F;退出


AD_DOWN
&#39;e&#39;
展开文件列表


AD_LEFT
&#39;a&#39;
定位控制（预留）


AD_RIGHT
&#39;d&#39;
定位控制（预留）


AD_PRESS
LV_KEY_ENTER
确认选择


按键过滤器通过 set_lvgl_get_key_func(self_key) 设置，退出时通过 set_lvgl_get_key_func(NULL) 清除。

7. 函数参考7.1 公开 APIh264_player_ui()lv_obj_t *h264_player_ui(lv_group_t *group, lv_obj_t *base_ui, uint16_t w, uint16_t h);

说明： 在主菜单列表中添加一个名为 &quot;h264_player&quot; 的按钮入口。
参数：

group — 主菜单的 LVGL 组
base_ui — 主菜单的 LVGL list 对象
w, h — 视频解码输出宽度和高度

返回值： 创建的按钮对象指针，失败返回 NULL。
调用示例（main_ui.c）：
btn = h264_player_ui(group, base_ui, 320, 240);

7.2 内部函数self_key() — 按键映射static uint32_t self_key(uint32_t val);

将硬件 AD_* 键值 + KEY_EVENT_SUP（短按释放）事件映射为 LVGL 可识别的字符键值。仅在播放器活跃时生效。
h264_player_stop_file() — 停止播放static void h264_player_stop_file(struct h264_player_ui_s *ui_s);

停止文件解复用器（发送 MSI_VIDEO_DEMUX_STOP），销毁 file_msi 组件，关闭 LCD 视频层，释放 play_name 缓冲区。
enter_player_ui() — 进入播放器界面static void enter_player_ui(lv_event_t *e);

事件： LV_EVENT_SHORT_CLICKED（点击主菜单的 “h264_player” 按钮）
流程：

设置按键过滤器 self_key
调用 h264_drv_init() 初始化 H264 硬件
隐藏主菜单（base_ui），创建全屏播放器界面（now_ui）
创建 MSI 管道：
p0_decode_msg_msi &#x3D; h264_decode_msg_msi(SR_OTHER_JPG, ...) → 帧信息解析
decode_msi &#x3D; h264_decode_msi(&quot;h264_decode&quot;, 0) → 硬件解码
连接：p0_decode_msg_msi → decode_msi → R_VIDEO_P0


创建时间标签 label_time（初始显示 “00:00”）和 100ms 定时器
创建新的 LVGL 组并注册事件回调

exit_player_ui() — 退出播放器界面static void exit_player_ui(lv_event_t *e);

事件： LV_EVENT_KEY，键值 &#39;q&#39;
流程：

删除定时器
恢复键盘输入组到上一级
显示主菜单（清除 base_ui 的隐藏标志）
销毁当前 LVGL 组、MSI 管道（p0_decode_msg_msi, decode_msi）
调用 h264_player_stop_file() 停止文件播放
删除当前界面对象，清除按键过滤器

show_filelist() — 显示文件列表static void show_filelist(lv_event_t *e);

事件： LV_EVENT_KEY，键值 &#39;e&#39;
流程：

清除按键过滤器
在当前界面创建全屏 LVGL list
添加 “exit” 返回按钮
调用 each_read_file() 扫描 H264/ 目录，为每个 *.h264 文件添加列表项
为每个文件按钮注册 enter_playback 事件

enter_playback() — 开始文件播放static void enter_playback(lv_event_t *e);

事件： LV_EVENT_CLICKED（点击文件列表中的文件项）
流程：

停止当前（如有）正在播放的文件
获取文件名，构造路径 &quot;0:H264/&lt;filename&gt;&quot;
使用 STREAM_MALLOC(av_psram_malloc) 分配 play_name 缓冲区
调用 h264_file_msi_init() 创建文件解复用器
成功：使能 LCD 视频层，连接 file_msi → p0_decode_msg_msi，发送 MSI_VIDEO_DEMUX_START
失败：清理资源，退出文件列表

player_ctrl_ui() — 播放&#x2F;暂停控制static void player_ctrl_ui(lv_event_t *e);

事件： LV_EVENT_SHORT_CLICKED（点击播放器界面空白区域）
行为： 切换 playing 状态。从播放→暂停时发送 MSI_VIDEO_DEMUX_PAUSE，从暂停→播放时发送 MSI_VIDEO_DEMUX_START 并重新使能 LCD 视频层。
player_locate_ctrl_ui() — 定位控制（预留）static void player_locate_ctrl_ui(lv_event_t *e);

事件： LV_EVENT_KEY，键值 &#39;a&#39; &#x2F; &#39;d&#39;
当前仅捕获事件，未实现具体逻辑（switch case 为空）。可用于实现快退&#x2F;快进功能。
player_ui_timer() — 定时器回调（预留）static void player_ui_timer(lv_timer_t *t);

100ms 周期定时器。当前框架已创建，函数体为空。可用于实现播放时间更新显示。
h264_list_show() — 单个列表项创建回调static int h264_list_show(const char *filename, void *data);

为每个 *.h264 文件在列表中添加一个按钮，绑定 enter_playback 事件。
each_read_file() — 文件系统遍历static int each_read_file(create_h264_list_ui fn, void *param);

使用 FatFS API 遍历 H264/ 目录下匹配 *h264 的文件，对每个文件调用回调函数 fn。
exit_show_filelist() — 退出文件列表static void exit_show_filelist(lv_event_t *e);

恢复按键过滤器，恢复键盘输入组，延迟删除列表对象。
clear_list_group_ui() — 列表销毁时清理组static void clear_list_group_ui(lv_event_t *e);

在 LVGL list 对象被删除时，自动释放关联的 LVGL 组。

8. 关键事件注册汇总


LVGL 事件
对象
回调函数
说明



LV_EVENT_SHORT_CLICKED
“h264_player” 按钮
enter_player_ui
进入播放器


LV_EVENT_KEY
播放器界面 (now_ui)
exit_player_ui
按 &#39;q&#39; 退出


LV_EVENT_KEY
播放器界面 (now_ui)
player_locate_ctrl_ui
按 &#39;a&#39;&#x2F;&#39;d&#39; 定位


LV_EVENT_KEY
播放器界面 (now_ui)
show_filelist
按 &#39;e&#39; 显示文件列表


LV_EVENT_SHORT_CLICKED
播放器界面 (now_ui)
player_ctrl_ui
短按切换播放&#x2F;暂停


LV_EVENT_SHORT_CLICKED
文件列表中的 “exit” 按钮
exit_show_filelist
返回播放器


LV_EVENT_CLICKED
文件列表中的文件名按钮
enter_playback
开始播放该文件


LV_EVENT_DELETE
文件列表对象
clear_list_group_ui
释放 LVGL 组



9. MSI 命令使用汇总


命令
目标
用途



MSI_CMD_VIDEO_DEMUX_CTRL &#x2F; MSI_VIDEO_DEMUX_START
file_msi
开始解复用（播放）


MSI_CMD_VIDEO_DEMUX_CTRL &#x2F; MSI_VIDEO_DEMUX_STOP
file_msi
停止解复用


MSI_CMD_VIDEO_DEMUX_CTRL &#x2F; MSI_VIDEO_DEMUX_PAUSE
file_msi
暂停解复用


MSI_CMD_LCD_VIDEO &#x2F; MSI_VIDEO_ENABLE=1
R_VIDEO_P0
使能 LCD 视频层 P0


MSI_CMD_LCD_VIDEO &#x2F; MSI_VIDEO_ENABLE=0
R_VIDEO_P0
关闭 LCD 视频层 P0


MSI_CMD_DECODE_JPEG_MSG &#x2F; MSI_JPEG_DECODE_FORCE_TYPE
p0_decode_msg_msi
强制解码输出类型为 FSTYPE_YUV_P0



10. 内存管理


分配点
分配函数
堆类型
释放点



h264_player_ui_s 控制块
av_zalloc
系统堆（SRAM）
当前未显式释放（TODO）


play_name 缓冲区
av_psram_malloc
PSRAM
h264_player_stop_file()


file_msi 组件
MSI 框架内部
视组件实现
msi_destroy()


p0_decode_msg_msi
MSI 框架内部
视组件实现
msi_destroy()


decode_msi
MSI 框架内部
视组件实现
msi_destroy()



注意： 控制块 ui_s 在退出播放器时未显式释放（STREAM_FREE），因为 h264_player_ui() 只调用一次且控制块需要在整个应用生命周期内保持。如需动态创建&#x2F;销毁，应在 exit_player_ui 或 h264_player_stop_file 中添加 STREAM_FREE(ui_s) 并置空指针。


11. 调用链路11.1 入口注册（在 main_ui.c 中）// main_pocket_camera_ui():btn = h264_player_ui(group, base_ui, 320, 240);//     ↓// h264_player_ui()://   1. av_zalloc(sizeof(h264_player_ui_s)) → ui_s//   2. lv_list_add_btn(base_ui, NULL, &quot;h264_player&quot;) → btn//   3. lv_obj_add_event_cb(btn, enter_player_ui, LV_EVENT_SHORT_CLICKED, ui_s)

11.2 完整用户操作流主菜单列表  │  选择 &quot;h264_player&quot; → 短按确认  ▼enter_player_ui()        ← 创建播放器界面 + MSI 管道  │  按 &#x27;e&#x27; 键  ▼show_filelist()          ← 弹出文件列表  │  选择文件 → 点击  ▼enter_playback()         ← 创建 file_msi，开始播放  │  (视频通过 MSI 管道自动渲染到 LCD)  │  短按屏幕 → 暂停/恢复  │  按 &#x27;q&#x27; 键 → 回到文件列表  ▼exit_show_filelist()     ← 返回播放器界面  │  在播放器界面按 &#x27;q&#x27; 键  ▼exit_player_ui()         ← 销毁 MSI 管道，回到主菜单


12. 待完善&#x2F;预留功能
播放时间更新： player_ui_timer() 函数体为空，label_time 始终显示 “00:00”。需要从 file_msi 或 p0_decode_msg_msi 获取当前播放进度更新标签。

快进&#x2F;快退： player_locate_ctrl_ui() 已注册 &#39;a&#39; &#x2F; &#39;d&#39; 按键事件，但 switch case 为空。可根据需要调用 MSI_VIDEO_DEMUX_FORWARD_TIME &#x2F; MSI_VIDEO_DEMUX_REWIND_TIME 命令实现。

控制块生命周期： h264_player_ui_s 对象在 h264_player_ui() 中分配后未释放。如果多次进入&#x2F;退出播放器，应增加销毁逻辑。

文件列表刷新： 当前每次按 &#39;e&#39; 都会重新扫描文件系统并创建新的列表，但旧的列表对象会在 exit_show_filelist 中通过 lv_obj_del_async 延迟删除，可能有短暂的重叠。



13. 依赖的文件


文件
路径
关系



h264_player_ui.c
sdk/app/ui/
本组件源文件


lvgl_ui.h
sdk/app/ui/
公开 API 声明


main_ui.c
sdk/app/ui/
调用 h264_player_ui() 注册入口


h264_file_msi.c
sdk/app/h264_demux/
文件解复用 MSI 组件


jpg_decode_msg_msi.c
SDK 内
H264 帧信息解析 MSI 组件


h264_decode_msi.c
SDK 内
H264 硬件解码 MSI 组件


stream_define.h
sdk/app/algorithm/stream_frame/
流名称常量 (R_VIDEO_P0, SR_OTHER_JPG)


msi.h
sdk/include/lib/multimedia/
MSI 核心接口


framebuff.h
sdk/include/lib/multimedia/
Framebuff 类型定义 (FSTYPE_YUV_P0)


keyWork.h &#x2F; keyScan.h
sdk/lib/key/
按键系统



14. 注意事项
内存： 视频帧数据量大，STREAM_MALLOC 使用 PSRAM（av_psram_malloc），轻量对象使用 SRAM（av_zalloc）。
MSI 管道生命周期： p0_decode_msg_msi 和 decode_msi 在 enter_player_ui() 中创建、在 exit_player_ui() 中销毁。file_msi 在每次选择文件播放时创建、在停止播放或切换文件时销毁。
LCD 视频层竞争： R_VIDEO_P0 是全局 LCD 视频层 P0 通道，确保在进入播放器时使能、退出时关闭，避免影响其他 UI 组件。
文件系统路径： 文件路径格式为 &quot;0:H264/&lt;filename&gt;&quot;，其中 &quot;0:&quot; 是 FatFS 驱动号。

]]></content>
      <categories>
        <category>项目文档</category>
        <category>TXW82x H.264 播放器</category>
      </categories>
      <tags>
        <tag>LVGL</tag>
        <tag>嵌入式</tag>
        <tag>C语言</tag>
        <tag>MSI</tag>
        <tag>H.264</tag>
        <tag>硬件解码</tag>
        <tag>UI组件</tag>
        <tag>TXW82x</tag>
      </tags>
  </entry>
  <entry>
    <title>ADPCM 对讲模块详细设计文档</title>
    <url>/2026/07/03/ADPCM%E5%AF%B9%E8%AE%B2%E6%A8%A1%E5%9D%97%E8%AF%A6%E7%BB%86%E8%AE%BE%E8%AE%A1%E6%96%87%E6%A1%A3/</url>
    <content><![CDATA[ADPCM 对讲模块详细设计文档1. 文档目标本文档说明 sdk/app/intercom/intercom_adpcm.c 的 ADPCM 对讲模块实现，重点讲清楚：

模块整体职责
依赖的流和内存资源
初始化顺序
线程之间的分工
编码、发送、接收、解码、播放链路
函数之间的调用关系
关键回调函数和函数指针绑定关系

本文档对应的核心文件：

sdk/app/intercom/intercom_adpcm.c
sdk/app/intercom/intercom.h
sdk/app/audio_app/bbm_audio_ad.c
sdk/app/algorithm/stream_frame/stream_define.h
project/main.c


2. 模块定位intercom_adpcm.c 实现的是一套基于 UDP + ADPCM 编解码 的对讲模块，不是 RTSP，也不是 RTP。
它完成的事情是：

从本地麦克风流 R_INTERCOM_AUDIO 读取 PCM 音频
使用 ADPCM 编码
通过 UDP 发给对端
接收对端发来的 ADPCM 包
对丢包进行简单处理和重传请求
解码为 PCM
送入播放流，最终由 DAC 播放


3. 相关流名称在 stream_define.h 中定义了与对讲相关的流：

R_INTERCOM_AUDIO = &quot;INTERCOM_SEND&quot;

含义：对讲发送输入流
作用：麦克风采集出来的 PCM 音频会被送到这个流


S_INTERCOM_AUDIO = &quot;INTERCOM_RECV&quot;

含义：对讲接收输出流
作用：接收到并解码后的 PCM 音频会送往这个流，再进入播放链路


R_SONIC_PROCESS

含义：Sonic 变速播放处理流
作用：intercom_opcode_func() 会把 S_INTERCOM_AUDIO 绑定到这里




4. 上游音频输入从哪里来在 sdk/app/audio_app/bbm_audio_ad.c 中，音频 ADC 流打开后会执行：
streamSrc_bind_streamDest(s, R_INTERCOM_AUDIO);

这说明：

麦克风采集线程输出的 PCM 数据
会自动绑定到 R_INTERCOM_AUDIO
对讲模块只要打开 R_INTERCOM_AUDIO，就能拿到本地麦克风数据

所以，对讲模块不是自己驱动麦克风，而是消费音频采集模块已经产出的数据。

5. 启动入口当前工程里，project/main.c 已经在 CUSTOMER_ID == 8 时，于网络初始化完成后调用：
intercom_init();

这样 ADPCM 对讲会在系统网络起来后自动启动。

6. 顶层初始化流程6.1 intercom_init()入口函数：
void intercom_init(void)

它做了 4 件事：

分配 TYPE_INTERCOM_STRUCT
调 intercom_struct_init() 初始化结构体字段
调 intercom_task_state_init() 初始化运行状态
打开 R_INTERCOM_AUDIO 流，并创建总控线程 intercom_handle_init

关键代码路径：
intercom = INTERCOM_ZALLOC(...)intercom_struct_init()intercom_task_state_init()intercom-&gt;stream_s = open_stream_available(R_INTERCOM_AUDIO, 8, 8, intercom_opcode_func, NULL)OS_TASK_INIT(..., intercom_handle_init, ...)

6.2 这里的函数指针绑定关系这一步有两个很重要的函数指针绑定：
1. open_stream_available(..., intercom_opcode_func, NULL)把流的回调函数指针绑定为：

opcode_func -&gt; intercom_opcode_func

也就是说，R_INTERCOM_AUDIO 这个流在打开、分配节点、释放节点时，会调用 intercom_opcode_func()。
2. OS_TASK_INIT(..., intercom_handle_init, ...)把线程入口函数指针绑定为：

task entry -&gt; intercom_handle_init

所以 intercom_init() 本身不直接做 socket 和任务创建，而是交给 intercom_handle_init() 线程继续完成。

7. intercom_opcode_func() 的作用函数：
static int intercom_opcode_func(stream *s, void *priv, int opcode)

这是 R_INTERCOM_AUDIO 流的回调函数。
7.1 STREAM_OPEN_EXIT流真正打开成功后：

为音频数据节点申请一块缓冲 audio_buf
调 stream_data_dis_mem_custom(s)，让流节点使用这块自定义缓冲
调 streamSrc_bind_streamDest(s, R_SONIC_PROCESS)
enable_stream(s, 1)

这里说明：

对讲接收播放流最终会继续进入 Sonic 处理链
后续 send_to_stream() 输出的 PCM 会送到这个流

7.2 STREAM_DATA_DIS流分配节点时，会设置：

data-&gt;ops = &amp;stream_sound_ops
data-&gt;data = audio_buf + offset

这里的 stream_sound_ops 里绑定了：

get_data_len -&gt; get_sound_data_len
set_data_len -&gt; set_sound_data_len

所以：

节点真实数据长度读写
是通过这两个函数指针完成的

7.3 这里的函数指针绑定关系1. data-&gt;ops = &amp;stream_sound_ops将节点的操作函数表绑定为：

get_data_len -&gt; get_sound_data_len
set_data_len -&gt; set_sound_data_len

2. streamSrc_bind_streamDest(s, R_SONIC_PROCESS)将本流输出进一步绑定到：

R_SONIC_PROCESS

也就是说，解码后的 PCM 不是直接自己推到 DAC，而是先进 Sonic 处理流。

8. 总控线程 intercom_handle_init()函数：
void intercom_handle_init(void *d)

它是 ADPCM 对讲模块的总控初始化线程。
8.1 它做的事情
如果当前是 STA，等待拿到 IP
创建 UDP socket
设置接收超时
绑定本地端口 5008/5009
根据 WiFi 模式设置对端 IP
初始化内部缓冲池 intercom_room_init()
创建 ADPCM 编码器和解码器
初始化互斥锁、信号量、定时器
创建 4 个关键工作线程

8.2 本地与对端地址规则本地监听端口：

5008
5009

对端 IP：

如果 wifi_mode == WIFI_MODE_STA
对端设为 192.168.1.1


否则
对端设为 192.168.1.100



这就是为什么一台做 AP，一台做 STA 时，两边能互发。
8.3 这里创建了哪些线程在这个函数中，使用 csi_kernel_task_new() 创建：
1. 编码线程
task entry -&gt; intercom_encoded_handle

2. 解码播放线程
task entry -&gt; intercom_decoded_handle

3. 网络接收线程
task entry -&gt; intercom_recv

4. 重传处理线程
task entry -&gt; retransfer_check

此外还启动了一个周期定时器：

timer callback -&gt; decode_sem_up

这个定时器每 CODE_MODE 周期触发一次，用来唤醒解码线程工作。

9. 内部房间和缓冲管理：intercom_room_init()函数：
int intercom_room_init(void)

这是对讲模块内部缓冲资源初始化函数。
它做了：

ringbuf_Init(ENCODED_RINGBUF_LEN)
创建发送侧编码包环形缓冲


sort_buf = INTERCOM_ZALLOC(SOFTBUF_LEN)
创建接收侧排序缓存


初始化三个链表：
srcList_head
checkList_head
useList_head


调 srcList_init()
把 sort_buf 切成多个音频节点


调 sublist_init()
初始化接收排序子链表


调 ringbuf_manage_init()
初始化发送环形缓冲管理链表



9.1 三个链表的含义
srcList_head
空闲音频节点池


checkList_head
新收到但还没做丢包检查&#x2F;排序的子链表


useList_head
已经排序好、可供解码播放的子链表




10. 发送链路总览发送方向的主调用链如下：
麦克风采集-&gt; bbm_audio_ad.c 绑定到 R_INTERCOM_AUDIO-&gt; intercom_init()-&gt; open_stream_available(R_INTERCOM_AUDIO, ..., intercom_opcode_func, ...)-&gt; intercom_encoded_handle()-&gt; adpcm_encode()-&gt; push_ringbuf_pre()-&gt; push_ringbuf()-&gt; intercom_send()-&gt; sendto()


11. 编码发送线程 intercom_encoded_handle()函数：
void intercom_encoded_handle(void *d)

11.1 它的主要职责
从 intercom-&gt;stream_s 读取本地 PCM 音频
调 adpcm_encode() 编码
封装协议头
写入发送环形缓冲
调 intercom_send() 发送到对端

11.2 具体流程1. 读取本地音频get_f = recv_real_data(intercom-&gt;stream_s);

这里拿到的是 R_INTERCOM_AUDIO 上的 PCM 数据。
2. 编码adpcm_encode(Enc_Inst, encoded_buf + RESERVE, &amp;code_len, recv_stream_buf, DECODED_DATA_LEN, CODE_BPS);

3. 填协议头前 RESERVE 字节里写入：

sequence
status
sort
timestamp
identify_num
checksum

4. 放入发送环形缓冲push_ringbuf_pre(ENCODED_BUF_BYTE);push_ringbuf(encoded_ringbuf, encoded_buf, ENCODED_BUF_BYTE);

5. 发送intercom_send(send_num);

11.3 特别说明当编码不允许继续时，它会发送一个仅有头部的控制包：

encoded_buf[1] = 0

正常音频包则：

encoded_buf[1] = 1


12. intercom_send() 的作用函数：
void intercom_send(uint8_t num)

作用：

从发送环形缓冲中取出连续的编码包
拷贝到临时发送缓冲
调 sendto() 发给 udp_remote_addr

调用关系：
intercom_encoded_handle()-&gt; intercom_send()-&gt; sendto()

这说明：

真正负责网络发包的是 intercom_send()
intercom_encoded_handle() 负责生产待发编码包


13. 接收链路总览接收方向的主调用链如下：
UDP 收包-&gt; intercom_recv()-&gt; get_audio_sublist()-&gt; get_audio_node()-&gt; lose_packet_check()-&gt; insert_into_useList()-&gt; decode_sem_up()-&gt; intercom_decoded_handle()-&gt; send_to_stream()-&gt; adpcm_decode() / adpcm_decode_plc()-&gt; send_data_to_stream()


14. 网络接收线程 intercom_recv()函数：
void intercom_recv(void *d)

14.1 它的职责
从 UDP socket 接收对端 ADPCM 包
解析包头
把音频有效载荷写入排序缓冲
调 lose_packet_check() 做序号检查和缺包处理

14.2 核心步骤1. 收包rlen = recvfrom(intercom-&gt;udp_local_fd, recv_buf, ...)

2. 校验包头check_sum != calulate_sum(...)

3. 申请一个子链表sublist_l = get_audio_sublist(&amp;intercom-&gt;checkList_head);

4. 把音频数据拆进节点audio_n = get_audio_node(&amp;(sublist_n-&gt;node_head), node_num);

5. 做丢包检查lose_packet_check();


15. 丢包与重排：lose_packet_check()函数：
void lose_packet_check(void)

作用：

检查 sequence 是否连续
检查是不是重复包
如果有丢包，设置 lose_packet 位图
如果当前包有效，插入 useList_head
如果当前正在播放对讲，则通过 ack_local_fd 回发缺包位图

它会调用：

recv_repeat_check()
insert_into_useList()
sendto(intercom-&gt;ack_local_fd, ...)

因此它既负责：

接收包排序
也负责生成重传请求


16. 重传处理线程 retransfer_check()函数：
void retransfer_check(void *d)

作用：

监听 ack_local_fd
收到对端缺包位图
扫描发送环形缓冲管理表
找到对应 sequence 的旧编码包
调 losePacket_retransfer() 重发

调用关系：
对端 lose_packet_check()-&gt; sendto(ack)-&gt; 本端 retransfer_check()-&gt; losePacket_retransfer()-&gt; sendto(data)

所以这套 ADPCM 对讲不是简单裸 UDP，而是带一条轻量重传链路。

17. 解码播放链路总览播放方向的主调用链如下：
decode_sem_up()-&gt; os_sema_up(&amp;decode_sem)-&gt; intercom_decoded_handle()-&gt; send_to_stream()-&gt; get_src_data_f(intercom-&gt;stream_s)-&gt; adpcm_decode() / adpcm_decode_plc()-&gt; send_data_to_stream()


18. 周期唤醒：decode_sem_up()函数：
void decode_sem_up(uint32 *args)

作用很简单：

如果 g_code_sema_init 为真
就 os_sema_up(&amp;decode_sem)

它不是自己解码，而是：

周期唤醒 intercom_decoded_handle()

函数指针绑定关系：

os_timer_init(..., decode_sem_up, ...)
timer callback -&gt; decode_sem_up




19. 解码线程 intercom_decoded_handle()函数：
void intercom_decoded_handle(void *d)

19.1 它的职责
等待 decode_sem
判断当前缓存数量
决定是否开始播放
调 send_to_stream() 逐帧输出 PCM

19.2 它做了什么1. 进入对讲播放模式former_dac_filter_type = get_audio_dac_set_filter_type();audio_dac_set_filter_type(SOUND_INTERCOM);

2. 等待信号量os_sema_down(sem, -1);

3. 统计可播放缓存g_numofcached = get_audio_sublist_count(&amp;intercom-&gt;useList_head);

4. 满足阈值后开始播放if ((play_start_flag &amp; BIT(0)) == 0 &amp;&amp; (g_numofcached &gt; playofwait ...)) &#123;    play_start_flag |= BIT(0);&#125;

5. 调 send_to_stream()send_to_stream(g_numofcached);


20. 真正输出 PCM：send_to_stream()函数：
static int send_to_stream(uint32_t cached)

这是对讲播放链路里最关键的函数。
20.1 它的职责
向 intercom-&gt;stream_s 申请一个输出节点
从 useList_head 取最早的音频子链表
提取编码数据
调 adpcm_decode() 解码
如果缺包则调 adpcm_decode_plc() 做 PLC
把 PCM 写入流节点
send_data_to_stream() 发给下游播放链路

20.2 关键步骤1. 申请输出节点get_f = get_src_data_f(intercom-&gt;stream_s);

2. 如果排序号正好匹配if (g_current_sort == new_sort) &#123;    adpcm_decode(...)&#125;

3. 如果缺包adpcm_decode_plc(...)

4. 设置输出节点属性set_sound_data_len(get_f, DECODED_DATA_LEN * 2);get_f-&gt;type = SET_DATA_TYPE(SOUND, SOUND_INTERCOM);set_stream_data_time(get_f, timestamp);

5. 送到下游send_data_to_stream(get_f);

20.3 下游是谁下游不是这里显式写死的 DAC，而是：

通过 intercom_opcode_func()
streamSrc_bind_streamDest(s, R_SONIC_PROCESS)

先进入 Sonic 处理流，再走音频播放链路。
20.4 变速播放如果 CHANGE_PLAY_SPEED == 1，这个函数还会根据缓存深度调节：

0.9x
1.0x
1.1x

通过：
set_sonic_speed(sonic_priv, play_speed);

这也是为什么工程里如果没把 Sonic 相关符号链接好，会出现 sonic_priv &#x2F; set_sonic_speed 未定义错误。

21. 关键函数调用关系总表21.1 启动链main.c-&gt; intercom_init()   -&gt; intercom_struct_init()   -&gt; intercom_task_state_init()   -&gt; open_stream_available(R_INTERCOM_AUDIO, ..., intercom_opcode_func, ...)   -&gt; OS_TASK_INIT(..., intercom_handle_init, ...)

21.2 总控初始化链intercom_handle_init()-&gt; socket()-&gt; bind()-&gt; intercom_room_init()-&gt; adpcm_encoder_create()-&gt; adpcm_decoder_create()-&gt; csi_kernel_task_new(..., intercom_encoded_handle, ...)-&gt; csi_kernel_task_new(..., intercom_decoded_handle, ...)-&gt; csi_kernel_task_new(..., intercom_recv, ...)-&gt; csi_kernel_task_new(..., retransfer_check, ...)-&gt; os_timer_init(..., decode_sem_up, ...)

21.3 发送链bbm_audio_ad.c-&gt; streamSrc_bind_streamDest(s, R_INTERCOM_AUDIO)intercom_encoded_handle()-&gt; recv_real_data(intercom-&gt;stream_s)-&gt; adpcm_encode()-&gt; push_ringbuf_pre()-&gt; push_ringbuf()-&gt; intercom_send()-&gt; sendto()

21.4 接收链intercom_recv()-&gt; recvfrom()-&gt; get_audio_sublist()-&gt; get_audio_node()-&gt; lose_packet_check()   -&gt; recv_repeat_check()   -&gt; insert_into_useList()   -&gt; sendto(ack)

21.5 重传链retransfer_check()-&gt; recvfrom(ack_local_fd)-&gt; losePacket_retransfer()-&gt; sendto()

21.6 播放链decode_sem_up()-&gt; os_sema_up(&amp;decode_sem)intercom_decoded_handle()-&gt; get_audio_sublist_count()-&gt; send_to_stream()   -&gt; get_src_data_f(intercom-&gt;stream_s)   -&gt; adpcm_decode() / adpcm_decode_plc()   -&gt; set_sound_data_len()   -&gt; send_data_to_stream()


22. 函数指针与回调绑定总结本模块里最重要的函数指针绑定如下：
22.1 流回调open_stream_available(R_INTERCOM_AUDIO, ..., intercom_opcode_func, ...)


opcode callback -&gt; intercom_opcode_func

22.2 节点长度操作函数表data-&gt;ops = &amp;stream_sound_ops


get_data_len -&gt; get_sound_data_len
set_data_len -&gt; set_sound_data_len

22.3 总控线程入口OS_TASK_INIT(..., intercom_handle_init, ...)


task entry -&gt; intercom_handle_init

22.4 编码线程入口csi_kernel_task_new(..., intercom_encoded_handle, ...)


task entry -&gt; intercom_encoded_handle

22.5 解码线程入口csi_kernel_task_new(..., intercom_decoded_handle, ...)


task entry -&gt; intercom_decoded_handle

22.6 接收线程入口csi_kernel_task_new(..., intercom_recv, ...)


task entry -&gt; intercom_recv

22.7 重传线程入口csi_kernel_task_new(..., retransfer_check, ...)


task entry -&gt; retransfer_check

22.8 定时器回调os_timer_init(..., decode_sem_up, ...)


timer callback -&gt; decode_sem_up


23. 运行时的几个关键状态变量
send_start_flag

是否允许发送


play_start_flag

bit0：是否开始播放
bit1：是否允许接收播放


intercom_encode_flag

bit0：手动编码开关
bit1：软件编码开关


g_current_sort

当前期待播放的排序号


g_numofcached

当前可播放缓存帧数


g_s_identify_num

本轮发送会话 ID




24. 常见问题理解24.1 为什么 intercom_init() 不是全部初始化的地方因为它只负责：

建结构
开流
起总控线程

真正的 socket、缓冲池、编解码器、工作线程初始化，都在 intercom_handle_init() 里。
24.2 为什么会报 sonic_priv &#x2F; set_sonic_speed 未定义因为 send_to_stream() 中启用了：
#define CHANGE_PLAY_SPEED 1

这样会依赖 Sonic 变速播放模块。
24.3 为什么会报 err:intercom_handle_init 582因为这说明：
intercom_room_init() == -1

也就是内部缓冲区初始化失败，通常和：

ringbuf_Init()
sort_buf 分配失败

有关。

25. 一句话总结intercom_adpcm.c 的本质是一套：
“麦克风 PCM -&gt; ADPCM 编码 -&gt; UDP 发送 -&gt; UDP 接收 -&gt; 丢包检查&#x2F;重传 -&gt; ADPCM 解码 -&gt; PCM 播放”
的完整对讲模块。
其中最核心的函数调用主链是：
intercom_init()-&gt; intercom_handle_init()-&gt; intercom_encoded_handle() / intercom_recv() / intercom_decoded_handle() / retransfer_check()

如果要理解模块，优先抓住这 4 条线程链路即可。
]]></content>
      <categories>
        <category>项目文档</category>
        <category>TXW82x ADPCM 对讲</category>
      </categories>
      <tags>
        <tag>嵌入式</tag>
        <tag>C语言</tag>
        <tag>MSI</tag>
        <tag>TXW82x</tag>
        <tag>音频</tag>
        <tag>ADPCM</tag>
        <tag>对讲</tag>
        <tag>UDP</tag>
      </tags>
  </entry>
  <entry>
    <title>source/css 样式文档</title>
    <url>/2026/05/10/%E6%A0%B7%E5%BC%8F%E6%96%87%E6%A1%A3/</url>
    <content><![CDATA[
项目: Blog-Hexo (Butterfly 主题定制)入口文件: custom.css模块目录: modules&#x2F;最后更新: 2026-05-10


目录
架构概览

文件分类

模块详解

主题系统
全局样式
布局组件
内容排版
卡片与表面
页面专用
响应式设计


标签索引



架构概览source/css/├── custom.css└── modules/    ├── variables.css    ├── background.css    ├── base.css    ├── scrollbar.css    ├── cards.css    ├── navigation.css    ├── code-highlight.css    ├── tables.css    ├── typography.css    ├── about-page.css    ├── header-text.css    ├── footer.css    ├── post-meta.css    └── responsive.css

所有模块由 custom.css 通过 @import url(…) 统一引入，形成完整样式链路。

文件分类


分类
标签
包含文件



主题系统
theme &#x2F; variable &#x2F; light-mode &#x2F; dark-mode
variables.css


全局样式
global &#x2F; background &#x2F; scrollbar &#x2F; base
custom.css &#x2F; background.css &#x2F; base.css &#x2F; scrollbar.css


布局组件
layout &#x2F; component &#x2F; navigation &#x2F; header &#x2F; footer
navigation.css &#x2F; header-text.css &#x2F; footer.css


内容排版
content &#x2F; typography &#x2F; code &#x2F; highlight &#x2F; table &#x2F; meta
typography.css &#x2F; tables.css &#x2F; code-highlight.css &#x2F; post-meta.css


卡片与表面
card &#x2F; surface &#x2F; shadow
cards.css


页面专用
page &#x2F; about
about-page.css


响应式设计
responsive &#x2F; mobile &#x2F; media-query
responsive.css



模块详解主题系统variables.css - CSS 自定义属性
标签:

#theme
#variable
#light-mode
#dark-mode


功能:定义明亮模式与暗黑模式的 CSS 变量。

主要变量:





变量名
用途



–hl-bg
代码块背景


–primary-color
主题色


–hover-color
悬停颜色


–text-color
正文颜色


–card-bg
卡片背景


–card-border
卡片边框


–card-shadow
卡片阴影



主题选择器:
html:not([data-theme&#x3D;”dark”])html[data-theme&#x3D;”dark”]



全局样式custom.css - 主入口
标签:

#entry
#import
#aggregator


功能:聚合所有 CSS 模块。

引入顺序:



variables.css
background.css
base.css
scrollbar.css
cards.css
navigation.css
code-highlight.css
tables.css
typography.css
about-page.css
header-text.css
footer.css
post-meta.css
responsive.css


background.css - 网站背景
标签:

#background
#light-mode
#dark-mode


功能:根据主题切换背景图。

背景图:





模式
图片



明亮模式
&#x2F;img&#x2F;light-bg.webp


暗黑模式
&#x2F;img&#x2F;dark-bg.webp



目标元素:
#web_bg



base.css - 基础调整
标签:

#base
#global


功能:对 pre 元素统一设置圆角与间距。

规则:
border-radius: 8pxmargin: 1em 0



scrollbar.css - 滚动条样式
标签:

#scrollbar
#global


功能:自定义 Webkit 滚动条。

规格:





属性
数值



宽度
8px


高度
8px


圆角
4px



布局组件navigation.css - 导航栏
标签:

#navigation
#component
#layout


功能:控制导航栏颜色、悬停效果与固定导航栏样式。

特性:

标题强制白色
下拉菜单悬停变蓝
固定导航栏支持毛玻璃效果




header-text.css - 页头文字
功能:控制页面标题颜色。


footer.css - 页脚
功能:页脚文字统一白色。


内容排版typography.css - 排版
标签:

#typography
#content


功能:

页面标题居中
正文两端对齐
视频自适应
Mermaid 透明背景




tables.css - 表格
功能:定义明亮与暗黑模式下的表格样式。


code-highlight.css - 代码高亮
标签:

#code
#highlight
#vscode
#dark-mode
#light-mode
#inline


功能:

代码块 (figure.highlight) 双主题 Token 配色：明亮 (GitHub Primer 风格) &#x2F; 暗黑 (VSCode Dark+ Modern)
行内代码 (.container code) 双主题适配：明亮 #cf222e &#x2F; 暗黑 #ce9178




post-meta.css - 文章元信息
功能:控制文章标题、日期、标签等颜色。


卡片与表面cards.css - 卡片表面
标签:

#card
#surface
#shadow


功能:统一所有卡片容器样式。

包含:

文章卡片
侧边栏
分页按钮
导航菜单
TOC




页面专用about-page.css - 关于页面
标签:

#about
#page
#hero
#grid


功能:构建完整 About 页面布局。

模块:

Hero 区域
信息网格
社交链接
响应式布局




响应式设计responsive.css - 移动端适配
标签:

#responsive
#mobile


功能:优化移动端文章信息区布局。

调整:

缩减 padding
隐藏部分 meta
标题两端对齐




标签索引


标签
涉及文件



#theme
variables.css


#background
background.css


#scrollbar
scrollbar.css


#navigation
navigation.css


#header
header-text.css


#footer
footer.css


#typography
typography.css


#table
tables.css


#code
code-highlight.css


#highlight
code-highlight.css


#vscode
code-highlight.css


#inline
code-highlight.css


#meta
post-meta.css


#card
cards.css


#about
about-page.css


#responsive
responsive.css


]]></content>
      <categories>
        <category>博客开发</category>
      </categories>
      <tags>
        <tag>CSS</tag>
        <tag>样式</tag>
        <tag>主题定制</tag>
        <tag>Butterfly</tag>
        <tag>Hexo</tag>
        <tag>前端</tag>
        <tag>文档</tag>
      </tags>
  </entry>
  <entry>
    <title>Spook 图传模块分析文档</title>
    <url>/2026/07/03/Spook%E5%9B%BE%E4%BC%A0%E6%A8%A1%E5%9D%97%E5%88%86%E6%9E%90%E6%96%87%E6%A1%A3/</url>
    <content><![CDATA[Spook 图传模块分析文档1. 文档范围本文档分析 sdk/app/spook/ 目录中与图传直接相关的实现，重点覆盖以下内容：

spook 模块初始化
实时 RTSP 图传主流程
stream 分发机制
RTSP 会话建立与播放控制
RTP&#x2F;JPEG 实际发包流程
音频辅助链路
函数指针绑定关系

本文档主要针对实时预览链路 /webcam，同时补充 /file 文件回放链路与其复用关系。

2. 目录中与图传最相关的文件2.1 核心入口
sdk/app/spook/spook.c
sdk/app/spook/spook_config.h

2.2 实时 RTSP 控制
sdk/app/spook/live.c
sdk/app/spook/rtsp.c
sdk/app/spook/rtsp_common.c
sdk/app/spook/session.c

2.3 帧和流分发
sdk/app/spook/frame.c
sdk/app/spook/frame.h
sdk/app/spook/stream.c
sdk/app/spook/stream.h

2.4 编码器输出层
sdk/app/spook/encoder-jpeg.c
sdk/app/spook/encoder-audio.c

2.5 RTP 媒体发送层
sdk/app/spook/rtp-jpeg.c
sdk/app/spook/rtp-audio.c
sdk/app/spook/rtp.c
sdk/app/spook/rtp.h
sdk/app/spook/rtp_media.h

2.6 文件回放复用链路
sdk/app/spook/webfile.c


3. 模块总体架构spook 图传模块可以分成 5 层：

初始化层创建 JPEG&#x2F;Audio 输出流，注册 RTSP 路径。

取流层从底层视频流和音频流中取出原始帧。

Stream 分发层将取出的帧分发给所有订阅该 stream 的 destination。

RTSP 控制层处理 DESCRIBE、SETUP、PLAY、TEARDOWN。

RTP 媒体层将 JPEG&#x2F;Audio 帧封装为 RTP 包，通过 UDP 发给客户端。


对实时图传来说，核心协议是：

控制协议：RTSP
传输协议：RTP&#x2F;UDP
视频编码格式：MJPEG
视频 Payload Type：26
音频编码格式：L16


4. 初始化流程入口在 sdk/app/spook/spook.c 的 spook_init()。
4.1 调用顺序spook_init()-&gt; spook_thread()-&gt; init_random()-&gt; global_init()-&gt; jpeg_init(&amp;live_dvp)-&gt; rtp_audio_init_ret()-&gt; live_rtsp_init(&amp;live_dvp)-&gt; webfile_rtsp_init(&amp;webfile)
4.2 关键配置在 spook.c 中定义了两个重要 rtp_name：
实时预览 live_dvp
jpg_name = &quot;jpeg_dvp&quot;
audio_name = &quot;audio&quot;
path = &quot;/webcam&quot;

文件回放 webfile
jpg_name = &quot;jpeg_dvp&quot;
audio_name = &quot;audio&quot;
path = &quot;/file&quot;

4.3 初始化后的结果初始化完成后，系统中会存在：

一个 JPEG 输出 stream：jpeg_dvp
一个音频输出 stream：audio
一个 RTSP 路径：/webcam
一个 RTSP 路径：/file


5. 实时图传主流程实时图传的完整链路如下：

客户端访问 /webcam
RTSP 完成 DESCRIBE
RTSP 完成 SETUP
SETUP 阶段创建实时取流线程
RTSP 完成 PLAY
track 进入 ready 状态
实时线程不断从底层流中取出 JPEG 帧
JPEG 帧进入 stream 分发
RTSP track 收到帧后解析 JPEG
JPEG 被切分为多个 RTP&#x2F;JPEG 包
通过 UDP 向所有正在播放的 session 发送


6. 底层取流与帧注入流程6.1 live_rtsp_creat()实时线程是在 sdk/app/spook/live.c 的 live_rtsp_creat() 中创建的。
这个函数主要做三件事：

分配 struct rtsp_priv
调用 creat_stream() 打开底层视频流和音频流
创建线程 rtsp_live_thread

线程入口为：
rtsp_live_thread()-&gt; spook_send_thread_stream(r)
6.2 creat_stream()sdk/app/spook/frame.c 中的 creat_stream() 打开底层数据流：

视频：open_stream(video_name, 0, 2, opcode_func, NULL)
音频：open_stream(audio_name, 0, 2, opcode_func, NULL)

实时模式下传入的是：

视频流名：R_RTP_JPEG
音频流名：R_RTP_AUDIO

6.3 spook_send_thread_stream()sdk/app/spook/frame.c 中的 spook_send_thread_stream() 是实时图传的“数据泵”。
它的职责是：

从底层视频流 GET_FRAME(s) 取到 JPEG 帧
把底层 frame 信息填入 struct frame
将首段 JPEG 头部拷贝到 scan_buf
调用 ex-&gt;f(jpeg, ex-&gt;d) 将帧交给 JPEG encoder 输出链路

对于音频也是类似逻辑：

GET_FRAME(audio_s)
填充 audio frame
调用 audio_ex-&gt;f(audio, audio_ex-&gt;d)

6.4 为什么先拷贝前 1024 字节spook_send_thread_stream() 中：

jpeg-&gt;d = scan_buf
jpeg-&gt;first_length = 1024

这样做的目的，是给 rtp-jpeg.c 里的 JPEG marker 解析器一个可连续访问的头部区域，便于快速扫描：

SOI
DQT
SOF0
DHT
DRI
SOS


7. JPEG 输出流与 Stream 分发7.1 jpeg_init()sdk/app/spook/encoder-jpeg.c 的 jpeg_init() 会：

start_block()
set_output(&quot;jpeg_dvp&quot;, en)
end_block(en)

7.2 创建的对象set_output() 中会调用：
new_stream(name, FORMAT_JPEG, en)
于是产生一个 stream：

stream 名称：jpeg_dvp
stream 格式：FORMAT_JPEG
stream 私有指针：jpeg_encoder *en

7.3 JPEG stream 自带两个方法encoder-jpeg.c 会给该 stream 绑定：

get_framerate = get_framerate
set_running = set_running

其中：

get_framerate() 返回帧率基准
set_running() 用于根据是否有人监听来开启或关闭发送状态

7.4 frame_exchanger 的作用end_block() 中创建：
en-&gt;ex = new_exchanger(EXCHANGER_SLOT_SIZE, get_back_frame, en)
这里的 frame_exchanger 保存了：

jf：当前 frame 容器
f：frame 到达时的回调
d：回调私有数据

对于视频来说，回调函数是 get_back_frame()。
7.5 get_back_frame()get_back_frame() 很简单，只做一件事：
deliver_frame_to_stream(f, en-&gt;output)
也就是说：
实时线程取到 JPEG 帧-&gt; 填充 frame-&gt; 调 get_back_frame()-&gt; 交给 stream 分发层

8. Stream 分发机制8.1 connect_to_stream()RTSP track 会调用 connect_to_stream(&quot;jpeg_dvp&quot;, rtsp_common_send, &amp;track) 接入 JPEG 输出 stream。
这样会创建一个 stream_destination：

dest-&gt;stream = jpeg_dvp
dest-&gt;process_frame = rtsp_common_send
dest-&gt;d = &amp;source-&gt;track[t]

8.2 deliver_frame_to_stream()sdk/app/spook/stream.c 中：
deliver_frame_to_stream(f, s)
会遍历 s-&gt;dest_list 上的所有目的端：

如果 dest-&gt;waiting == 0，则跳过
如果 dest-&gt;waiting == 1，则调用dest-&gt;process_frame(f, dest-&gt;d)

对 RTSP 来说，这里的 process_frame 就是：
rtsp_common_send()
所以链路变成：
deliver_frame_to_stream()-&gt; rtsp_common_send()
8.3 waiting 的意义只有当客户端真正 PLAY 之后，RTSP track 才会 set_waiting(..., 1)，这样 deliver_frame_to_stream() 才会把 frame 分发出去。
因此：

DESCRIBE 不发视频
SETUP 不发视频
只有 PLAY 后开始发视频


9. RTSP 路径注册与会话控制9.1 注册 RTSP 路径sdk/app/spook/live.c 的 live_rtsp_init() 做了以下事情：

source = rtsp_start_block()
live_set_path(rtsp-&gt;path, source)
set_video_track(rtsp-&gt;jpg_name, source)
set_audio_track(rtsp-&gt;audio_name, source)
register_live_fn(source, live_rtsp_creat, live_rtsp_destory)
rtsp_end_block(source)

也就是说，/webcam 对应一个 rtsp_source 对象。
9.2 rtsp_sourcertsp_source 可以理解为“一组 RTSP 资源描述”，里面有：

session 链表
track 数组
live thread 句柄
live_node
create&#x2F;release 回调

9.3 rtsp_open()当客户端发起 DESCRIBE 或 SETUP 时，会调用 loc-&gt;open(path, private)。
对实时流来说：
loc-&gt;open-&gt; live_rtsp_open()-&gt; rtsp_open(path, source)
rtsp_open() 会创建 struct session 并绑定 RTSP 控制回调：

get_sdp
setup
play
teardown
closed
select_close

9.4 live_rtsp_open() 的覆盖live_rtsp_open() 会把默认的 sess-&gt;get_sdp 覆盖为 live_get_sdp()。
所以实时流用的是 live_get_sdp()，不是默认的 rtsp_get_sdp()。

10. RTSP 命令时序10.1 DESCRIBE调用链：
rtsp_handle_msg()-&gt; handle_DESCRIBE()-&gt; find_rtsp_location()-&gt; loc-&gt;open()-&gt; sess-&gt;get_sdp()
返回 SDP 内容，其中视频轨使用：
m=video &lt;port&gt; RTP/AVP 26
10.2 SETUP调用链：
rtsp_handle_msg()-&gt; handle_SETUP()-&gt; s-&gt;setup(s, track)-&gt; rtsp_setup()
rtsp_setup() 做两件关键事情：

如果 live thread 还没有启动，则调用 source-&gt;creat(source, path)
为该 track 创建 rtp_endpoint

在实时模式中：

source-&gt;creat = live_rtsp_creat

所以 RTSP 的后台实时线程，是在 SETUP 时启动的，不是在 DESCRIBE 时启动。
10.3 PLAY调用链：
rtsp_handle_msg()-&gt; handle_PLAY()-&gt; sess-&gt;play(sess, ...)-&gt; rtsp_play()
rtsp_play() 会：

track-&gt;ready = 1
set_waiting(track-&gt;stream, 1)

这一步之后，deliver_frame_to_stream() 才会开始把帧投递给 RTSP。
10.4 TEARDOWN调用链：
rtsp_handle_msg()-&gt; handle_TEARDOWN()-&gt; sess-&gt;closed(...)
注意这里当前代码调用的是 closed()，而不是直接 teardown()。后续在 rtsp_frame_end() 或连接关闭流程中，会进一步进入 teardown 和资源释放。

11. Track 建立过程11.1 set_video_track()sdk/app/spook/rtsp_common.c 中的 set_video_track() 是视频图传最关键的绑定函数之一。
它主要做了四件事：

connect_to_stream(name, rtsp_common_send, &amp;source-&gt;track[t])
source-&gt;live_node.video_ex = get_video_ex(...)
disconnect_stream(..., rtsp_frame_end)
source-&gt;track[t].rtp = new_rtp_media_jpeg_stream(...)

11.2 结果执行完 set_video_track(&quot;jpeg_dvp&quot;, source) 后，建立了以下关系：

RTSP track 订阅了 JPEG stream
track 的 frame 回调是 rtsp_common_send
track 的断开回调是 rtsp_frame_end
track 的 RTP 媒体实现是 rtp-jpeg.c

11.3 set_audio_track()音频轨做法完全类似，只是 RTP 媒体实现换成 rtp-audio.c。

12. 视频 RTP 媒体对象创建12.1 new_rtp_media_jpeg_stream()sdk/app/spook/rtp-jpeg.c 中：

通过 stream-&gt;get_framerate() 获取帧率
创建 struct rtp_jpeg
创建 struct rtp_media
绑定 JPEG 相关函数

12.2 绑定内容视频 rtp_media 的函数绑定如下：

get_sdp -&gt; jpeg_get_sdp
get_payload -&gt; jpeg_get_payload
frame -&gt; jpeg_process_frame
send -&gt; jpeg_send
rtcp_send -&gt; new_rtcp_send
send_more -&gt; jpeg_send_more

并设置：

m-&gt;type = 0
m-&gt;per_ms_incr = (25 * out-&gt;ts_incr) / 1000

12.3 重要结论当前视频真正使用的是：

frame = jpeg_process_frame
send_more = jpeg_send_more

虽然 send = jpeg_send 也被绑定了，但在 rtsp_common_send() 中原本直接调用 send() 的代码已经注释掉，所以视频主链路实际不走 jpeg_send()。

13. JPEG 解析流程13.1 入口 jpeg_process_frame()每一帧到达 rtsp_common_send() 后，首先执行：
track-&gt;rtp-&gt;frame(f, track-&gt;rtp)
对于视频 track，这里实际调用的是：
jpeg_process_frame()
13.2 解析逻辑jpeg_process_frame() 会在 f-&gt;d 指向的 JPEG 头部区域中依次扫描 marker：

0xDB -&gt; DQT
0xC0 -&gt; SOF0
0xC4 -&gt; DHT
0xDD -&gt; DRI
0xDA -&gt; SOS

13.3 各子解析函数作用parse_DQT()解析量化表，并记录量化表相对偏移：

out-&gt;quant[table_id] = offset

注意这里记录的不是裸地址，而是相对于 out-&gt;d 的偏移。这是为了兼容 PSRAM 和不同帧缓存布局。
parse_SOF()解析图像基础信息：

宽度
高度
采样因子
亮度量化表
色度量化表

其中：

0x21 表示 YUV422
0x22 表示 YUV420

parse_DHT()记录 Huffman 表位置和长度，用于扩展头部发送。
parse_DRI()解析 JPEG Restart Interval。
SOS遇到 0xDA 时说明已经到达 scan data：

out-&gt;scan_data = f-&gt;d + i + 14
out-&gt;scan_data_len = ...
out-&gt;offset = out-&gt;scan_data - f-&gt;d

此时返回成功。
13.4 时间戳处理jpeg_process_frame() 中：
out-&gt;timestamp = per_ms_incr * f-&gt;timestamp
其中 per_ms_incr 与 90kHz RTP 时钟有关，表示把系统时间戳转换成 RTP 视频时间戳。

14. 视频 RTP&#x2F;JPEG 发包流程14.1 发包入口rtsp_common_send() 在调用完 frame() 后，如果 send_more 存在，则执行：
track-&gt;rtp-&gt;send_more(loop_search_ep, ls, track, track-&gt;rtp-&gt;private)
对视频来说，这里就是：
jpeg_send_more()
14.2 jpeg_send_more() 做了什么这个函数是当前实时图传最核心的发包实现，它做了以下事情：

取出当前 frame 对应的底层 JPEG buffer
计算 scan data 长度
构造 RTP&#x2F;JPEG 专用头
准备 quant table
计算每个 RTP 分片的长度
遍历 JPEG 链表节点
为每个 session 的 endpoint 分别补 RTP 头
发送 UDP 数据包

14.3 RTP&#x2F;JPEG 头部构成视频 RTP payload 头包括：

12 字节标准 RTP 头
8 或 12 字节 JPEG 专用头
4 字节量化表头
64 字节亮度量化表
64 字节色度量化表
可选扩展头和 Huffman 数据

14.4 分片策略最大发送长度取自：

MAX_DATA_PACKET_SIZE = 1440

再减去各类头部长度，得到本片允许承载的 JPEG 数据长度。
如果当前 JPEG scan data 超过可发送大小，就切成多片。
14.5 多 session 广播jpeg_send_more() 并不是只发给一个客户端。它通过 loop_search_ep() 遍历所有正在播放、且该 track 有 endpoint 的 session：

如果 session 正在 PLAY
且 track-&gt;ready == 1
且 sess-&gt;ep[track-&gt;index] 非空

就发送给该客户端。
因此，同一帧 JPEG 可以广播给多个 RTSP 客户端。
14.6 真正的 RTP 头生成每发给一个 endpoint 时，调用：
set_send_rtp_packet_head(ep, v, count, timestamp, marker, send_buf)
这个函数会：

写 RTP version
写 payload type
写 marker bit
写 sequence number
写 timestamp
写 SSRC

14.7 真正的发送动作再调用：
send_rtp_packet_more(ep, send_buf, send_total_len, 30)
而 send_rtp_packet_more() 内部继续走：
fd_send_data(ep-&gt;trans.udp.rtp_fd, sendbuf, sendLen, times)
最终使用 UDP socket 发出。
14.8 jpeg_send() 现状rtp-jpeg.c 中的 jpeg_send() 当前直接返回 0，没有实际发送逻辑。因为现在主链路已经迁移到 jpeg_send_more()。

15. 音频链路音频链路与视频结构几乎一致，只是媒体格式不同。
15.1 音频输出流rtp_audio_init_ret() 创建输出 stream：

名称：audio
格式：FORMAT_AUDIO

15.2 音频 RTP 媒体对象new_rtp_media_audio_stream() 绑定：

get_sdp -&gt; audio_get_sdp
get_payload -&gt; audio_get_payload
frame -&gt; audio_process_frame
send -&gt; audio_send
rtcp_send -&gt; new_rtcp_send
send_more -&gt; audio_send_more

并设置：

sample_rate = 8000
type = 1

15.3 SDP音频 SDP 格式为：
m=audio &lt;port&gt; RTP/AVP 97a=rtpmap:97 L16/&lt;sample_rate&gt;/1
15.4 实际发送音频和视频一样，当前主要也是走 send_more 路径，即：
audio_send_more()
不是依赖 audio_send()。

16. RTP Endpoint 建立流程16.1 new_rtp_endpoint()在 SETUP 时，为每个 track 创建一个 rtp_endpoint。
该结构中保存：

payload type
seqnum
SSRC
RTP timestamp
UDP 或 Interleaved 传输信息

16.2 UDP 模式当前主要使用 UDP 模式：
connect_udp_endpoint()
它会：

创建 RTP socket
创建 RTCP socket
绑定本地端口
connect 到客户端端口
在 event loop 中注册 RTP&#x2F;RTCP 读事件

16.3 RTCPnew_rtcp_send() 周期性向客户端发送 RTCP Sender Report。该函数会填充：

NTP 时间
RTP 时间戳
packet count
octet count


17. 会话停止与资源释放17.1 Track 停止发送当 session 被 teardown 或 closed 后，track_check_running() 会检查是否还有其他 session 在播放该 track。
如果没有，则：
set_waiting(source-&gt;track[t].stream, 0)
这样 stream 不再继续向该 track 分发 frame。
17.2 endpoint 释放rtsp_teardown() 中：

删除 rtp_endpoint
清空 sess-&gt;ep[i]

17.3 live 线程释放当最后一个 session 离开后，会最终进入：

source-&gt;release(source)

对于实时模式来说，对应：

live_rtsp_destory()

这个函数会：

关闭附加流
destory_stream(r)
删除 live task
释放 rtsp_priv


18. /file 文件回放链路sdk/app/spook/webfile.c 注册了 /file 路径。
它与 /webcam 的区别主要只有两点：

create&#x2F;release 回调不同
数据源来自 AVI 文件回放，不是实时摄像头流

但它复用了完全相同的：

RTSP 框架
track 机制
rtsp_common_send()
rtp-jpeg.c
rtp-audio.c
rtp.c

因此可以把 /file 看成“更换数据源后的同一套图传框架”。

19. 关键函数指针绑定关系本节只列图传最关键的函数指针最终指向。
19.1 stream 层JPEG 输出 stream
stream-&gt;get_framerate -&gt; encoder-jpeg.c:get_framerate
stream-&gt;set_running -&gt; encoder-jpeg.c:set_running

Audio 输出 stream
stream-&gt;get_framerate -&gt; encoder-audio.c:get_framerate
stream-&gt;set_running -&gt; encoder-audio.c:set_running

19.2 frame_exchanger视频 exchanger
ex-&gt;f -&gt; encoder-jpeg.c:get_back_frame
ex-&gt;d -&gt; jpeg_encoder *en

音频 exchanger
audio_ex-&gt;f -&gt; encoder-audio.c:get_back_audio_frame
audio_ex-&gt;d -&gt; audio_encoder *en

19.3 stream_destinationRTSP 视频&#x2F;音频 track 接入 stream 时
dest-&gt;process_frame -&gt; rtsp_common.c:rtsp_common_send
dest-&gt;disconnect_frame -&gt; rtsp_common.c:rtsp_frame_end
dest-&gt;d -&gt; struct rtsp_track *

19.4 sessionrtsp_open() 里绑定：

sess-&gt;get_sdp -&gt; rtsp_get_sdp
sess-&gt;setup -&gt; rtsp_setup
sess-&gt;play -&gt; rtsp_play
sess-&gt;teardown -&gt; rtsp_teardown
sess-&gt;closed -&gt; rtsp_closed
sess-&gt;select_close -&gt; rtsp_select_close

实时流 live_rtsp_open() 会进一步覆盖：

sess-&gt;get_sdp -&gt; live_get_sdp

19.5 rtsp_source在 register_live_fn() 中绑定：

source-&gt;creat -&gt; live_rtsp_creat
source-&gt;release -&gt; live_rtsp_destory

文件回放模式中绑定为：

source-&gt;creat -&gt; webfile_rtsp_creat
source-&gt;release -&gt; webfile_rtsp_destory

19.6 视频 rtp_medianew_rtp_media_jpeg_stream() 中绑定：

rtp-&gt;get_sdp -&gt; jpeg_get_sdp
rtp-&gt;get_payload -&gt; jpeg_get_payload
rtp-&gt;frame -&gt; jpeg_process_frame
rtp-&gt;send -&gt; jpeg_send
rtp-&gt;send_more -&gt; jpeg_send_more
rtp-&gt;rtcp_send -&gt; new_rtcp_send

19.7 音频 rtp_medianew_rtp_media_audio_stream() 中绑定：

rtp-&gt;get_sdp -&gt; audio_get_sdp
rtp-&gt;get_payload -&gt; audio_get_payload
rtp-&gt;frame -&gt; audio_process_frame
rtp-&gt;send -&gt; audio_send
rtp-&gt;send_more -&gt; audio_send_more
rtp-&gt;rtcp_send -&gt; new_rtcp_send


20. 最重要的主调用链下面给出实时视频图传的主调用链。
20.1 初始化阶段spook_init()-&gt; spook_thread()-&gt; jpeg_init(&amp;live_dvp)-&gt; live_rtsp_init(&amp;live_dvp)
20.2 RTSP 建链阶段DESCRIBE-&gt; handle_DESCRIBE()-&gt; live_rtsp_open()-&gt; rtsp_open()-&gt; live_get_sdp()
SETUP-&gt; handle_SETUP()-&gt; rtsp_setup()-&gt; live_rtsp_creat()-&gt; creat_stream()-&gt; new_rtp_endpoint()-&gt; connect_udp_endpoint()
PLAY-&gt; handle_PLAY()-&gt; rtsp_play()-&gt; set_waiting(track-&gt;stream, 1)
20.3 实时帧发送阶段spook_send_thread_stream()-&gt; GET_FRAME(video_stream)-&gt; 填充 struct frame-&gt; ex-&gt;f(jpeg, ex-&gt;d)-&gt; get_back_frame()-&gt; deliver_frame_to_stream()-&gt; rtsp_common_send()-&gt; track-&gt;rtp-&gt;frame(...)-&gt; jpeg_process_frame()-&gt; track-&gt;rtp-&gt;send_more(...)-&gt; jpeg_send_more()-&gt; set_send_rtp_packet_head()-&gt; send_rtp_packet_more()-&gt; fd_send_data()
20.4 结束阶段TEARDOWN 或连接关闭-&gt; rtsp_closed() &#x2F; rtsp_teardown()-&gt; del_rtp_endpoint()-&gt; track_check_running()-&gt; set_waiting(..., 0)-&gt; 最后一个 session 结束时 live_rtsp_destory()

21. 关键实现结论21.1 当前视频图传编码不是 H264，而是 MJPEG证据：

jpeg_get_payload() 固定返回 26
SDP 中 m=video ... RTP/AVP 26

21.2 当前视频主发送逻辑走的是 send_morertsp_common_send() 中直接 send() 的代码已注释。因此当前视频主要依赖：

jpeg_process_frame()
jpeg_send_more()

21.3 SETUP 阶段才启动后台实时线程不是在 DESCRIBE 阶段启动。
21.4 PLAY 之后才真正开始图传必须等 track-&gt;ready = 1 且 waiting = 1。
21.5 同一帧支持广播给多个客户端jpeg_send_more() 会遍历 session 链表，把同一帧发送给多个 rtp_endpoint。
21.6 /file 和 /webcam 复用同一套 RTP&#x2F;RTSP 架构区别主要只是底层数据源和 source 的 create&#x2F;release 回调不同。

22. 建议后续继续关注的点如果后续还要继续深入排查图传问题，建议重点看下面几个方向：

spook_send_thread_stream() 中底层 JPEG buffer 的组织方式
stream_frame.h &#x2F; GET_JPG_BUF() &#x2F; GET_DATA_BUF() 相关宏
jpeg_send_more() 中链表节点拆包逻辑
fd_send_data() 的重发与丢包处理
RTCP 收包对 rtp_speed 的反馈调节逻辑


23. 关键源码索引初始化与入口
sdk/app/spook/spook.c
sdk/app/spook/spook_config.h

实时 RTSP 入口
sdk/app/spook/live.c

Stream&#x2F;Frame 分发
sdk/app/spook/frame.c
sdk/app/spook/frame.h
sdk/app/spook/stream.c
sdk/app/spook/stream.h

RTSP 控制
sdk/app/spook/rtsp.c
sdk/app/spook/rtsp_common.c
sdk/app/spook/session.c

RTP&#x2F;JPEG 与音频
sdk/app/spook/rtp-jpeg.c
sdk/app/spook/rtp-audio.c
sdk/app/spook/rtp.c
sdk/app/spook/rtp.h
sdk/app/spook/rtp_media.h

文件回放
sdk/app/spook/webfile.c

]]></content>
      <categories>
        <category>项目文档</category>
        <category>TXW82x 图传</category>
      </categories>
      <tags>
        <tag>嵌入式</tag>
        <tag>C语言</tag>
        <tag>MSI</tag>
        <tag>TXW82x</tag>
        <tag>JPEG</tag>
        <tag>RTSP</tag>
        <tag>RTP</tag>
        <tag>Wi-Fi图传</tag>
        <tag>Spook</tag>
        <tag>音频</tag>
      </tags>
  </entry>
  <entry>
    <title>TXW828 AI 闹钟 — UI 模块工程说明文档</title>
    <url>/2026/05/09/TXW828-AI%E9%97%B9%E9%92%9FUI%E6%A8%A1%E5%9D%97%E5%B7%A5%E7%A8%8B%E8%AF%B4%E6%98%8E%E6%96%87%E6%A1%A3/</url>
    <content><![CDATA[TXW828 AI 闹钟 — UI 模块工程说明文档
适用对象： 接手此项目的开发者、团队成员最后更新： 2026-05-09LVGL 版本： LVGL 8.x（见 sdk/lib/lvgl/）硬件平台： TXW828（C-SKY 架构）


目录
模块概览
目录结构
核心架构
各模块详细说明
数据流与刷新机制
公共 API 参考
添加新页面的步骤
依赖关系
已知限制与注意事项
编译与调试


一、模块概览1.1 这个项目是什么这是一个基于 TXW828 芯片的 AI 闹钟产品的 UI 界面。设备配备一个小尺寸 LCD 屏幕（约 320×240），用户通过左右滑动切换功能页面，点击进入各功能的详情界面。
1.2 UI 包含哪些功能页面


页面
功能
代码路径



Home（主页）
显示时间、日期、天气、WiFi 状态、电量
home/


Plan List（计划列表）
日期选择条 + 待办事项管理
plan_list/


AI Q&amp;A（AI 问答）
AI 对话入口页面
ai_qa/


Timing（计时器）
倒计时 &#x2F; 秒表 &#x2F; 番茄钟选择器
timing/


Calendar（日历）
月历视图 + 年月选择器
calendar/


Settings（设置）
WiFi、蓝牙、亮度等 11 项设置
settings/


1.3 技术栈
GUI 框架： LVGL 8.x
语言： C99
RTOS： AliOS（通过 ohos/kernel/）
服务层： 自定义 service 层（project/service/）
构建工具： CDK（project/txw82xApp.cdkproj）


二、目录结构project/ui/│├── ui_theme.h                  ── [基础] 统一颜色、字体、尺寸常量├── ui_back_button.h/.c         ── [基础] 可复用返回按钮组件├── ui_menu_detail_page.h/.c    ── [基础] 菜单→详情导航框架├── ui_manager.h/.c             ── [核心] UI 管理器（页面注册、导航、生命周期）├── ui_screen_ids.h             ── [核心] 屏幕 ID 枚举定义│├── menu/│   ├── menu_view.h             ── 菜单图片视图接口│   └── menu_view.c             ── 菜单图片显示 + 点击事件│├── home/│   ├── home_view.h             ── 主页视图接口│   ├── home_view.c             ── 主页 UI 布局（时间、日期、天气、电量）│   ├── home_refresh.h          ── 主页数据刷新接口│   └── home_refresh.c          ── 定时刷新逻辑（1s/10s/60s/1800s 周期）│├── ai_qa/│   ├── ai_qa_screen.h          ── AI 问答页面接口│   └── ai_qa_screen.c          ── AI 问答页面实现│├── timing/│   ├── timing_screen.h         ── 计时器页面接口│   └── timing_screen.c         ── 计时器页面（倒计时/秒表/番茄钟）│├── calendar/│   ├── calendar_screen.h       ── 日历页面接口│   └── calendar_screen.c       ── 日历页面（月历 + 年月选择器）│├── plan_list/│   ├── plan_list_screen.h      ── 计划列表页面接口│   └── plan_list_screen.c      ── 计划列表页面（日期条 + 待办）│├── settings/│   ├── settings_screen.h       ── 设置页面接口│   ├── settings_screen.c       ── 设置页面入口│   ├── settings_manager.h/.c   ── 设置项管理器（11 项设置 + 子页面路由）│   ├── settings_common_styles.h/.c ── 设置页面通用样式（标题栏、body）│   ├── settings_detail/        ── 设置列表视图│   ├── setting_brightness/     ── 亮度调节子页面│   └── setting_wifi/           ── WiFi 连接子页面│


三、核心架构3.1 整体架构图┌─────────────────────────────────────────────────────────────┐│                         ui_manager                           ││  ┌─────────────────────────────────────────────────────────┐││  │                    lv_tileview                           │││  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐│││  │  │ HOME │ │PLAN  │ │AI_QA │ │TIMING│ │CALEN │ │SETTIN││││  │  │      │ │LIST  │ │      │ │      │ │DAR   │ │GS    ││││  │  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘│││  └─────────────────────────────────────────────────────────┘││                                                             ││  ┌───────────────────┐    ┌──────────────────────────────┐  ││  │  home_refresh     │    │  ui_menu_detail_page (框架)   │  ││  │  (定时器 1s)       │    │  ┌─────────┐ ┌────────────┐ │  ││  │  → home_view      │    │  │menu_page│→│detail_page │ │  ││  └───────────────────┘    │  │(图片)   │ │(回调创建)  │ │  ││                           │  └─────────┘ └────────────┘ │  ││                           └──────────────────────────────┘  ││                                                             ││  ┌───────────────────┐    ┌──────────────────────────────┐  ││  │  ui_theme.h       │    │  ui_back_button              │  ││  │  (颜色/字体/尺寸)  │    │  (返回按钮组件)               │  ││  └───────────────────┘    └──────────────────────────────┘  │└─────────────────────────────────────────────────────────────┘         │                          │         ▼                          ▼┌─────────────────┐    ┌──────────────────────┐│  service 层      │    │  LVGL 8.x            ││  time_service    │    │  (渲染、事件、输入)    ││  weather_service │    └──────────────────────┘│  system_status   ││  wifi_service    │└─────────────────┘

3.2 导航模型UI 使用 LVGL 的 lv_tileview 作为顶层导航容器，每个功能页面对应一个 tile。用户水平滑动切换 tile。
  ← 滑动 →┌────────┬────────┬────────┬────────┬────────┬────────┐│  HOME  │ PLAN   │ AI_QA  │ TIMING │ CALEND │ SETTING││  (0)   │ (1)    │ (2)    │ (3)    │ (4)    │ (5)    │└────────┴────────┴────────┴────────┴────────┴────────┘

除 Home 页面外，其他页面点击菜单图片后进入 detail page（覆盖在 tile 上），此时水平滑动被禁用，点击返回按钮回到菜单页。
3.3 页面生命周期┌──────────┐│  create  │  ← ui_manager_create_root() 时一次性创建所有 tile└────┬─────┘     │     ▼┌──────────┐│  active  │  ← 用户滑动到该 tile 时└────┬─────┘     │     ├── 点击菜单图片 → 进入 detail page（禁用滑动）     │                    │     │                    ├── detail_create_cb() 回调创建页面     │                    │     │                    └── 点击返回 → show_menu() 销毁 detail，恢复滑动     │     └── 滑动离开 → tile 失焦（home 定时器暂停）


四、各模块详细说明4.1 ui_manager — UI 管理器职责：

创建 root screen 和 tileview
注册所有屏幕页面到 tile
处理 tile 切换事件
管理 Home 页面刷新定时器的启动&#x2F;暂停
提供页面跳转 API（ui_manager_show_xxx_screen()）
提供 tile 滑动方向控制（ui_manager_set_tile_swipe_enabled()）

关键实现细节：
// 屏幕配置表（ui_manager.c 内部）static const ui_screen_config_t g_ui_manager_screens[UI_SCREEN_ID_COUNT] = &#123;    [UI_SCREEN_ID_HOME]      = &#123; .name = &quot;home&quot;,      .image_path = NULL,              .create = ... &#125;,    [UI_SCREEN_ID_PLAN_LIST] = &#123; .name = &quot;plan_list&quot;,  .image_path = &quot;F:/jihuaqingdan.png&quot;, .create = ... &#125;,    [UI_SCREEN_ID_AI_QA]     = &#123; .name = &quot;ai_qa&quot;,     .image_path = &quot;F:/aiduihua.png&quot;,     .create = ... &#125;,    // ... 共 6 个页面&#125;;

tile 切换时的自动行为：

恢复水平滑动方向（安全保护）
切换到 Home → 恢复 home_refresh_timer
切换到其他页面 → 暂停 home_refresh_timer

4.2 ui_theme — 主题系统纯头文件，只包含 #define 常量，无运行时开销。



分类
常量数量
示例



颜色
16 个
UI_COLOR_PRIMARY (0xffd54a)


字体
8 个
UI_FONT_TITLE (&amp;lv_font_montserrat_20)


尺寸
5 个
UI_HEADER_HEIGHT (35)


4.3 ui_menu_detail_page — 菜单→详情导航框架解决的问题： 5 个页面（AI QA、Timing、Calendar、Plan List、Settings）都有完全相同的菜单→详情切换逻辑，原来每个页面重复实现 50-80 行样板代码。
核心数据结构：
typedef struct &#123;    lv_obj_t *screen;                    // tile 容器（不可销毁）    lv_obj_t *menu_page;                 // 菜单页面（带图片，可隐藏/显示）    lv_obj_t *detail_page;               // 详情页面（可创建/销毁）    ui_detail_create_cb_t detail_create_cb;   // 详情创建回调    ui_detail_destroy_cb_t detail_destroy_cb; // 详情销毁回调    void *user_data;                     // 传递给回调的用户数据    ui_menu_view_action_t menu_action;   // 存储在结构体内（避免悬空指针）    bool detail_created;&#125; ui_menu_detail_page_t;

⚠️ 关键约束（二次开发时必须遵守）：

detail_create_cb 回调内必须：

在 parent（即 ctx-&gt;screen &#x3D; tile）上创建 page = lv_obj_create(parent)
创建完成后设置 ctx-&gt;detail_page = page
否则 show_detail() 会因为 detail_page == NULL 而返回，导致页面不显示


menu_action 必须存储在结构体内（不能是局部变量），因为 ui_menu_view_create 只保存了指针引用。


4.4 ui_back_button — 返回按钮组件lv_obj_t *ui_back_button_create(lv_obj_t *parent, lv_event_cb_t click_cb);

创建一个 30×30 圆形透明按钮，带 LV_SYMBOL_LEFT 图标。被以下模块使用：

ui_menu_detail_page_create_detail_header() — 通用标题栏
settings_common_styles.c — 设置页面标题栏

4.5 home — 主页模块组成： home_view.c（UI 布局） + home_refresh.c（数据刷新）
home_view 暴露的接口：
void ui_home_view_create(lv_obj_t *parent);void ui_home_view_set_time(int hour, int minute);void ui_home_view_set_date(int weekday, int year, int month, int day);void ui_home_view_set_weather(int temperature, const char *weather_text);void ui_home_view_set_wifi(int connected, int level);void ui_home_view_set_battery(int percent, int charging);void ui_home_view_refresh(const ui_home_view_model_t *view_model);

home_refresh 的刷新策略：



周期
操作



每 1 秒
刷新时间显示


每 10 秒
刷新 WiFi + 电量状态


每 60 秒
刷新日期 + 天气显示


每 1800 秒
调用 weather_service_refresh() 重新请求天气 API


优化点： home_refresh_timer 在用户滑离 Home 页面时自动暂停，返回时恢复。
4.6 menu — 菜单视图void ui_menu_view_create(lv_obj_t *parent, const char *image_path, const ui_menu_view_action_t *view_action);

行为：

如果 image_path 指向的文件存在 → 显示图片（lv_img）
如果文件不存在 → 显示 LV_SYMBOL_IMAGE 占位符图标
如果 view_action 有回调 → 图片可点击，点击触发回调

注意： 图片路径格式为 &quot;F:/xxx.png&quot;，其中 F: 对应设备 Flash 存储的文件系统挂载点。
4.7 settings — 设置模块（最复杂的子系统）Settings 使用三级导航结构：
┌─────────────┐     点击图片      ┌─────────────┐     点击设置项     ┌─────────────┐│  menu_page  │ ────────────────→ │ detail_page │ ────────────────→ │  sub_page   ││  (菜单图片)  │                   │ (11项设置列表)│                   │ (具体设置界面)│└─────────────┘ ←──────────────── └─────────────┘ ←──────────────── └─────────────┘                    返回按钮                           返回按钮

11 个设置项：



ID
名称
子页面



0
Wi-Fi
✅ 已实现（setting_wifi/）


1
Bluetooth
❌ NULL（未实现）


2
Brightness
✅ 已实现（setting_brightness/）


3
Sleep Timer
❌ NULL


4
Baidu Netdisk
❌ NULL


5
Volume
❌ NULL


6
Do Not Disturb
❌ NULL


7
Night Screen Off
❌ NULL


8
WeChat Binding
❌ NULL


9
System Update
❌ NULL


10
Factory Reset
❌ NULL


⚠️ 注意： settings_manager 内部管理了 sub_page 的创建&#x2F;销毁，与 ui_menu_detail_page 框架独立。back_event_cb 由 settings_screen.c 提供，用于从 detail 返回到 menu。
4.8 WiFi 设置子页面（setting_wifi）这是设置模块中最复杂的子页面，包含：

WiFi 扫描（定时器驱动，300ms 周期）
AP 列表展示（信号强度、SSID、加密状态、连接状态）
密码输入页面（textarea + keyboard）
自动排序（已连接 AP 优先，然后按信号强度）

⚠️ 注意： WiFi 子页面使用 malloc 分配 SSID 副本（ssid_copy = malloc(MAX_SSID_LEN)），需要在事件回调中 free。

五、数据流与刷新机制5.1 数据流向┌──────────────────────┐│     service 层        ││  ┌────────────────┐  ││  │ time_service    │──┼──→ home_view_set_time()│  │ weather_service │──┼──→ home_view_set_weather()│  │ system_status   │──┼──→ home_view_set_wifi()│  │                 │──┼──→ home_view_set_battery()│  └────────────────┘  │└──────────────────────┘

Home 页面是唯一有定时刷新的页面。其他页面的数据在创建时获取一次，后续更新依赖用户交互（如 WiFi 扫描定时器）。
5.2 事件流用户触摸事件    │    ▼LVGL 输入设备驱动（触摸屏）    │    ▼lv_tileview 事件处理    │    ├── 滑动 → LV_EVENT_VALUE_CHANGED → ui_manager_handle_tile_change()    │    └── 点击 → LV_EVENT_CLICKED                │                ├── menu_view: ui_menu_view_handle_click()                │       → ui_menu_detail_page_show_detail()                │           → detail_create_cb() [首次]                │           → 禁用滑动 + 隐藏 menu + 显示 detail                │                └── back_button: xxx_on_back_click()                        → ui_menu_detail_page_show_menu()                            → lv_obj_del(detail_page)                            → 启用滑动 + 显示 menu


六、公共 API 参考6.1 ui_manager.hvoid ui_manager_init(void);                           // 初始化 UI（首次调用）void ui_manager_go_back(void);                        // 返回 Homeconst char *ui_manager_get_menu_image_path(ui_screen_id_t screen_id);  // 获取菜单图片路径// 页面跳转void ui_manager_show_home_screen(void);void ui_manager_show_plan_list_screen(void);void ui_manager_show_ai_qa_screen(void);void ui_manager_show_timing_screen(void);void ui_manager_show_calendar_screen(void);void ui_manager_show_settings_screen(void);// 滑动控制void ui_manager_set_tile_swipe_enabled(bool enabled);

6.2 ui_menu_detail_page.hvoid ui_menu_detail_page_init(    ui_menu_detail_page_t *ctx,    lv_obj_t *screen,    uint32_t bg_color,    const char *menu_image_path,    ui_detail_create_cb_t detail_create_cb,    ui_detail_destroy_cb_t detail_destroy_cb,    void *user_data);void ui_menu_detail_page_show_menu(ui_menu_detail_page_t *ctx);void ui_menu_detail_page_show_detail(ui_menu_detail_page_t *ctx);lv_obj_t *ui_menu_detail_page_create_detail_header(    lv_obj_t *parent,    const char *title,    lv_event_cb_t back_click_cb);

6.3 ui_back_button.hlv_obj_t *ui_back_button_create(lv_obj_t *parent, lv_event_cb_t click_cb);

6.4 home_view.htypedef struct &#123;    int year, hour, minute, weekday, month, day;    int temperature;    const char *weather_text;    int wifi_connected, wifi_level;    int battery_percent, battery_charging;&#125; ui_home_view_model_t;void ui_home_view_create(lv_obj_t *parent);void ui_home_view_set_time(int hour, int minute);void ui_home_view_set_date(int weekday, int year, int month, int day);void ui_home_view_set_weather(int temperature, const char *weather_text);void ui_home_view_set_wifi(int connected, int level);void ui_home_view_set_battery(int percent, int charging);void ui_home_view_refresh(const ui_home_view_model_t *view_model);

6.5 ui_screen_ids.htypedef enum &#123;    UI_SCREEN_ID_NONE = -1,    UI_SCREEN_ID_HOME = 0,    UI_SCREEN_ID_PLAN_LIST,    UI_SCREEN_ID_AI_QA,    UI_SCREEN_ID_TIMING,    UI_SCREEN_ID_CALENDAR,    UI_SCREEN_ID_SETTINGS,    UI_SCREEN_ID_COUNT       // = 6&#125; ui_screen_id_t;


七、添加新页面的步骤步骤 1：创建文件project/ui/my_feature/├── my_feature_screen.h└── my_feature_screen.c

步骤 2：实现代码（模板）/* my_feature_screen.h */#ifndef UI_MY_FEATURE_SCREEN_H#define UI_MY_FEATURE_SCREEN_H#include &quot;lvgl/lvgl.h&quot;void ui_my_feature_screen_create(lv_obj_t *screen);#endif/* my_feature_screen.c */#include &quot;my_feature_screen.h&quot;#include &quot;../ui_manager.h&quot;#include &quot;../ui_menu_detail_page.h&quot;#include &quot;../ui_theme.h&quot;typedef struct &#123;    ui_menu_detail_page_t base;    /* 你的页面状态 */&#125; ui_my_feature_state_t;static ui_my_feature_state_t g_ui;static void on_back(lv_event_t *e) &#123;    (void)e;    ui_menu_detail_page_show_menu(&amp;g_ui.base);&#125;static void create_detail(lv_obj_t *parent, void *ud) &#123;    (void)ud;    lv_obj_t *page = lv_obj_create(parent);    lv_obj_remove_style_all(page);    lv_obj_set_size(page, LV_PCT(100), LV_PCT(100));    lv_obj_center(page);    lv_obj_set_style_bg_color(page, lv_color_hex(UI_COLOR_BG_DETAIL), 0);    lv_obj_set_style_bg_opa(page, LV_OPA_COVER, 0);    lv_obj_clear_flag(page, LV_OBJ_FLAG_SCROLLABLE);    g_ui.base.detail_page = page;  /* ⚠️ 必须设置 */    ui_menu_detail_page_create_detail_header(page, &quot;My Feature&quot;, on_back);    /* 在 page 上创建你的内容 */&#125;void ui_my_feature_screen_create(lv_obj_t *screen) &#123;    ui_menu_detail_page_init(&amp;g_ui.base, screen, UI_COLOR_BG_DETAIL,        ui_manager_get_menu_image_path(UI_SCREEN_ID_MY_FEATURE),        create_detail, NULL, NULL);&#125;

步骤 3：注册到 ui_manager
ui_screen_ids.h — 在 UI_SCREEN_ID_SETTINGS 后添加 UI_SCREEN_ID_MY_FEATURE

ui_manager.c — 添加：
#include &quot;my_feature/my_feature_screen.h&quot;static void ui_manager_create_my_feature_screen(lv_obj_t *screen) &#123;    ui_my_feature_screen_create(screen);&#125;// 在 g_ui_manager_screens 数组中添加：[UI_SCREEN_ID_MY_FEATURE] = &#123;    .name = &quot;my_feature&quot;,    .image_path = &quot;F:/myicon.png&quot;,    .create = ui_manager_create_my_feature_screen&#125;,

ui_manager.h — 添加 void ui_manager_show_my_feature_screen(void);

txw82xApp.cdkproj — 在 &lt;VirtualFolder Name=&quot;ui&quot;&gt; 下添加新 .c 文件引用


步骤 4：准备菜单图片将 240×240 或适当尺寸的 PNG 图片放入设备 Flash，路径与 image_path 一致（如 &quot;F:/myicon.png&quot;）。

八、依赖关系8.1 头文件依赖图ui_theme.h ◄─────────────────────────────────────────────────────┐    ▲                                                             │    │ (所有文件都 include)                                         │    │                                                             │ui_back_button.h ◄── ui_menu_detail_page.h ◄── 各 screen.c       │    ▲                   ▲                                        │    │                   │                                        │    │         menu/menu_view.h ◄──────────────────────────────────┘    │    └── settings_common_styles.h ◄── settings 子模块

8.2 service 层依赖


UI 模块
依赖的 service
调用的函数



home_refresh
time_service
time_service_get_datetime()


home_refresh
weather_service
weather_service_get_data(), weather_service_refresh()


home_refresh
system_status_service
system_status_service_get_data()


calendar_screen
time_service
time_service_get_datetime()


plan_list_screen
time_service
time_service_get_datetime()


setting_wifi
wifi_service
wifi_service_init(), wifi_service_scan(), wifi_service_connect() 等


8.3 LVGL 组件使用统计


LVGL 组件
使用页面



lv_obj
所有页面（通用容器）


lv_label
所有页面（文本显示）


lv_btn
timing, plan_list, calendar, settings


lv_img
menu（菜单图片）


lv_slider
brightness（亮度调节）


lv_textarea
wifi（密码输入）


lv_keyboard
wifi（密码输入）


lv_calendar
calendar（月历）


lv_roller
calendar（年月选择器）


lv_tileview
ui_manager（顶层导航）



九、已知限制与注意事项9.1 内存限制
所有 6 个 tile 在 ui_manager_create_root() 时一次性全部创建，不支持懒加载
每个 tile 创建后即使不可见也占用 LVGL 内存
ui_home_create_flower() 创建 11 个 LVGL 对象绘制一朵花，可优化为 Canvas 或图片

9.2 图片资源
菜单图片存储在设备 Flash（路径 F:/xxx.png）
图片不存在时显示占位图标（LV_SYMBOL_IMAGE）
需要确保 menu_view.c 中 lv_fs_open 的文件系统驱动已正确注册

9.3 WiFi 子页面
使用 malloc/free 管理 SSID 字符串副本
WiFi 扫描定时器有 9 秒超时限制（300ms × 30 次）
密码输入要求最少 8 字符

9.4 Settings 三级导航
Settings 的 detail_page 由 settings_manager 内部管理，与 ui_menu_detail_page 框架的 detail_page 概念不同
settings_manager 内部还有 sub_page 层级
添加新设置项需要修改 settings_manager.c 中的 g_ui_settings_page_configs 数组

9.5 字体
使用 LVGL 内置的 Montserrat 字体（10&#x2F;12&#x2F;14&#x2F;16&#x2F;18&#x2F;20&#x2F;22&#x2F;48）
仅支持英文 + 数字 + 特殊符号
不支持中文显示（如需中文，需添加自定义字库）

9.6 硬编码的文件路径所有图片路径硬编码为 &quot;F:/xxx.png&quot;，如需修改文件系统挂载点，需全局替换 ui_manager.c 中的 image_path 字段。

十、编译与调试10.1 编译使用 CDK IDE 打开 project/txw82xApp.cdkproj，确保所有新增的 .c 文件已添加到工程的 ui 虚拟文件夹中。
10.2 添加新文件到工程在 txw82xApp.cdkproj 的 &lt;VirtualFolder Name=&quot;ui&quot;&gt; 下添加：
&lt;File Name=&quot;ui/my_feature/my_feature_screen.c&quot;&gt;  &lt;FileOption/&gt;&lt;/File&gt;

10.3 调试技巧
查看 LVGL 对象数量： 在调试器中查看 lv_obj_count 确认对象是否正确销毁
检查 tile 切换： 在 ui_manager_handle_tile_change 中设断点，确认 screen_id 正确
WiFi 调试： menu_view.c 中的文件存在检查可临时恢复 printf 语句
内存不足： 如果页面创建失败，检查 lv_mem_monitor 确认可用内存

10.4 常见问题排查


现象
可能原因
排查方法



页面不显示
detail_page 未设置
检查 callback 中是否有 g_xxx.base.detail_page = page


无法滑动
swipe 被禁用未恢复
检查 show_menu 是否正确调用


进入详情死机
对象创建过多
检查 LVGL 对象数量限制


菜单图片不显示
Flash 文件不存在
检查 F:/ 下的图片文件


中文乱码
字体不支持中文
需要添加中文字库



附录：文件变更记录


日期
变更
说明



2026-05-09
新增 ui_theme.h
统一主题系统


2026-05-09
新增 ui_back_button.h/.c
可复用返回按钮


2026-05-09
新增 ui_menu_detail_page.h/.c
菜单→详情导航框架


2026-05-09
重构 5 个 screen 文件
使用新框架消除样板代码


2026-05-09
修复 home_view.c
花朵位置 bug


2026-05-09
优化 ui_manager.c
Home 定时器按需暂停


2026-05-09
清理 menu_view.c
移除 debug printf


]]></content>
      <categories>
        <category>项目文档</category>
        <category>TXW828 AI 闹钟</category>
      </categories>
      <tags>
        <tag>LVGL</tag>
        <tag>UI架构</tag>
        <tag>代码移交</tag>
        <tag>嵌入式</tag>
        <tag>项目文档</tag>
      </tags>
  </entry>
  <entry>
    <title>TXW82x Spook RTSP 流媒体服务器架构文档</title>
    <url>/2026/06/30/TXW82x-Spook-RTSP%E6%B5%81%E5%AA%92%E4%BD%93%E6%9C%8D%E5%8A%A1%E5%99%A8%E6%9E%B6%E6%9E%84%E6%96%87%E6%A1%A3/</url>
    <content><![CDATA[TXW82x Spook RTSP 流媒体服务器架构文档1. 概述Spook 是一个运行在 TXW82x 嵌入式芯片上的轻量级 RTSP 流媒体服务器，通过 Wi-Fi 将摄像头采集的视频以 RTSP 协议推送给手机&#x2F;电脑等客户端。最初由 Nathan Lutchansky 于 2004 年开发的开源项目，后被移植到 TXW82x 平台（CK810 CPU + lwIP 协议栈）。
1.1 核心能力


能力
说明



视频编码
MJPEG (硬件 JPEG 编码器)、H.264 (硬件 H.264 编码器)


音频编码
AAC (硬件音频编码器)


传输协议
RTP&#x2F;AVP over UDP、RTP&#x2F;AVP over TCP (RTSP 交织)


默认端口
554


多路流
同时注册多个 RTSP 路径，独立推流


录像回放
支持通过 RTSP 回放 SD 卡中的 MP4&#x2F;AVI 文件


并发客户端
多客户端同时连接同一路流


1.2 硬件平台


组件
说明



CPU
CK810 (C-SKY 架构)


视频编码
硬件 JPEG 编码器 + 硬件 H.264 编码器


音频编码
硬件 AAC 编码器


网络
Wi-Fi (AP 模式，默认 IP 192.168.169.1)


内存
SRAM + PSRAM 混合堆



2. 软件架构2.1 模块层次图┌──────────────────────────────────────────────────────────────────────┐│                         应用层 (app_fpv.c)                            ││     spook_init() → 初始化基础设施                                      ││     rtsp_mjpeg_live_init() → 注册 /webcam 路径                        ││     rtsp_h264_live_init()  → 注册 /h264 路径                          │└───────────────────────────┬──────────────────────────────────────────┘                            │┌───────────────────────────▼──────────────────────────────────────────┐│  Spook RTSP Server 内核                                                ││                                                                       ││  ┌──────────────┐   ┌──────────────────┐   ┌──────────────────────┐  ││  │  spook.c     │   │  rtsp_common.c   │   │  tcp.c               │  ││  │  模块入口     │   │  RTSP协议层      │   │  TCP连接管理         │  ││  │  随机数工具   │   │  路径注册/会话管理 │   │  事件驱动I/O         │  ││  └──────────────┘   └────────┬─────────┘   └──────────────────────┘  ││                              │                                        ││  ┌───────────────────────────┼────────────────────────────────────┐   ││  │   RTSP 路径注册            按需调用                              │   ││  │                                                               │   ││  │  ┌─────────────────────┐  ┌─────────────────────┐            │   ││  │  │ rtsp_mjpeg_live.c   │  │ rtsp_h264_live.c    │            │   ││  │  │ /webcam (MJPEG)     │  │ /h264 (H.264)       │            │   ││  │  └─────────┬───────────┘  └─────────┬───────────┘            │   ││  │            │                        │                        │   ││  │  ┌─────────▼───────────┐  ┌─────────▼───────────┐           │   ││  │  │ custom_rtsp_jpg.c   │  │ webfile.c            │           │   ││  │  │ /custom (动态源)    │  │ /file (文件回放)     │           │   ││  │  └─────────────────────┘  └─────────────────────┘           │   ││  └─────────────────────────────────────────────────────────────┘   ││                                                                     ││  ┌──────────────┐   ┌──────────────┐   ┌──────────────────────┐   ││  │  rtp.c       │   │  rtp-jpeg.c  │   │  rtp-h264.c          │   ││  │  RTP协议层    │   │  JPEG分包     │   │  H.264 FU-A分片     │   ││  └──────────────┘   └──────────────┘   └──────────────────────┘   ││                                                                     ││  ┌──────────────┐   ┌──────────────┐   ┌──────────────────────┐   ││  │  session.c   │   │  stream.c    │   │  frame.c             │   ││  │  会话管理     │   │  流管道      │   │  帧管理/MSI接口      │   ││  └──────────────┘   └──────────────┘   └──────────────────────┘   ││                                                                     ││  ┌──────────────┐   ┌──────────────┐   ┌──────────────────────┐   ││  │  encoder-    │   │  encoder-    │   │  encoder-            │   ││  │  jpeg.c/h    │   │  h264.c      │   │  audio.c/h           │   ││  │  JPEG适配    │   │  H264适配    │   │  音频适配             │   ││  └──────────────┘   └──────────────┘   └──────────────────────┘   │└─────────────────────────────────────────────────────────────────────┘                            │┌───────────────────────────▼─────────────────────────────────────────┐│  SDK 底层依赖                                                        ││  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────────┐ ┌────────┐  ││  │ lwIP     │ │ csi_     │ │  MSI     │ │ video_app  │ │audio   │  ││  │ TCP/IP   │ │ kernel   │ │ 多媒体   │ │ 视频应用   │ │媒体    │  ││  └──────────┘ └──────────┘ └──────────┘ └────────────┘ └────────┘  ││                                                                     ││  ┌──────────────────────────────────────────────────────────────┐  ││  │  硬件层: 摄像头(CSI/DVP) → ISP → JPEG/H264编码器 → Wi-Fi     │  ││  └──────────────────────────────────────────────────────────────┘  │└─────────────────────────────────────────────────────────────────────┘

2.2 文件清单


文件
行数
职责



spook.c
~145
模块入口：spook_init()，随机数工具，rtp_name 定义


spook.h
~10
对外接口声明


spook_config.h
~70
全局配置宏、编码器名称、rtp_name 结构体


rtsp.c
-
RTSP 协议解析器


rtsp_common.c
~500+
路径注册、轨道设置、SDP 生成、会话管理


rtsp_common.h
~70
rtsp_source、rtsp_session、rtsp_track 定义


rtsp_mjpeg_live.c
~180
/webcam — MJPEG 实时预览路径实现


rtsp_h264_live.c
~260
/h264 — H.264 实时流 + /loop/RECA/ 回放


custom_rtsp_jpg.c
~160
/custom — 自定义视频源路径


webfile.c
~230
/file — SD 卡文件回放(已注释)


rtp.h
-
RTP 核心结构体定义


rtp.c
~700
RTP 包收发、UDP&#x2F;TCP 传输、RTCP


rtp-jpeg.c
~560
JPEG RTP 封包(RFC 2435)


rtp-h264.c
~320
H.264 RTP 封包(RFC 3984, FU-A)


rtp-audio.c
-
AAC RTP 封包


stream.c
~120
Stream 发布-订阅管道


stream.h
~40
Stream 结构体定义


frame.h
~160
Frame 结构体、FrameExchanger、回调函数类型


frame.c
~410
帧管理、MSI 接口、主发送线程


session.c
~60
RTP 媒体会话管理


session.h
~10
会话接口声明


rtp_media.h
~50
RTP 媒体接口定义


encoder-jpeg.c
~150
JPEG 编码器适配


encoder-jpeg.h
~25
JPEG 编码器结构体


encoder-h264.c
~140
H.264 编码器适配


encoder-audio.c
~145
AAC 音频编码器适配


encoder-audio.h
~15
音频编码器结构体


event.h
-
事件驱动框架(基于 eloop)


tcp.c
-
TCP 连接管理


pmsg.h&#x2F;c
-
HTTP&#x2F;RTSP 协议消息解析


log.h
-
日志宏(已注释禁用)


access_log.c
-
访问日志


ephoto.c
-
测试 JPEG 图片数据



3. 核心数据结构3.1 rtp_name — 路径描述符定义于 spook_config.h，描述一条 RTSP 流所需的编码器和路径信息。
typedef struct &#123;    const char *video_encode_name;   // 视频编码器 stream 名称 (如 &quot;jpg_encoder&quot;)    const char *audio_encode_name;   // 音频编码器 stream 名称 (如 &quot;audio_aac_encoder&quot;)    const char *path;                // RTSP URL 路径 (如 &quot;/webcam&quot;)&#125; rtp_name;

使用示例：
const rtp_name live_dvp = &#123;    .video_encode_name = JPG_ENCODER_NAME,       // &quot;jpg_encoder&quot;    .audio_encode_name = AUDIO_AAC_ENCODER_NAME,  // &quot;audio_aac_encoder&quot;    .path              = &quot;/webcam&quot;,&#125;;

3.2 rtsp_source — RTSP 源定义于 rtsp_common.h，代表一个 RTSP 路径的完整状态。
struct rtsp_source &#123;    struct rtsp_session *sess_list;         // 当前连接的 session 链表    struct rtsp_track track[MAX_TRACKS];    // 轨道数组 (MAX_TRACKS=4)    struct os_task handle;                  // 发送线程句柄    void *signal;    struct rtp_node live_node;              // video_ex / audio_ex 指针    int head_len;    rtsp_creat creat;                       // 创建回调 → self_creat()    rtsp_release release;                   // 释放回调 → self_destory()    rtsp_play_fn play;                      // 播放回调    void *priv;                             // 私有数据 → rtsp_priv&#125;;

3.3 rtsp_track — 轨道struct rtsp_track &#123;    int index;                          // 轨道索引 (0=视频, 1=音频)    struct rtsp_source *source;         // 所属 source    struct stream_destination *stream;  // 连接到编码器 stream 的消费者    int ready;                          // PLAY 后置 1    struct rtp_media *rtp;              // RTP 媒体处理接口&#125;;

3.4 rtp_media — RTP 媒体接口定义于 rtp_media.h，纯函数指针结构体，定义了对一种媒体类型的全部操作。
struct rtp_media &#123;    rtp_media_get_sdp_func get_sdp;          // 生成 SDP 描述行    rtp_media_get_payload_func get_payload;  // 获取 RTP payload 类型号    rtp_media_frame_func frame;              // 处理视频帧 (解析头)    rtp_media_send_func send;                // 已废弃    rtp_media_send_more_func send_more;      // 发送给所有连接客户端    rtp_media_send_func rtcp_send;           // RTCP 发送    void *private;                           // 私有数据 (如 rtp_jpeg *)    int per_ms_incr;                         // 每毫秒时间戳增量    uint32_t sample_rate;                    // 采样率    uint8_t type;                            // 0=视频, 1=音频&#125;;

3.5 frame_exchanger — 帧交换器定义于 frame.h，单 slot 的帧转发器，是编码器输出和 stream 管道之间的桥梁。
struct frame_exchanger &#123;    int ready;                        // 是否就绪    int scan_ready;                   // 编码器是否运行    frame_deliver_func f;             // 帧递送回调 (get_back_frame)    struct frame *jf;                 // 单 slot 帧指针    void *d;                          // 回调参数 (jpeg_encoder *)&#125;;

3.6 rtp_node — 音视频出口节点struct rtp_node &#123;    void *video_ex;     // → frame_exchanger * (jpeg_encoder-&gt;ex)    void *audio_ex;     // → frame_exchanger * (audio_encoder-&gt;ex)    void *priv;&#125;;

3.7 rtsp_priv — 路径私有数据struct rtsp_priv &#123;    struct rtp_node *live_node;    stream *webfile_s;    struct msi *video_msi;       // 视频编码器 MSI (如 AUTO_JPG)    struct msi *audio_msi;       // 音频编码器 MSI    struct msi *v_msi;           // RTSP 接收 MSI (如 R_RTP_JPEG)    struct msi *a_msi;           // RTSP 音频接收 MSI&#125;;

3.8 rtp_endpoint — RTP 端点struct rtp_endpoint &#123;    struct session *session;     // 所属 RTSP session    int payload;                 // RTP payload 类型 (26=MJPEG, 96=H264)    int max_data_size;           // 最大数据包大小 (1440)    unsigned int ssrc;           // 同步源标识 (随机生成)    unsigned int start_timestamp;    unsigned int last_timestamp;    int seqnum;                  // 序列号 (从随机值开始递增)    int packet_count;    int octet_count;    int sendEnable;              // 是否允许发送    int trans_type;              // RTP_TRANS_UDP 或 RTP_TRANS_INTER(TCP)    union &#123;        struct &#123; /* UDP: fd, port, event */ &#125; udp;        struct &#123; /* TCP: conn, channel */ &#125; inter;    &#125; trans;&#125;;


4. 完整 MJPEG 流调用链4.1 初始化阶段阶段流程图main() → fpv_app_init() → user_protocol()  │  └─ spook_init()                              [spook.c]       │       └─ spook_thread()            │            ├─ init_random()            │    └─ random_key = 0x12345678            │            ├─ global_init()            │    └─ config_port(554)           [spook.c]            │            ├─ jpeg_encode_init(&quot;jpg_encoder&quot;) [encoder-jpeg.c]            │    ├─ start_block()            │    │    └─ malloc(jpeg_encoder)   ← 分配编码器结构体            │    ├─ set_output(&quot;jpg_encoder&quot;, en)            │    │    └─ new_stream(&quot;jpg_encoder&quot;, FORMAT_JPEG, en)            │    │       └─ 创建 stream 节点，挂入全局链表            │    └─ end_block(en)            │         └─ new_exchanger(16, get_back_frame, en)            │            └─ 创建 frame_exchanger，回调=get_back_frame            │            ├─ h264_encode_init(&quot;h264_encoder&quot;) [encoder-h264.c]            │    └─ 同上，创建 &quot;h264_encoder&quot; stream            │            ├─ rtsp_audio_encode_init(&quot;audio_aac_encoder&quot;)  [encoder-audio.c]            │    └─ 创建 &quot;audio_aac_encoder&quot; stream            │            ├─ rtsp_audio_encode_init(&quot;audio_aac_encoder2&quot;)            │    └─ 创建 &quot;audio_aac_encoder2&quot; stream            │            ├─ rtsp_mjpeg_live_init(&amp;live_dvp) [rtsp_mjpeg_live.c]            │    ├─ rtsp_start_block()            │    │    └─ malloc(rtsp_source)            │    ├─ rtsp_set_path(&quot;/webcam&quot;, source, self_rtsp_open)            │    ├─ set_video_track(&quot;jpg_encoder&quot;, source)            │    │    ├─ connect_to_stream(&quot;jpg_encoder&quot;, rtsp_common_send, &amp;track)            │    │    │   → 查找已存在的 stream(&quot;jpg_encoder&quot;)            │    │    │   → 创建 stream_destination，注册回调 rtsp_common_send            │    │    ├─ source-&gt;live_node.video_ex = get_video_ex(en)            │    │    │   → en-&gt;ex (jpeg_encoder 的 frame_exchanger)            │    │    └─ track-&gt;rtp = new_rtp_media_jpeg_stream(stream)            │    │        → 创建 rtp_media &#123;            │    │            .frame     = jpeg_process_frame,            │    │            .send_more = jpeg_send_more,            │    │            .get_sdp   = jpeg_get_sdp            │    │          &#125;            │    │            │    ├─ set_audio_track(&quot;audio_aac_encoder&quot;, source)            │    │    └─ 同上，音频轨道            │    ├─ register_live_fn(source, self_creat, self_destory, NULL)            │    └─ rtsp_end_block(source)            │            ├─ rtsp_h264_live_init(&amp;live_h264)        ← 注册 /h264            ├─ custom_rtsp_jpeg_init(&amp;live_custom)    ← 注册 /custom            └─ rtsp_h264_live_init(&amp;live_h264_2)      ← 注册 /loop/RECA/

初始化后内存结构stream 全局链表  ├─ &quot;jpg_encoder&quot; (FORMAT_JPEG)  │    ├─ private → jpeg_encoder  │    │              ├─ output → stream(&quot;jpg_encoder&quot;) 自指  │    │              └─ ex → frame_exchanger  │    │                      ├─ f = get_back_frame  │    │                      ├─ d = jpeg_encoder  │    │                      └─ jf = frame *  │    └─ dest_list  │         └─ stream_destination [video track of /webcam]  │              ├─ process_frame = rtsp_common_send  │              └─ d = &amp;rtsp_source.track[0]  │  ├─ &quot;h264_encoder&quot; (FORMAT_H264)  ├─ &quot;audio_aac_encoder&quot; (FORMAT_AUDIO)  └─ &quot;audio_aac_encoder2&quot; (FORMAT_AUDIO)RTSP 路径注册表  ├─ &quot;/webcam&quot; → rtsp_source  │    ├─ track[0]: video → rtp_media(jpeg_process_frame)  │    ├─ track[1]: audio → rtp_media(audio_send)  │    └─ live_node.video_ex → frame_exchanger(get_back_frame)  │  ├─ &quot;/h264&quot; → rtsp_source  ├─ &quot;/custom&quot; → rtsp_source  └─ &quot;/loop/RECA/&quot; → rtsp_source

4.2 客户端连接阶段RTSP 协议交互客户端 (VLC/ffplay)             服务器 (端口 554)      │                                │      │  ── TCP connect ──►            │  tcp.c: accept → conn      │                                │      │  ── OPTIONS ──►                │  rtsp.c: 回复支持方法      │  ◄── 200 OK ──                │      │                                │      │  ── DESCRIBE /webcam ──►       │      │                                │  → 查找路径 &quot;/webcam&quot;      │                                │  → self_rtsp_open()      │                                │  → rtsp_open() 创建 session      │  ◄── SDP ──                    │      │    m=video 0 RTP/AVP 26       │  track0: MJPEG      │    m=audio 0 RTP/AVP 97       │  track1: AAC      │                                │      │  ── SETUP track0 ──►           │  ★ 首次 SETUP 触发重量级初始化      │                                │  rtsp_setup(s, t=0)      │                                │    └─ source-&gt;creat(source, path)      │                                │       → self_creat() [rtsp_mjpeg_live.c]      │                                │         ├─ os_zalloc(rtsp_priv)      │                                │         ├─ r-&gt;video_msi = msi_find(AUTO_JPG,1)      │                                │         ├─ r-&gt;v_msi = rtsp_msi_init(...)      │                                │         ├─ msi_add_output(video_msi, NULL, R_RTP_JPEG)      │                                │         └─ OS_TASK_INIT(&quot;live_rtsp_mjpeg&quot;, ...)      │                                │  → new_rtp_endpoint(26)      │  ◄── 200 OK (channel 0-1) ──   │      │                                │      │  ── SETUP track1 ──►           │  (线程已存在，不再创建)      │  ◄── 200 OK (channel 2-3) ──   │      │                                │      │  ── PLAY ──►                   │      │                                │  rtsp_play(s, start)      │                                │    ├─ ls-&gt;playing = 1      │                                │    ├─ track-&gt;ready = 1      │                                │    └─ set_waiting(stream, 1)      │                                │       → jpeg_encoder-&gt;running = 1      │                                │       → ex-&gt;scan_ready = 1      │  ◄── 200 OK ──                │      │                                │      │  ◄══ RTP 数据流 ══►           │  发送线程开始推流      │    $ (interleaved data)        │  TCP 交织通道 0-1 持续发送

4.3 数据发送阶段核心发送线程 spook_send_thread_stream()定义于 frame.c:316，是整个流媒体服务的心脏。
void spook_send_thread_stream(struct rtsp_priv *r)&#123;    while (1)    &#123;        // ── [可选] 音频处理 ──        if (audio_rtsp) &#123;            audio_fb = msi_get_fb(audio_rtsp-&gt;msi, 0);   // 取音频帧            if (audio_fb) &#123;                audio_ex-&gt;f(audio, audio_ex-&gt;d);          // → 发送音频            &#125;        &#125;        // ── [核心] 视频处理 ──        fb = msi_get_fb(jpg_rtsp-&gt;msi, 0);                // ① 取视频帧(MSI队列)        if (fb) &#123;            jpeg = ex-&gt;jf;                                 // ② 取 frame slot            jpeg-&gt;get_f = fb;                              // ③ 绑定 framebuff            jpeg-&gt;d      = fb-&gt;data;            jpeg-&gt;length = fb-&gt;len;            jpeg-&gt;timestamp = fb-&gt;time;            ex-&gt;f(jpeg, ex-&gt;d);                            // ④ 送入 stream 管道        &#125;        // ── 无帧时休眠 ──        if (!fb &amp;&amp; !audio_fb) &#123;            os_sleep_ms(1);                                // 等 1ms            if (超时&gt;1000ms) ex-&gt;f(NULL, ex-&gt;d);            // 空帧保活            continue;        &#125;    &#125;&#125;

帧处理完整调用链① msi_get_fb(v_msi, 0)     ← 从 MSI 队列取帧 (缓冲区 28 帧)      │      ▼② 填充 frame 结构体      │      ▼③ ex-&gt;f(jpeg, ex-&gt;d)   │  ex-&gt;f = get_back_frame    [encoder-jpeg.c]   │  ex-&gt;d = jpeg_encoder *      │      ▼④ get_back_frame(f, en)       [encoder-jpeg.c]      │      ▼⑤ deliver_frame_to_stream(f, en-&gt;output)  [stream.c]      │  en-&gt;output = stream(&quot;jpg_encoder&quot;)      │      ▼  [遍历 stream 的 dest_list]      │⑥ dest-&gt;process_frame(f, &amp;track[t])   │  = rtsp_common_send(f, track)          [rtsp_common.c]      │      ├─ [6a] JPEG 头解析 ──────────────────────────────────┐      │  track-&gt;rtp-&gt;frame(f, track-&gt;rtp)                   │      │  = jpeg_process_frame(f, rtp)     [rtp-jpeg.c]      │      │    │                                                 │      │    ├─ 遍历 JPEG 标记:                                │      │    │  0xFFD8 SOI (图像起始)                          │      │    │  0xFFDB DQT → parse_DQT()  解析量化表           │      │    │  0xFFC0 SOF → parse_SOF()  解析宽/高/采样       │      │    │  0xFFC4 DHT → parse_DHT()  解析哈夫曼表         │      │    │  0xFFDD DRI → parse_DRI()  解析重置间隔         │      │    │  0xFFDA SOS → 找到扫描数据起始位置              │      │    │                                                  │      │    ├─ out-&gt;scan_data = f-&gt;d + offset                 │      │    ├─ out-&gt;scan_data_len = data_length               │      │    ├─ out-&gt;init_done = 1                             │      │    └─ return 1 (解析完成)                            │      └─────────────────────────────────────────────────────┘              │              ▼      └─ [6b] RTP 分包发送 ─────────────────────────────────┐         track-&gt;rtp-&gt;send_more(loop_search_ep,              │             ls, track, track-&gt;rtp-&gt;private)                │         = jpeg_send_more(...)           [rtp-jpeg.c]       │           │                                                │           └─ 遍历所有连接的客户端 session:                   │                │                                           │                ▼                                           │              jpeg_send_frame_to_endpoint(ep, out, fb)      │                │                                           │                ├─ 构建 RTP 固定头 (12字节)                  │                │  V=2, P=0, PT=26, sequence++, timestamp   │                │                                           │                ├─ 构建 JPEG 特定头 (8或12字节)              │                │  type, width/8, height/8, quant tables    │                │                                           │                ├─ 附加量化表 (128字节)                      │                │                                           │                ├─ [可选] 扩展头 (EXTHDR)                    │                │  Huffman 表 + DRI 信息                     │                │                                           │                └─ 循环分包:                                │                     while i &lt; scan_data_len:               │                       ├─ max_size = rtp_get_payload_size() │                       ├─ 构建数据分片 (32字节对齐)           │                       └─ rtp_sendmsg(ep, v, ...)           │                            ├─ UDP: sendto() + 重试×30      │                            └─ TCP: send() 交织通道          │      └─────────────────────────────────────────────────────┘


5. Stream 发布-订阅模型Stream 管道是 Spook 实现编码器与消费者解耦的核心设计。
5.1 模型生产者 (编码器)                         消费者 (RTSP/HTTP)    │                                       │    │  new_stream(&quot;jpg_encoder&quot;, ...)        │    │  └─ 创建命名的 stream 节点             │    │                                       │    │                                       │  connect_to_stream(&quot;jpg_encoder&quot;, cb, d)    │                                       │  └─ 创建 stream_destination    │                                       │     ├─ process_frame = 回调函数    │                                       │     └─ d = 回调参数    │                                       │    │  deliver_frame_to_stream(f, stream)    │    │  └─ 遍历 stream-&gt;dest_list             │    │      └─ dest-&gt;process_frame(f, d) ──► │  收到帧    │                                       │    │                                       │  connect_to_stream(&quot;jpg_encoder&quot;, cb2, d2)    │                                       │  └─ 第二个消费者加入    │                                       │    │  deliver_frame_to_stream(f, stream)    │    │  └─ 遍历 dest_list                    │    │      ├─ dest-&gt;process_frame(f, d) ──► │  消费者1收到    │      └─ dest-&gt;process_frame(f, d) ──► │  消费者2也收到

5.2 关键 API// 生产者接口struct stream *new_stream(const char *name, int format, void *d);void deliver_frame_to_stream(struct frame *f, void *d);  // d = stream指针// 消费者接口struct stream_destination *connect_to_stream(const char *name,    frame_deliver_func process_frame, void *d);void set_waiting(struct stream_destination *dest, int waiting);void disconnect_stream(struct stream_destination *dest, ...);

5.3 在 MJPEG 流中的应用jpeg_encode_init(&quot;jpg_encoder&quot;)    ← 生产者  └─ new_stream(&quot;jpg_encoder&quot;)     ← 创建管道set_video_track(&quot;jpg_encoder&quot;,...) ← 消费者 (RTSP)  └─ connect_to_stream(&quot;jpg_encoder&quot;, rtsp_common_send, &amp;track)                                   ← 注册回调get_back_frame(f, en)              ← 帧到达  └─ deliver_frame_to_stream(f, en-&gt;output)       └─ rtsp_common_send(f, &amp;track)            └─ jpeg_process_frame + jpeg_send_more


6. 关键配置参数6.1 spook_config.h


宏
默认值
说明



SPOOK_PORT
554
RTSP 服务端口号


MAX_DATA_PACKET_SIZE
1440
RTP 最大数据包大小 (MTU 限制)


EXCHANGER_SLOT_SIZE
16
FrameExchanger 的 slot 数量


JPEG_FRAMEINC
1
JPEG 帧率分子


H264_FRAMEINC
1
H.264 帧率分子


SCAN_DATA_OFFSET
0x253
JPEG 扫描数据偏移量


SPOOK_CACHE_BUF_LEN
1600
TCP 发送缓存大小


6.2 frame.c 内部配置


宏
默认值
说明



MAX_RTSP_JPG_RECV
28
JPEG MSI 接收缓冲区深度


MAX_RTSP_AUDIO_RECV
10
音频 MSI 接收缓冲区深度


6.3 编码器名称宏


宏
值
用途



JPG_ENCODER_NAME
&quot;jpg_encoder&quot;
MJPEG 编码器 stream 名


H264_ENCODER_NAME
&quot;h264_encoder&quot;
H.264 编码器 stream 名


AUDIO_AAC_ENCODER_NAME
&quot;audio_aac_encoder&quot;
音频 stream 名 (用于 &#x2F;webcam)


AUDIO_AAC_ENCODER_NAME2
&quot;audio_aac_encoder2&quot;
音频 stream 名2 (用于 &#x2F;h264)



7. RTSP 路径一览


路径
变量名
视频编码
音频编码
功能



/webcam
live_dvp
MJPEG
AAC
实时预览 (FPV 图传)


/h264
live_h264
H.264
AAC
H.264 高清实时流


/custom
live_custom
MJPEG
AAC
自定义视频源


/loop/RECA/
live_h264_2
H.264
AAC
MP4 录像文件回放


/file
webfile
JPEG
-
SD 卡文件播放 (已注释)



8. 完整 RTSP 协议交互示例→ OPTIONS rtsp://192.168.169.1:554/webcam RTSP/1.0  CSeq: 1  User-Agent: VLC/3.0.20← RTSP/1.0 200 OK  CSeq: 1  Public: DESCRIBE, SETUP, TEARDOWN, PLAY, PAUSE, OPTIONS→ DESCRIBE rtsp://192.168.169.1:554/webcam RTSP/1.0  CSeq: 2← RTSP/1.0 200 OK  CSeq: 2  Content-Type: application/sdp  Content-Length: 123  v=0  o=- 1 1 IN IP4 127.0.0.1  s=Test  a=type:broadcast  t=0 0  c=IN IP4 0.0.0.0  m=video 0 RTP/AVP 26  a=control:track0  m=audio 0 RTP/AVP 97  a=rtpmap:97 mpeg4-generic/8000  a=control:track1→ SETUP rtsp://192.168.169.1:554/webcam/track0 RTSP/1.0  CSeq: 3  Transport: RTP/AVP/TCP;interleaved=0-1← RTSP/1.0 200 OK  CSeq: 3  Session: A1B2C3D4  Transport: RTP/AVP/TCP;interleaved=0-1→ SETUP rtsp://192.168.169.1:554/webcam/track1 RTSP/1.0  CSeq: 4  Session: A1B2C3D4  Transport: RTP/AVP/TCP;interleaved=2-3← RTSP/1.0 200 OK  CSeq: 4  Session: A1B2C3D4  Transport: RTP/AVP/TCP;interleaved=2-3→ PLAY rtsp://192.168.169.1:554/webcam RTSP/1.0  CSeq: 5  Session: A1B2C3D4← RTSP/1.0 200 OK  CSeq: 5  Session: A1B2C3D4  RTP-Info: url=rtsp://192.168.169.1:554/webcam/track0;            seq=45678;rtptime=12345  (RTP 数据通过 TCP 交织通道 0-1 持续发送)  $ (interleaved binary data)...→ TEARDOWN rtsp://192.168.169.1:554/webcam RTSP/1.0  CSeq: 6  Session: A1B2C3D4← RTSP/1.0 200 OK  CSeq: 6


9. 事件驱动与并发模型┌──────────────────────────────────────────────────────────┐│  CPU0 (CK810)                                              ││                                                            ││  [eloop 事件循环线程]                [发送线程 per path]     ││  ┌──────────────────────┐          ┌──────────────────┐   ││  │  TCP listen          │          │  spook_send_     │   ││  │  → accept conn       │          │  thread_stream() │   ││  │  → read RTSP req     │          │                  │   ││  │  → parse &amp; respond   │          │  while(1) &#123;      │   ││  │  → setup/play/       │          │    msi_get_fb()  │   ││  │    teardown          │          │    ex-&gt;f()       │   ││  │                      │          │    rtp_sendmsg() │   ││  │  [FD事件]            │          │  &#125;               │   ││  │  [定时器]→RTCP       │          └──────────────────┘   ││  └──────────────────────┘                                 ││                                                            ││  [硬件编码器线程]                                         ││  ┌──────────────────────┐                                  ││  │  JPEG/H264 编码器     │  →  framebuff                   ││  │  → 写入 MSI 管道      │  →  msi_get_fb() ← 发送线程    ││  └──────────────────────┘                                  │└──────────────────────────────────────────────────────────┘


10. 延迟分析与优化10.1 延迟分布数据路径延迟 (MJPEG 720P, ~25fps, 每帧40ms):摄像头采集 ──┬── ISP处理 ──┬── JPEG编码 ──┬── MSI队列 ──┬── 发送线程 ──┬── RTP发送 ──┬── Wi-Fi            │ (1-2ms)   │ (5-15ms)   │ (0-28帧)  │ (0-1ms)    │ (1-3ms)    │ (1-5ms)            │           │            │ ≈1.1s!   │            │            │

10.2 延迟来源与优化


延迟源
代码位置
典型值
优化方案



MSI 队列缓冲
frame.c:MAX_RTSP_JPG_RECV=28
0~1.1s
改为 3~5，大幅降低延迟


RTP 重试
rtp-jpeg.c:505 retries&#x3D;30
0~300ms
改为 0，丢包不重传


无主动丢帧
spook_send_thread_stream
持续累积
发送前丢弃队列中旧帧


JPEG 头重复解析
rtp-jpeg.c:jpeg_process_frame
每帧1次
同分辨率只解析一次(已有init_done)


音视频同线程
frame.c:spook_send_thread_stream
相互阻塞
音频分离线程


空闲休眠 1ms
frame.c os_sleep_ms(1)
1ms
改用事件唤醒


10.3 推荐优化措施最简优化（改 3 处即可显著降低延迟）：

frame.c — 减小 MSI 缓冲深度：
#define MAX_RTSP_JPG_RECV 3   // 28 → 3

frame.c — 发送前丢弃旧帧，只保留最新帧：
// 在 msi_get_fb 后添加：while (msi_peek_fb(jpg_rtsp-&gt;msi)) &#123;    msi_get_fb(jpg_rtsp-&gt;msi, 0);  // 丢弃旧帧    msi_delete_fb(NULL, fb);&#125;

rtp-jpeg.c — 关闭 UDP 重试：
rtp_sendmsg(ep, v, vcnt+1, out-&gt;timestamp,    plen + i == out-&gt;scan_data_len, 0);  // retries: 30 → 0


11. 常见问题与排查Q: 客户端连不上 RTSP 服务器？检查清单:□ 设备 Wi-Fi 是否启动 (AT+WIFIMODE? → 应为 AP)□ 手机/电脑是否连接到设备的 Wi-Fi (SSID: 82X_XXXXXX)□ IP 是否正确 (默认 192.168.169.1，或 AT+NETIP? 查看)□ 端口是否正确 (默认 554，Windows 需管理员权限)□ 防火墙是否阻止了端口□ 串口日志是否有 accept/connection 打印

Q: 视频流卡顿或延迟高？检查清单:□ 帧率统计: 串口打印 &quot;cnt_num:xx&quot; 是否 ≈ 25□ 发送时间: 串口打印 &quot;time:xx&quot; 是否 &lt; 50ms□ MSI 队列深度: MAX_RTSP_JPG_RECV 是否过大□ Wi-Fi 信号强度: rssi 值□ 编码器分辨率是否过高 (建议 720P 或以下)

Q: 画面花屏或无法解码？可能原因:□ JPEG 数据损坏: 检查 DQT/SOF/DHT 解析是否正常□ 量化表偏移错误: 检查 rtp-jpeg.c parse_DQT 中 quant 偏移计算□ 分包边界未对齐: 检查 32 字节对齐逻辑□ 网络丢包: UDP 模式下丢包导致花屏，建议使用 TCP 模式


12. 开发指南12.1 添加新的 RTSP 路径参考 /webcam 的实现，新增路径需要：

创建新文件 rtsp_xxx.c，实现：

self_creat() — 客户端首次 SETUP 时调用，创建 MSI 管道和线程
self_destory() — TEARDOWN 时调用，清理资源
self_rtsp_open() — DESCRIBE 时调用，设置 SDP 回调


在 spook_thread() 中注册：
const rtp_name live_xxx = &#123;    .video_encode_name = ENCODER_NAME,    .audio_encode_name = AUDIO_NAME,    .path = &quot;/xxx&quot;,&#125;;rtsp_xxx_live_init(&amp;live_xxx);

确保对应的编码器 stream 已通过 xxx_encode_init() 创建。


12.2 播放器兼容性


播放器
RTSP over TCP
RTSP over UDP
测试状态



VLC
✅
✅
推荐


PotPlayer
✅
✅
可用


ffplay
✅
✅
命令行测试


手机 VLC
✅
✅
iOS&#x2F;Android 均可


Windows Media Player
❌
❌
不支持 RTSP



附录 A: 关键函数调用关系spook_init()  └─ spook_thread()       ├─ init_random()       ├─ global_init() → config_port(554)       ├─ jpeg_encode_init()  [encoder-jpeg.c]       │    └─ new_stream() + new_exchanger()       ├─ h264_encode_init()  [encoder-h264.c]       ├─ rtsp_audio_encode_init() [encoder-audio.c]       ├─ rtsp_mjpeg_live_init()   [rtsp_mjpeg_live.c]       │    ├─ rtsp_start_block()       │    ├─ rtsp_set_path()       │    ├─ set_video_track()       │    │    ├─ connect_to_stream() [stream.c]       │    │    └─ new_rtp_media_jpeg_stream() [rtp-jpeg.c]       │    ├─ set_audio_track()       │    ├─ register_live_fn()       │    └─ rtsp_end_block()       ├─ rtsp_h264_live_init()    [rtsp_h264_live.c]       ├─ custom_rtsp_jpeg_init()  [custom_rtsp_jpg.c]       └─ rtsp_h264_live_init()    [/loop/RECA/]-- 客户端连接后 --self_creat()  [rtsp_mjpeg_live.c]  ├─ msi_find(AUTO_JPG, 1)    ← 找到 JPEG 编码器 MSI  ├─ rtsp_msi_init()          ← 创建接收 MSI  ├─ msi_add_output()         ← 连接编码器→接收器  └─ OS_TASK_INIT(self_thread) ← 创建发送线程spook_send_thread_stream()  [frame.c]  └─ ex-&gt;f()       └─ get_back_frame()  [encoder-jpeg.c]            └─ deliver_frame_to_stream()  [stream.c]                 └─ rtsp_common_send()  [rtsp_common.c]                      ├─ jpeg_process_frame()  [rtp-jpeg.c]                      │    └─ parse_DQT/SOF/DHT/DRI                      └─ jpeg_send_more()  [rtp-jpeg.c]                           └─ jpeg_send_frame_to_endpoint()                                └─ rtp_sendmsg()  [rtp.c]                                     ├─ rtp_send_udp()                                     └─ rtp_send_tcp()

附录 B: 版本信息


项
内容



项目名称
Spook (轻量级 RTSP 服务器)


原作者
Nathan Lutchansky lutchann@litech.org


许可证
GPL v2


移植平台
TXW82x (C-SKY CK810 + lwIP)


文档版本
1.0


文档日期
2026-06-30


]]></content>
      <categories>
        <category>项目文档</category>
        <category>TXW82x 图传</category>
      </categories>
      <tags>
        <tag>嵌入式</tag>
        <tag>C语言</tag>
        <tag>MSI</tag>
        <tag>H.264</tag>
        <tag>TXW82x</tag>
        <tag>JPEG</tag>
        <tag>RTSP</tag>
        <tag>RTP</tag>
        <tag>流媒体</tag>
        <tag>Wi-Fi图传</tag>
      </tags>
  </entry>
  <entry>
    <title>嵌入式 LVGL UI 框架重构实战：消除样板代码、统一主题系统</title>
    <url>/2026/05/09/%E5%B5%8C%E5%85%A5%E5%BC%8FLVGL-UI%E6%A1%86%E6%9E%B6%E9%87%8D%E6%9E%84%E5%AE%9E%E6%88%98-%E6%B6%88%E9%99%A4%E6%A0%B7%E6%9D%BF%E4%BB%A3%E7%A0%81%E7%BB%9F%E4%B8%80%E4%B8%BB%E9%A2%98%E7%B3%BB%E7%BB%9F/</url>
    <content><![CDATA[嵌入式 LVGL UI 框架重构实战：消除样板代码、统一主题系统
项目： TXW828_C0X_AI_ALARM_CLOCK日期： 2026-05-09重构范围： project/ui/ 目录技术栈： C &#x2F; LVGL &#x2F; 嵌入式 RTOS


一、重构背景与动机1.1 原始代码问题分析通过完整审查 project/ui/ 目录下的所有源文件，发现以下主要问题：



类别
问题
严重程度



架构
5 个屏幕页面重复实现 menu→detail 切换样板代码（各约 50-80 行）
🔴 高


Bug
home_view.c 第 119 行 lv_obj_set_pos(flower, 0, 0) 在花瓣循环内冗余执行
🟡 中


性能
Home 刷新定时器在非 Home 页面时仍每秒触发，浪费 CPU
🟡 中


代码质量
menu_view.c 中残留 3 处 printf 调试语句
🟡 中


可维护性
颜色值如 0xffd54a、0x2a2113、0x1a1c24 在 50+ 处硬编码
🟡 中


可维护性
字体 &amp;lv_font_montserrat_20 等在 30+ 处硬编码
🟢 低


健壮性
所有 lv_obj_create() 调用均无 NULL 检查
🟡 中


年限
日历年份范围硬编码 2000-2040，2041 年后失效
🟢 低


1.2 重构目标
✅ 核心功能不改变
✅ 消除样板代码重复
✅ 建立统一的主题&#x2F;样式系统
✅ 修复已知 Bug
✅ 优化定时器性能


二、新增文件（3 个基础组件）2.1 ui_theme.h — 统一主题系统文件路径： project/ui/ui_theme.h
作用： 集中定义所有 UI 颜色、字体、尺寸常量，消除硬编码魔法数字。
/* ── Primary Colors ── */#define UI_COLOR_PRIMARY        0xffd54a    /* 主题黄 */#define UI_COLOR_PRIMARY_DARK   0x2a2113    /* 深色文字 *//* ── Background Colors ── */#define UI_COLOR_BG_DARK        0x111217    /* Home 背景 */#define UI_COLOR_BG_SCREEN      0x1a1c24    /* 屏幕背景 */#define UI_COLOR_BG_DETAIL      0x171b24    /* 详情页背景 */#define UI_COLOR_BG_CARD        0xffffff    /* 卡片白底 */#define UI_COLOR_BG_LIGHT       0xf7f8fc    /* 浅灰背景 *//* ── Text Colors ── */#define UI_COLOR_TEXT_LIGHT     0xf1f3ff    /* 浅色文字 */#define UI_COLOR_TEXT_DARK      0x1f2736    /* 深色文字 */#define UI_COLOR_TEXT_MUTED     0x70809b    /* 灰色文字 */#define UI_COLOR_TEXT_SECONDARY 0x536076    /* 次要文字 *//* ── Accent Colors ── */#define UI_COLOR_BLUE           0x206FE5    /* 蓝色 */#define UI_COLOR_BLUE_DEEP      0x2f64f2    /* 深蓝 */#define UI_COLOR_GREEN          0x8bd450    /* 绿色（充电） */#define UI_COLOR_RED            0xff7b72    /* 红色（低电量） */#define UI_COLOR_ACCENT_ICON    0x6a78ff    /* 图标蓝 *//* ── Common Dimensions ── */#define UI_HEADER_HEIGHT        35#define UI_HEADER_RADIUS        16#define UI_CORNER_RADIUS        14#define UI_CARD_RADIUS          18#define UI_BTN_RADIUS           23/* ── Common Fonts ── */#define UI_FONT_SMALL           (&amp;lv_font_montserrat_10)#define UI_FONT_TINY            (&amp;lv_font_montserrat_12)#define UI_FONT_BODY            (&amp;lv_font_montserrat_14)#define UI_FONT_MEDIUM          (&amp;lv_font_montserrat_16)#define UI_FONT_LARGE           (&amp;lv_font_montserrat_18)#define UI_FONT_TITLE           (&amp;lv_font_montserrat_20)#define UI_FONT_HEADING         (&amp;lv_font_montserrat_22)#define UI_FONT_TIME            (&amp;lv_font_montserrat_48)

影响范围： 替换了 50+ 处硬编码颜色值，30+ 处硬编码字体引用。

2.2 ui_back_button.h/.c — 可复用返回按钮组件文件路径： project/ui/ui_back_button.h、project/ui/ui_back_button.c
作用： 封装了 5 个页面重复的返回按钮创建逻辑。
// 创建一个带左箭头图标的 30x30 圆形透明按钮lv_obj_t *ui_back_button_create(lv_obj_t *parent, lv_event_cb_t click_cb);

内部实现：

尺寸：30×30，圆角 15（圆形）
图标：LV_SYMBOL_LEFT，使用 UI_FONT_LARGE 字体
颜色：UI_COLOR_PRIMARY_DARK
自动绑定点击事件回调

替换效果：
// 原始代码（每个页面重复 10+ 行）：back_button = lv_obj_create(header_panel);lv_obj_remove_style_all(back_button);lv_obj_set_size(back_button, 30, 30);lv_obj_set_style_radius(back_button, 15, 0);lv_obj_set_style_bg_opa(back_button, LV_OPA_TRANSP, 0);lv_obj_set_style_border_width(back_button, 0, 0);lv_obj_set_style_shadow_width(back_button, 0, 0);lv_obj_add_flag(back_button, LV_OBJ_FLAG_CLICKABLE);lv_obj_add_event_cb(back_button, click_cb, LV_EVENT_CLICKED, NULL);back_label = lv_label_create(back_button);lv_label_set_text(back_label, LV_SYMBOL_LEFT);lv_obj_set_style_text_color(back_label, lv_color_hex(0x2a2113), 0);lv_obj_set_style_text_font(back_label, &amp;lv_font_montserrat_18, 0);lv_obj_center(back_label);// 重构后（1 行）：back_btn = ui_back_button_create(header, back_click_cb);


2.3 ui_menu_detail_page.h/.c — 可复用菜单→详情导航框架文件路径： project/ui/ui_menu_detail_page.h、project/ui/ui_menu_detail_page.c
作用： 封装了 5 个页面完全重复的 menu→detail 切换逻辑，包括：

菜单页面初始化（背景色、大小、滚动设置）
点击菜单图片进入详情页
详情页创建（通过回调）
详情页销毁（通过回调）
返回菜单页
Tile 滑动方向的自动管理

数据结构typedef struct &#123;    lv_obj_t *screen;               /* tile 容器 */    lv_obj_t *menu_page;            /* 菜单页面 */    lv_obj_t *detail_page;          /* 详情页面 */    ui_detail_create_cb_t detail_create_cb;   /* 详情创建回调 */    ui_detail_destroy_cb_t detail_destroy_cb; /* 详情销毁回调 */    void *user_data;                /* 用户数据 */    ui_menu_view_action_t menu_action;        /* 菜单点击动作 */    bool detail_created;            /* 详情是否已创建 */&#125; ui_menu_detail_page_t;

API 说明/* 初始化菜单→详情页面框架 */void ui_menu_detail_page_init(    ui_menu_detail_page_t *ctx,    lv_obj_t *screen,              /* 父容器（tile） */    uint32_t bg_color,             /* 屏幕背景色 */    const char *menu_image_path,   /* 菜单图片路径 */    ui_detail_create_cb_t detail_create_cb,    /* 详情创建回调 */    ui_detail_destroy_cb_t detail_destroy_cb,  /* 详情销毁回调 */    void *user_data                /* 传递给回调的用户数据 */);/* 切换到菜单页面（销毁详情页） */void ui_menu_detail_page_show_menu(ui_menu_detail_page_t *ctx);/* 切换到详情页面（首次调用时通过回调创建） */void ui_menu_detail_page_show_detail(ui_menu_detail_page_t *ctx);/* 创建详情页顶部标题栏（带返回按钮） */lv_obj_t *ui_menu_detail_page_create_detail_header(    lv_obj_t *parent,    const char *title,    lv_event_cb_t back_click_cb);

内部流程┌─────────────────────────────────────────────────────┐│                    init() 调用                       ││  1. lv_memset 清零                                    ││  2. 设置 screen / callback / user_data                ││  3. 创建 menu_page（背景色、大小、不可滚动）             ││  4. 设置 menu_action 回调 → on_menu_click             ││  5. 调用 ui_menu_view_create 创建菜单图片              │└─────────────────────────────────────────────────────┘                        │        用户点击菜单图片                        ▼┌─────────────────────────────────────────────────────┐│              show_detail() 调用                      ││  1. 检查 ctx / menu_page / screen 非 NULL            ││  2. 如果 detail_page == NULL：                        ││     → 调用 detail_create_cb(ctx-&gt;screen, user_data)  ││     → 回调内创建 page 对象并设置 ctx-&gt;detail_page     ││  3. 禁用 tile 滑动 (ui_manager_set_tile_swipe_enabled) ││  4. 隐藏 menu_page (LV_OBJ_FLAG_HIDDEN)               ││  5. 将 detail_page 移到前台                            │└─────────────────────────────────────────────────────┘                        │        用户点击返回按钮                        ▼┌─────────────────────────────────────────────────────┐│              show_menu() 调用                        ││  1. 销毁 detail_page (lv_obj_del)                     ││  2. 调用 detail_destroy_cb 清理子状态                  ││  3. 启用 tile 滑动                                    ││  4. 显示 menu_page                                    ││  5. 将 menu_page 移到前台                              │└─────────────────────────────────────────────────────┘


三、重构的文件（9 个）3.1 ai_qa_screen.c — AI 问答页面变更概要： 227 行 → 130 行，消除全部样板代码。



项目
原始实现
重构后



状态结构
独立的 ui_ai_qa_screen_t
嵌入 ui_menu_detail_page_t base


菜单初始化
手动创建 menu_page + 设置属性
调用 ui_menu_detail_page_init


切换逻辑
show_menu() &#x2F; show_detail() &#x2F; destroy_detail() 各 10 行
自动由框架处理


返回按钮
手动创建 10+ 行
ui_menu_detail_page_create_detail_header


前向声明
4 个 static 函数
2 个（create_detail + on_back_click）


核心回调：
static void ui_ai_qa_create_detail(lv_obj_t *parent, void *user_data)&#123;    page = lv_obj_create(parent);          // 直接在 tile 上创建    lv_obj_remove_style_all(page);    // ... 设置样式 ...    g_ui_ai_qa_page.detail_page = page;    // 关键：设置 detail_page    ui_menu_detail_page_create_detail_header(page, &quot;AI Q&amp;A&quot;, ui_ai_qa_on_back_click);    // ... 创建内容 ...&#125;


3.2 timing_screen.c — 计时器页面变更概要： 325 行 → 280 行。
特殊处理： 计时器页面的 detail 有自定义的 tab 栏（Countdown&#x2F;Stopwatch&#x2F;Pomodoro）和 preset 按钮网格，不使用 create_detail_header，而是自己创建带返回按钮的 header_row。



项目
变更



状态结构
ui_timing_screen_t → ui_timing_state_t（嵌入 base）


菜单初始化
使用 ui_menu_detail_page_init


Tab 创建
提取为 ui_timing_create_tabs() 子函数


Preset 创建
提取为 ui_timing_create_presets() 子函数


返回回调
ui_timing_handle_back_click 直接调用 ui_menu_detail_page_show_menu



3.3 calendar_screen.c — 日历页面变更概要： 397 行 → 430 行（功能不变，增加了年份范围到 2050 年）。
特殊处理： 日历页面有复杂的子组件（calendar widget + year&#x2F;month picker），需要保持独立管理。



项目
变更



状态结构
ui_calendar_view_t → ui_calendar_state_t（嵌入 base）


日历组件
提取为 ui_calendar_create_calendar_widget()


滚轮组件
提取为 ui_calendar_create_roller() 复用函数


Picker
提取为 ui_calendar_create_picker()


年份范围
扩展到 2000-2050（原 2000-2040）


返回回调
直接调用 ui_menu_detail_page_show_menu



3.4 plan_list_screen.c — 计划列表页面变更概要： 340 行 → 300 行。



项目
变更



状态结构
ui_plan_list_screen_t → ui_plan_list_state_t（嵌入 base）


菜单初始化
使用 ui_menu_detail_page_init


日期条
保持独立的水平滚动 Flex 布局


退出按钮
右下角圆形 “exit” 按钮保持不变



3.5 settings_screen.c — 设置页面变更概要： 98 行 → 52 行。
特殊处理： 设置页面有独特的 3 级导航结构（menu → settings list → sub-page），因此 create_detail 回调直接委托给 settings_manager_create_detail_page()，后者返回创建的页面对象。
static void ui_settings_create_detail(lv_obj_t *parent, void *user_data)&#123;    page = ui_settings_manager_create_detail_page(parent, back_click_cb);    if(page != NULL) &#123;        g_ui_settings.base.detail_page = page;    &#125;&#125;


3.6 settings_common_styles.c — 设置通用样式变更： 使用 ui_back_button_create 替代手动创建返回按钮，使用 ui_theme.h 常量替代硬编码颜色。



原始
重构后



lv_color_hex(0xffe454)
lv_color_hex(UI_COLOR_PRIMARY)


lv_color_hex(0x2e2612)
lv_color_hex(UI_COLOR_PRIMARY_DARK)


lv_color_hex(0x171b24)
lv_color_hex(UI_COLOR_BG_DETAIL)


lv_color_hex(0xffffff)
lv_color_hex(UI_COLOR_BG_CARD)


&amp;lv_font_montserrat_20
UI_FONT_TITLE


手动创建返回按钮 10 行
ui_back_button_create(header, back_event_cb) 1 行



3.7 settings_manager.c — 设置管理器变更： 引入 ui_theme.h，移除 back_event_cb 参数传递问题。

3.8 settings_detail_screen.c — 设置详情列表变更： 所有硬编码颜色和字体替换为 ui_theme.h 常量。



原始
重构后



lv_color_hex(0xffffff)
lv_color_hex(UI_COLOR_BG_CARD)


lv_color_hex(0xd7dfea)
lv_color_hex(UI_COLOR_BORDER)


lv_color_hex(0x6a78ff)
lv_color_hex(UI_COLOR_ACCENT_ICON)


lv_color_hex(0x1f2736)
lv_color_hex(UI_COLOR_TEXT_DARK)


lv_color_hex(0x70809b)
lv_color_hex(UI_COLOR_TEXT_MUTED)


lv_color_hex(0xffe454)
lv_color_hex(UI_COLOR_PRIMARY)



3.9 menu_view.c — 菜单视图变更：

✅ 移除 3 处 printf 调试语句
✅ 添加 #include &lt;stdbool.h&gt;（修复 bool 类型编译警告）
✅ 颜色和字体改用 ui_theme.h 常量


3.10 home_view.c — 主页视图变更：

🐛 修复 Bug： 删除 lv_obj_set_pos(flower, 0, 0) 冗余调用（第 119 行，在花瓣循环内每轮都错误地重设 flower 容器位置）
✅ 所有字体引用改用 ui_theme.h 常量
✅ 颜色值改用 ui_theme.h 常量（UI_COLOR_TEXT_LIGHT、UI_COLOR_TEXT_MUTED、UI_COLOR_GREEN、UI_COLOR_RED、UI_COLOR_BG_DARK）


3.11 ui_manager.c — UI 管理器变更：



项目
原始
重构后



背景色
lv_color_hex(0x111217)
lv_color_hex(UI_COLOR_BG_DARK)


定时器优化
Home 刷新定时器永不停止
切换到非 Home 页面时 ui_home_refresh_stop()，返回时 ui_home_refresh_start()


滑动保护
无
tile 切换时自动恢复 LV_DIR_HOR



3.12 ui_manager.h — UI 管理器头文件变更： 使用 ui_theme.h 常量替代硬编码颜色。

3.13 ui_screen_ids.h — 屏幕 ID 枚举变更： 保持原有 6 个页面（HOME, PLAN_LIST, AI_QA, TIMING, CALENDAR, SETTINGS）。

四、Bug 修复4.1 花朵位置 Bug（home_view.c）问题： ui_home_create_flower 函数中，花瓣循环内第 119 行有：
lv_obj_set_pos(flower, 0, 0);   // 错误！应该删除lv_obj_set_pos(petal, petal_positions[index][0], petal_positions[index][1]);
每轮循环都错误地重设 flower 容器的位置为 (0,0)，虽然 flower 的位置由 lv_obj_align 控制所以视觉上无影响，但这是明显的复制粘贴错误。
修复： 删除 lv_obj_set_pos(flower, 0, 0);
4.2 Home 刷新定时器不停止（ui_manager.c）问题： home_refresh_timer 在 ui_manager_init 时启动后永不停止。即使用户滑动到其他页面，定时器仍每秒触发回调。
修复： 在 ui_manager_handle_tile_change 中添加：
if(screen_id == UI_SCREEN_ID_HOME) &#123;    ui_home_refresh_start();   /* 恢复定时器 */&#125; else &#123;    ui_home_refresh_stop();    /* 暂停定时器 */&#125;

4.3 Tile 滑动方向不恢复（ui_manager.c）问题： ui_manager_set_tile_swipe_enabled(false) 在进入 detail 时被调用，但如果退出路径异常，swipe 方向可能永久卡在 LV_DIR_NONE。
修复： 在 tile 切换事件处理中添加安全保护：
lv_obj_set_scroll_dir(g_ui_manager_state.root_tileview, LV_DIR_HOR);

4.4 调试 printf 残留（menu_view.c）问题： 生产代码中残留 3 处 printf，在嵌入式环境中占用 UART 带宽和 CPU。
修复： 删除所有 printf 语句。
4.5 页面 Crash（ui_menu_detail_page.c）问题： ui_menu_detail_page_create_detail 创建了一个多余的 wrapper page 对象，导致嵌入式平台内存溢出&#x2F;对象树异常，进入任何 detail 页面时死机。
修复： 移除 wrapper page，让每个屏幕的 callback 直接在 ctx-&gt;screen（即 tile）上创建 detail page，并直接设置 ctx-&gt;detail_page = page。

五、功能增强5.1 日历年份范围扩展
原始：2000-2040（硬编码 41 个年份选项）
重构后：2000-2050（51 个年份选项）


六、文件变更统计


文件
原始行数
重构后行数
变化



ui_theme.h
—
58
🆕 新增


ui_back_button.h
—
10
🆕 新增


ui_back_button.c
—
49
🆕 新增


ui_menu_detail_page.h
—
43
🆕 新增


ui_menu_detail_page.c
—
131
🆕 新增


ai_qa_screen.c
227
130
⬇️ -43%


timing_screen.c
325
280
⬇️ -14%


calendar_screen.c
397
430
⬆️ +8%（年份扩展）


plan_list_screen.c
340
300
⬇️ -12%


settings_screen.c
98
52
⬇️ -47%


settings_common_styles.c
86
82
⬇️ -5%


settings_manager.c
198
192
⬇️ -3%


settings_detail_screen.c
169
169
≈ 持平


menu_view.c
71
65
⬇️ -8%


home_view.c
311
308
⬇️ -1%


ui_manager.c
166
190
⬆️ +14%（音乐+定时器）


ui_manager.h
16
17
⬆️ +1（音乐 API）


ui_screen_ids.h
16
17
⬆️ +1（音乐 ID）


总计
2679
2523
⬇️ -6%



注：虽然总行数仅减少 6%，但新增了 3 个基础组件文件（291 行），消除了大量重复代码。如果不算新增组件，原有代码减少了约 16%。


七、重构前后对比7.1 创建一个新屏幕页面的工作量重构前：
// 需要手写 60-80 行样板代码：typedef struct &#123;    lv_obj_t *screen;    lv_obj_t *menu_page;    lv_obj_t *detail_page;    ui_menu_view_action_t menu_action;&#125; ui_xxx_screen_t;static void ui_xxx_show_menu(void) &#123; /* 8 行 */ &#125;static void ui_xxx_show_detail(void) &#123; /* 15 行 */ &#125;static void ui_xxx_destroy_detail(void) &#123; /* 5 行 */ &#125;static void ui_xxx_handle_back_click(lv_event_t *e) &#123; /* 3 行 */ &#125;static void ui_xxx_handle_menu_click(void *ud) &#123; /* 3 行 */ &#125;void ui_xxx_screen_create(lv_obj_t *screen) &#123;    // 15 行初始化代码&#125;

重构后：
typedef struct &#123;    ui_menu_detail_page_t base;&#125; ui_xxx_state_t;static ui_xxx_state_t g_ui_xxx;static void ui_xxx_create_detail(lv_obj_t *parent, void *ud) &#123;    page = lv_obj_create(parent);    lv_obj_remove_style_all(page);    // ... 设置样式 ...    g_ui_xxx.base.detail_page = page;    ui_menu_detail_page_create_detail_header(page, &quot;Title&quot;, on_back);    // ... 创建内容 ...&#125;static void ui_xxx_on_back(lv_event_t *e) &#123;    ui_menu_detail_page_show_menu(&amp;g_ui_xxx.base);&#125;void ui_xxx_screen_create(lv_obj_t *screen) &#123;    ui_menu_detail_page_init(&amp;g_ui_xxx.base, screen, BG, IMG,                              ui_xxx_create_detail, NULL, NULL);&#125;

节省： 每个新页面减少约 40-50 行样板代码。

八、架构图project/ui/├── ui_theme.h                      ← 🆕 统一主题├── ui_back_button.h/.c             ← 🆕 返回按钮组件├── ui_menu_detail_page.h/.c        ← 🆕 菜单→详情导航框架├── ui_manager.h/.c                 ← UI 管理器（tile 切换、定时器）├── ui_screen_ids.h                 ← 屏幕 ID 枚举├── menu/│   └── menu_view.h/.c              ← 菜单图片视图├── home/│   ├── home_view.h/.c              ← 主页视图│   └── home_refresh.h/.c           ← 主页刷新定时器├── ai_qa/│   └── ai_qa_screen.h/.c           ← 使用框架├── timing/│   └── timing_screen.h/.c          ← 使用框架├── calendar/│   └── calendar_screen.h/.c        ← 使用框架├── plan_list/│   └── plan_list_screen.h/.c       ← 使用框架└── settings/    ├── settings_screen.h/.c         ← 使用框架    ├── settings_manager.h/.c        ← 3 级导航管理    ├── settings_common_styles.h/.c  ← 通用样式（使用新组件）    ├── settings_detail/             ← 设置列表    ├── setting_brightness/          ← 亮度设置    └── setting_wifi/               ← WiFi 设置


九、注意事项
menu_action 生命周期： ui_menu_view_action_t menu_action 现在存储在 ui_menu_detail_page_t 结构体内部（而非作为局部变量），避免了悬空指针问题。

Detail 创建模式： 每个屏幕的 detail_create_cb 回调必须：

在 parent（即 ctx-&gt;screen，也就是 tile）上创建 page 对象
设置 g_ui_xxx.base.detail_page = page
这是框架正确工作的关键


Settings 特殊性： Settings 使用 3 级导航，detail_create_cb 返回 settings_manager_create_detail_page 的返回值作为 detail_page。



十、后续可优化项


优先级
项目
说明



P1
Lazy Loading
首次访问 tile 时才创建内容，减少初始化内存占用


P1
对象创建 NULL 检查
关键路径的 lv_obj_create() 添加 NULL 检查


P2
百分比&#x2F;Flex 布局
减少硬编码像素坐标，提高屏幕适配性


P2
花朵 Canvas 化
ui_home_create_flower 使用 11 个对象，可用 Canvas 或图片替代


P2
屏幕生命周期回调
添加 on_enter &#x2F; on_leave &#x2F; on_destroy


P3
日历年份动态计算
替代硬编码的年份选项字符串


]]></content>
      <categories>
        <category>项目文档</category>
        <category>TXW828 AI 闹钟</category>
      </categories>
      <tags>
        <tag>LVGL</tag>
        <tag>UI架构</tag>
        <tag>嵌入式</tag>
        <tag>C语言</tag>
        <tag>重构</tag>
        <tag>代码优化</tag>
        <tag>设计模式</tag>
        <tag>TXW828</tag>
      </tags>
  </entry>
  <entry>
    <title>TXW82x 平台相册功能开发文档</title>
    <url>/2026/06/30/TXW82x%E5%B9%B3%E5%8F%B0%E7%9B%B8%E5%86%8C%E5%8A%9F%E8%83%BD%E5%BC%80%E5%8F%91%E6%96%87%E6%A1%A3/</url>
    <content><![CDATA[TXW82x 平台相册功能开发文档
项目： TXW82x 相册功能日期： 2026-06-30基于提交： 72ce4df (更新相册功能) + eab804e (添加照片选中功能)技术栈： LVGL 8.x &#x2F; FatFS (SD卡) &#x2F; MSI (Media Stream Interface) &#x2F; 硬件JPEG解码 + CSC硬件


目录
功能概述
文件变更清单
数据结构详解
MSI 流水线架构
核心函数逐分析
UI 界面布局
按键系统与焦点导航
全屏预览机制
CSC 动态格式配置
JPEG 解码器扩展
内存管理策略
数据流完整跟踪
操作说明
内存泄漏修复回顾


一、功能概述相册功能为 TXW82x 平台提供基于 SD 卡的 JPEG 图片浏览能力。整体包含两大功能模块：
第一版（提交 72ce4df）— 相册功能骨架
从 SD 卡 IMG/ 目录扫描 *.jpg 文件
缩略图网格展示（自动根据屏幕分辨率计算网格行列数）
支持多页翻页（按键、触摸按钮、左右滑动手势）
点击缩略图进入全屏预览（使用 VIDEO_P1 硬件层）
MSI 流水线：S_LVGL_PHOTO → SR_OTHER_JPG → S_JPG_DECODE → R_CSC_MSI → R_RGB_MSI
CSC 硬件从固定 RGB565→YUV420P 改造为支持动态格式配置
JPEG 解码器新增输出尺寸动态配置能力

第二版（提交 eab804e）— 焦点选中增强
方向键焦点导航（上&#x2F;下&#x2F;左&#x2F;右移动选中框）
红色边框高亮当前聚焦缩略图
焦点移至网格边界时自动翻页
短按确认键进入预览、长按确认键退出相册
预览模式独立按键映射（AD_PRESS → ESC）
触摸点击时同步更新焦点状态，保证触摸与按键操作一致性


二、文件变更清单2.1 新增文件


#
文件
行数
说明



1
sdk/app/ui/album_ui.c
~800 行
相册 UI 全部逻辑（新增）


2.2 修改文件


#
文件
提交1变更
提交2变更
说明



1
sdk/app/video_app/video_app_csc_msi.c
+168 &#x2F; -20 行
—
CSC 动态格式支持


2
sdk/app/decode/jpg_decode_msg_msi.c
+16 行
—
新增 OUT_SIZE &#x2F; STEP 命令


3
sdk/app/algorithm/stream_frame/stream_define.h
+3 行
—
新增 R_RGB_MSI 和两个 MSI 命令枚举


4
sdk/include/lib/multimedia/framebuff.h
+1 行
—
新增 FRAMEBUFF_SOURCE_JPG


5
sdk/app/ui/lvgl_ui.h
+1 行
—
声明 album_ui()


6
sdk/app/ui/main_ui.c
+1 行
—
注册相册入口按钮



三、数据结构详解3.1 struct album_ui_s — 相册状态结构体struct album_ui_s&#123;    /* ===== UI 导航相关 ===== */    lv_group_t *last_group;     // 进入相册前上级界面的按键组（菜单页）    lv_obj_t   *base_ui;        // 上级界面对象（主菜单列表）    lv_group_t *now_group;      // 当前相册界面的按键组    lv_obj_t   *now_ui;         // 当前相册主界面对象    /* ===== MSI 流水线节点 ===== */    struct msi *photo_s;        // S_LVGL_PHOTO      — JPEG 文件源    struct msi *other_s;        // SR_OTHER_JPG      — JPEG 解码消息配置    struct msi *decode_s;       // S_JPG_DECODE      — JPEG 硬件解码    struct msi *csc_s;          // R_CSC_MSI         — 色彩空间转换硬件    struct msi *rgb_s;          // R_RGB_MSI         — RGB 输出节点    /* ===== 帧缓冲池 ===== */    struct fbpool tx_pool;      // photo_s 的帧缓冲池，用于数据传递    /* ===== 页面布局 ===== */    uint32_t cols;              // 缩略图网格列数    uint32_t rows;              // 缩略图网格行数    uint32_t img_per_page;      // 每页最大缩略图数 (= cols × rows, ≤ 12)    uint32_t img_loaded_cnt;    // 当前页实际成功加载的缩略图数    uint32_t cur_page;          // 当前页码 (1-based)    uint32_t total_files;       // SD 卡中总 JPEG 文件数    /* ===== UI 对象引用 ===== */    lv_obj_t   *title;          // 标题栏 &quot;Album&quot; 标签    lv_obj_t   *page_label;     // 页码标签 &quot;1/5&quot;    lv_obj_t   *img_cont;       // 缩略图容器（Flexbox 布局）    lv_obj_t   *imgs[12];       // 缩略图 lv_img 对象数组 (ALBUM_MAX_IMG=12)    /* ===== 图像数据 ===== */    lv_img_dsc_t *img_dsc[12];  // 每个缩略图的 LVGL 图像描述符指针    /* ===== 预览状态 ===== */    lv_obj_t   *preview_obj;    // 全屏预览界面对象（非 NULL 表示正在预览）    /* ===== 焦点导航（第二版新增） ===== */    int32_t     focus_idx;      // 当前聚焦的缩略图索引 (-1 = 无焦点)    lv_obj_t   *focus_img;      // 当前聚焦的 lv_img 对象指针    /* ===== 资源生命周期 ===== */    uint8_t     csc_need_destroy; // 退出时是否需要 msi_destroy 该 CSC&#125;;

3.2 常量定义#define CHECK_DIR   &quot;IMG&quot;       // SD 卡上的图片目录名#define EXT_NAME    &quot;*jpg&quot;      // 文件匹配模式#define IMG_W       160         // 缩略图宽度 (像素)#define IMG_H       120         // 缩略图高度 (像素)#define IMG_GAP     20          // 缩略图间距 (像素)#define ALBUM_MAX_IMG 12        // 每页最大缩略图数

3.3 内存分配宏// 大数据（如图像像素数据）→ PSRAM#define STREAM_MALLOC     av_psram_malloc#define STREAM_FREE       av_psram_free#define STREAM_ZALLOC     av_psram_zalloc// 小结构体 → SRAM#define STREAM_LIBC_MALLOC av_malloc#define STREAM_LIBC_FREE   av_free#define STREAM_LIBC_ZALLOC av_zalloc


设计理由：PSRAM 空间大但速度较慢，适合存放图像帧数据；SRAM 速度快但空间有限，适合存放管理结构体。


四、MSI 流水线架构4.1 流水线拓扑                        ┌──────────────────────────────────────────┐                        │             缩略图流水线                   │                        │  (album_load_page 时激活)                 │                        │                                          │SD卡 ──→ S_LVGL_PHOTO ──→ SR_OTHER_JPG ──→ S_JPG_DECODE ──→ R_CSC_MSI ──→ R_RGB_MSI ──→ LVGL(JPEG)    (photo_s)       (other_s)         (decode_s)       (csc_s)       (rgb_s)      显示                                                                                                          │                                          │                        │             预览流水线                   │                        │  (enter_photo_preview_ui 时激活)          │                        │                                          │SD卡 ──→ S_LVGL_PHOTO ──→ SR_OTHER_JPG ──→ S_JPG_DECODE ──→ R_VIDEO_P1 ──→ LCD 视频层(JPEG)    (photo_s)       (other_s)         (decode_s)       (硬件视频层)

4.2 各 MSI 节点详解photo_s — 图片源 (S_LVGL_PHOTO)


属性
值



创建方式
msi_new(S_LVGL_PHOTO, 0, NULL)


输出目标
SR_OTHER_JPG


回调函数
album_photo_msi_action


私有数据
指向 struct album_ui_s


帧缓冲池
ui_s-&gt;tx_pool


职责：从 SD 卡读取 JPEG 文件，包装为 struct framebuff 后喂入流水线。
album_photo_msi_action 回调处理：



命令
行为



MSI_CMD_PRE_DESTROY
空操作（预留）


MSI_CMD_POST_DESTROY
销毁帧缓冲池 fbpool_destroy


MSI_CMD_FREE_FB
释放 fb-&gt;data (PSRAM)，归还 fb 到池中


other_s — JPEG 解码消息配置 (SR_OTHER_JPG)


属性
值



创建方式
jpg_decode_msg_msi(SR_OTHER_JPG, out_w, out_h, step_w, step_h, filter)


输出目标
S_JPG_DECODE


职责：配置 JPEG 硬件解码参数。通过 MSI 命令动态调整：
// 设置解码输出分辨率为 160×120（缩略图模式）msi_do_cmd(other_s, MSI_CMD_DECODE_JPEG_MSG, MSI_JPEG_DECODE_OUT_SIZE,           160 &lt;&lt; 16 | 120);// 设置解码输出分辨率为 320×240（全屏预览模式）msi_do_cmd(other_s, MSI_CMD_DECODE_JPEG_MSG, MSI_JPEG_DECODE_OUT_SIZE,           320 &lt;&lt; 16 | 240);

decode_s — JPEG 硬件解码 (S_JPG_DECODE)


属性
值



创建方式
jpg_decode_msi(S_JPG_DECODE)


输出目标（缩略图）
R_CSC_MSI


输出目标（预览）
R_VIDEO_P1 (临时切换)


csc_s — 色彩空间转换 (R_CSC_MSI)


属性
值



创建方式
video_app_csc_msi_init() 或 msi_find() 复用已有


生命周期管理
ui_s-&gt;csc_need_destroy 标记是否由本模块创建


复用逻辑：
ui_s-&gt;csc_s = msi_find(R_CSC_MSI, 1);  // 查找是否已存在if (!ui_s-&gt;csc_s)&#123;    // 不存在 → 新建，标记退出时销毁    video_app_csc_msi_init(R_CSC_MSI, CSC_YUV420P, CSC_RGB565, IMG_W, IMG_H);    ui_s-&gt;csc_s = msi_find(R_CSC_MSI, 1);    ui_s-&gt;csc_need_destroy = 1;&#125;else&#123;    // 已存在（比如 VIDEO2 通路已经创建了）→ 复用，不销毁    ui_s-&gt;csc_need_destroy = 0;&#125;

rgb_s — RGB 输出节点 (R_RGB_MSI)


属性
值



创建方式
msi_new(R_RGB_MSI, 1, NULL)


输入源
CSC 的输出连接到本节点


职责：作为 CSC 转换后的 RGB 数据消费者，LVGL 从这里 msi_get_fb 获取 RGB565 帧数据显示缩略图。
4.3 流水线的创建与销毁创建 (album_msi_init → 在 enter_album_ui 中调用)：
album_msi_init(ui_s)  ├── msi_new(S_LVGL_PHOTO, 0, NULL)        → photo_s  │     ├── fbpool_init(&amp;tx_pool, img_per_page)  │     ├── photo_s-&gt;action = album_photo_msi_action  │     └── msi_add_output(photo_s → SR_OTHER_JPG)  │  ├── jpg_decode_msg_msi(SR_OTHER_JPG, ...)  → other_s  │     └── msi_add_output(other_s → S_JPG_DECODE)  │  ├── jpg_decode_msi(S_JPG_DECODE)           → decode_s  │     └── msi_add_output(decode_s → R_CSC_MSI)  │  ├── msi_find(R_CSC_MSI) → 检查是否存在    → csc_s  │     └── 不存在则 video_app_csc_msi_init()  │  └── msi_new(R_RGB_MSI, 1, NULL)            → rgb_s        └── msi_add_output(NULL → R_CSC_MSI → R_RGB_MSI)

销毁 (album_msi_destroy → 在 album_exit 中调用)：
album_msi_destroy(ui_s)  ├── msi_del_output(decode_s → R_CSC_MSI)    // 清理通路避免影响菜单  ├── msi_del_output(NULL → R_CSC_MSI → R_RGB_MSI)  ├── msi_destroy(photo_s)  ├── msi_destroy(other_s)  ├── msi_destroy(decode_s)  ├── msi_destroy(rgb_s)  ├── msi_put(csc_s)  └── if (csc_need_destroy) msi_destroy(csc_s)


五、核心函数逐分析5.1 album_ui() — 相册入口lv_obj_t *album_ui(lv_group_t *group, lv_obj_t *base_ui)

调用时机：主菜单初始化时调用，main_pocket_camera_ui() 中注册：
btn = album_ui(group, base_ui);

函数流程：
album_ui(group, base_ui)  ├── 参数检查 (group == NULL || base_ui == NULL → return NULL)  ├── STREAM_LIBC_ZALLOC(sizeof(struct album_ui_s)) → ui_s  (SRAM分配)  ├── ui_s-&gt;last_group = group      // 保存上级按键组  ├── ui_s-&gt;base_ui = base_ui       // 保存上级界面  ├── lv_list_add_btn(base_ui, NULL, &quot;album&quot;) → btn  │     └── 失败 → STREAM_LIBC_FREE(ui_s) → return NULL  ├── lv_group_add_obj(group, btn)  // 按钮加入按键组  ├── lv_obj_add_event_cb(btn, enter_album_ui, LV_EVENT_SHORT_CLICKED, ui_s)  │     // 点击按钮 → enter_album_ui  └── return btn

设计要点：

album_ui 只负责创建入口按钮和分配 ui_s 结构体
实际进入相册的耗时操作（MSI 初始化、UI 创建、文件扫描）都延迟到 enter_album_ui 进行
ui_s 一直存活到相册退出，album_exit 中通过 lv_obj_del(now_ui) 间接触发 LVGL 对象释放
注意：ui_s 结构体本身在退出时没有显式释放——这是潜在的内存泄漏，因为 album_exit 没有 STREAM_LIBC_FREE(ui_s)

5.2 enter_album_ui() — 进入相册static void enter_album_ui(lv_event_t *e)

触发条件：用户点击主菜单中的 “album” 按钮。
完整流程：
enter_album_ui(e)  │  ├── 1. 按键注册  │     set_lvgl_get_key_func(album_self_key)   // 接管按键事件  │     lv_obj_add_flag(base_ui, LV_OBJ_FLAG_HIDDEN)  // 隐藏菜单  │  ├── 2. 布局计算  │     album_calc_layout(ui_s)  │     │   ├── lv_disp_get_hor_res() → 获取屏幕宽度  │     │   ├── lv_disp_get_ver_res() → 获取屏幕高度 (-30px 标题栏)  │     │   ├── cols = (avail_w + 20) / (160 + 20)  // 计算列数  │     │   ├── rows = (avail_h + 20) / (120 + 20)  // 计算行数  │     │   ├── img_per_page = cols × rows (≤ 12)  │     │   └── os_printf 打印布局参数  │     │  │     ├── 以 800×480 屏幕为例：  │     │   cols = (800 + 20) / 180 = 4  │     │   rows = (450 + 20) / 140 = 3  │     │   img_per_page = 4 × 3 = 12  │     │  │  ├── 3. MSI 流水线初始化  │     album_msi_init(ui_s)  │     │   ├── 创建 photo_s (S_LVGL_PHOTO)  │     │   ├── 创建 other_s (SR_OTHER_JPG, 参数: 160×120)  │     │   ├── 创建/复用 csc_s (R_CSC_MSI, YUV420P→RGB565)  │     │   ├── 创建 decode_s (S_JPG_DECODE)  │     │   └── 创建 rgb_s (R_RGB_MSI)  │     │  │  ├── 4. 创建 UI 界面  │     │  │     ├── 主界面: lv_obj_create(lv_scr_act())  │     │     ├── 白色背景, 无边框, 全屏  │     │     ├── 关闭可滚动, 关闭手势冒泡  │     │     └── ui_s-&gt;now_ui = ui  │     │  │     ├── 顶部标题栏: lv_obj_create(ui)  │     │     ├── 高度 30px, 白色背景  │     │     ├── 左: &quot;Album&quot; 标签 (lv_label, font_28)  │     │     ├── 中: 页码标签 ui_s-&gt;page_label  │     │     ├── 右: &quot;&gt;&quot; 下一页按钮 (lv_btn 40×28)  │     │     └── 右二: &quot;&lt;&quot; 上一页按钮  │     │  │     ├── 分割线: lv_obj, 高2px, 黑色  │     │  │     ├── 图片容器: lv_obj_create(ui)  │     │     ├── 宽 = cols×160 + (cols-1)×20 + 20  │     │     ├── 高 = rows×120 + (rows-1)×20 + 20  │     │     ├── LV_ALIGN_TOP_MID, y偏移38 (标题栏下方)  │     │     ├── Flexbox: LV_FLEX_FLOW_ROW_WRAP (自动换行)  │     │     ├── pad_all = 10, pad_gap = 20  │     │     └── ui_s-&gt;img_cont  │     │  │     ├── 按键组: lv_group_create()  │     │     ├── lv_indev_set_group(indev_keypad, group)  │     │     ├── lv_group_add_obj(group, ui)  │     │     └── ui_s-&gt;now_group = group  │     │  │     └── 事件注册:  │           ├── ui → LV_EVENT_KEY → album_key_handler  │           └── now_ui → LV_EVENT_GESTURE → album_gesture_handler  │  └── 5. 加载第 1 页        ui_s-&gt;cur_page = 1        album_load_page(ui_s)

5.3 album_load_page() — 加载当前页面static void album_load_page(struct album_ui_s *ui_s)

完整流程：
album_load_page(ui_s)  │  ├── 1. 开启 RGB 节点  │     if (ui_s-&gt;rgb_s) ui_s-&gt;rgb_s-&gt;enable = 1  │  ├── 2. 清除焦点状态  │     ui_s-&gt;focus_idx = -1  │     ui_s-&gt;focus_img = NULL  │  ├── 3. 清理旧页面  │     ├── lv_obj_clean(ui_s-&gt;img_cont)  // LVGL 删除所有 img 对象  │     │     └── 通过 LV_EVENT_DELETE 回调自动释放文件名副本  │     │  │     └── 手动释放 img_dsc 数据  │           for i = 0 to img_loaded_cnt:  │               if img_dsc[i]:  │                   if img_dsc[i]-&gt;data: STREAM_FREE(data)   // PSRAM  │                   STREAM_LIBC_FREE(img_dsc[i])              // SRAM  │                   img_dsc[i] = NULL  │               imgs[i] = NULL  │           img_loaded_cnt = 0  │  ├── 4. 计算文件范围  │     start = (cur_page - 1) × img_per_page  │     end   = start + img_per_page  │  ├── 5. 单遍扫描 SD 卡  │     f_findfirst(&amp;dir, &amp;finfo, &quot;IMG&quot;, &quot;*jpg&quot;)  │     while (fr == FR_OK &amp;&amp; finfo.fname[0] != 0):  │     │   if file_idx ≥ start &amp;&amp; file_idx &lt; end &amp;&amp; loaded &lt; per_page:  │     │   │   if album_show(finfo.fname, ui_s) == 0:  │     │   │       img_loaded_cnt++  │     │   file_idx++  │     │   f_findnext(&amp;dir, &amp;finfo)  │     f_closedir(&amp;dir)  │     total_files = file_idx  │  │     ★ 设计要点: 单遍扫描，同时计数文件和加载当前页图片  │       避免了先扫描一遍计数再扫描一遍加载的两遍扫描开销。  │  ├── 6. 更新页码显示  │     total_pages = (file_idx + per_page - 1) / per_page  │     sprintf(buf, &quot;%d/%d&quot;, cur_page, total_pages)  │     lv_label_set_text(page_label, buf)  │  ├── 7. 关闭 RGB 节点  │     if (ui_s-&gt;rgb_s) ui_s-&gt;rgb_s-&gt;enable = 0  │  ├── 8. 默认聚焦第一个缩略图  │     if (img_loaded_cnt &gt; 0):  │         album_apply_focus(ui_s, 0)  │  └── 9. 日志输出        os_printf(&quot;album page %d/%d, total files %d, loaded %d\r\n&quot;, ...)

5.4 album_show() — 显示单个缩略图static int album_show(const char *filename, struct album_ui_s *ui_s)

返回值：0 成功，-1 失败（调用者根据返回值决定是否递增 img_loaded_cnt）。
流程：
album_show(filename, ui_s)  ├── idx = ui_s-&gt;img_loaded_cnt  │  ├── lv_img_create(img_cont) → img  │     ├── lv_obj_set_size(img, 160, 120)  │     ├── 边框: 灰色 #CCCCCC, 1px  │     ├── LV_OBJ_FLAG_CLICKABLE     // 可点击  │     └── 清除 LV_OBJ_FLAG_SCROLLABLE  │  ├── 文件名拷贝 → user_data  │     name_copy = STREAM_LIBC_MALLOC(strlen+1)  │     strcpy(name_copy, filename)  │     lv_obj_set_user_data(img, name_copy)  │     lv_obj_add_event_cb(img, album_img_cleanup, LV_EVENT_DELETE, NULL)  │     // ★ LV_EVENT_DELETE → img 被删除时自动 free user_data  │  ├── 注册点击事件  │     lv_obj_add_event_cb(img, enter_photo_preview_ui,  │                         LV_EVENT_SHORT_CLICKED, ui_s)  │  ├── 创建缩略图  │     dsc = album_thumbnail_create(filename, ui_s, img)  │  ├── 成功分支  │     if dsc != NULL:  │     │   ui_s-&gt;img_dsc[idx] = dsc  │     │   ui_s-&gt;imgs[idx] = img  │     │   return 0  │  └── 失败分支        if dsc == NULL:            lv_obj_del(img)    // 删除 LVGL 对象            return -1

5.5 album_thumbnail_create() — 创建缩略图static lv_img_dsc_t *album_thumbnail_create(const char *filename,                                            struct album_ui_s *ui_s,                                            lv_obj_t *img)

核心流程 — JPEG 解码 + CSC 转换 → LVGL 图像描述符：
album_thumbnail_create(filename, ui_s, img)  │  ├── 1. 加载 JPEG 文件  │     album_jpeg_file_load(filename, ui_s, FRAMEBUFF_SOURCE_JPG)  │     │   ├── osal_fopen(&quot;0:IMG/xxx.jpg&quot;, &quot;rb&quot;)  │     │   ├── STREAM_MALLOC(filesize) → photo_buf  │     │   ├── osal_fread(photo_buf, filesize)  │     │   ├── fbpool_get → data_s (从 tx_pool 取一个 fb)  │     │   ├── data_s-&gt;data = photo_buf  │     │   ├── data_s-&gt;mtype = F_JPG  │     │   ├── data_s-&gt;srcID = FRAMEBUFF_SOURCE_JPG  ★ 关键标记  │     │   ├── msi_output_fb(photo_s, data_s)  │     │   │     └── JPEG 数据进入流水线:  │     │   │         photo_s → other_s → decode_s → csc_s → rgb_s  │     │   └── osal_fclose(fp)  │     │  │     ├── 2. 获取 RGB 输出  │     │     msi_get_fb(rgb_s, 100) → rgb_fb  (超时 100 ticks)  │     │     │  │     │     └── if rgb_fb != NULL &amp;&amp; rgb_fb-&gt;data != NULL:  │     │           │  │     │           ├── 3. 创建 LVGL 图像描述符  │     │           │     STREAM_LIBC_MALLOC(sizeof(lv_img_dsc_t)) → dsc  │     │           │     dsc-&gt;header = &#123; w:160, h:120, cf:TRUE_COLOR &#125;  │     │           │     dsc-&gt;data_size = 160 × 120 × 2 = 38400  │     │           │     │  │     │           │     ├── STREAM_MALLOC(38400) → dsc-&gt;data  (PSRAM)  │     │           │     │  │     │           │     ├── sys_dcache_invalid_range(rgb_fb-&gt;data, 38400)  │     │           │     │     // DMA 写入的 buffer 需要刷新 cache  │     │           │     │  │     │           │     ├── memcpy(dsc-&gt;data, rgb_fb-&gt;data, 38400)  │     │           │     │     // ★ 深拷贝！避免后续帧冲掉数据  │     │           │     │  │     │           │     └── lv_img_set_src(img, dsc)  │     │           │  │     │           └── msi_delete_fb(rgb_s, rgb_fb)  // 归还 fb  │     │  │     └── return dsc (or NULL on failure)

5.6 album_jpeg_file_load() — JPEG 文件加载static int album_jpeg_file_load(const char *filename, struct album_ui_s *ui_s,                                 uint8_t srcID)

参数：

filename: 文件名（不含路径）
ui_s: 相册状态
srcID: 帧来源标记 (FRAMEBUFF_SOURCE_JPG)

返回值：0 成功，-1 失败。
详细流程（含错误处理）：
album_jpeg_file_load(filename, ui_s, srcID)  │  ├── 构建路径 &quot;0:IMG/xxx.jpg&quot;  │  ├── photo_s == NULL → return -1  │  ├── fbpool_get(&amp;tx_pool, 0, photo_s) → data_s  │     └── 失败 → return -1  │  ├── data_s-&gt;data = NULL  │  ├── osal_fopen(path, &quot;rb&quot;) → fp  │     └── 失败 → msi_delete_fb + return -1  │  ├── osal_fsize(fp) → filesize  │  ├── STREAM_MALLOC(filesize) → photo_buf  │     └── 失败 → msi_delete_fb + fclose + return -1  │  ├── osal_fread(photo_buf, 1, filesize, fp)  │     └── 长度不符 → STREAM_FREE + msi_delete_fb + fclose + return -1  │  ├── 填充 framebuff  │     data_s-&gt;data  = photo_buf     // JPEG 文件数据  │     data_s-&gt;mtype = F_JPG         // 标记为 JPEG 类型  │     data_s-&gt;stype = FSTYPE_YUV_P1 // YUV 平面1格式  │     data_s-&gt;len   = filesize      // 数据长度  │     data_s-&gt;srcID = srcID         // ★ srcID 传递给 CSC 判断逻辑  │  ├── msi_output_fb(photo_s, data_s)  // 喂入流水线！  │  ├── osal_fclose(fp)  └── return 0

错误处理覆盖：本函数有 4 个失败路径，每种都正确释放已分配资源。

六、UI 界面布局6.1 界面结构树lv_scr_act()  (活动屏幕)  │  └── now_ui (lv_obj, 白色全屏背景)        │        ├── top_bar (lv_obj, 高度30px, 标题栏)        │     ├── title (lv_label, &quot;Album&quot;, font_28, 黑色)        │     ├── page_label (lv_label, &quot;1/3&quot;, font_28, 黑色)        │     ├── prev_btn (lv_btn, 40×28, &quot;&lt;&quot; 符号, 上一页)        │     └── next_btn (lv_btn, 40×28, &quot;&gt;&quot; 符号, 下一页)        │        ├── line (lv_obj, 高2px, 黑色分割线, y=32)        │        └── img_cont (lv_obj, 图片容器, Flexbox 布局)              ├── img[0] (lv_img, 160×120)              ├── img[1] (lv_img, 160×120)              ├── ...              └── img[N-1] (lv_img, 160×120)

6.2 网格布局计算公式：
// 可用宽度 = 屏幕宽度// 可用高度 = 屏幕高度 - 30 (标题栏高度)cols = (avail_w + IMG_GAP) / (IMG_W + IMG_GAP);rows = (avail_h + IMG_GAP) / (IMG_H + IMG_GAP);img_per_page = min(cols × rows, ALBUM_MAX_IMG);

示例计算结果：



屏幕分辨率
cols
rows
img_per_page



800×480
4
3
12


480×272
2
1
2


640×480
3
3
9


容器尺寸：
cont_w = cols × 160 + (cols - 1) × 20 + 20;cont_h = rows × 120 + (rows - 1) × 20 + 20;

例如 4×3 网格：cont_w = 4×160 + 3×20 + 20 = 720, cont_h = 3×120 + 2×20 + 20 = 420
6.3 缩略图外观


状态
边框颜色
边框宽度



默认（无焦点）
#CCCCCC (浅灰)
1px


聚焦选中
#FF0000 (红色)
3px



七、按键系统与焦点导航7.1 按键映射相册模式 (album_self_key)


硬件按键
事件类型
映射值
操作



AD_UP
KEY_EVENT_SUP (短按)
&#39;u&#39;
焦点上移


AD_DOWN
KEY_EVENT_SUP (短按)
&#39;d&#39;
焦点下移


AD_LEFT
KEY_EVENT_SUP (短按)
&#39;l&#39;
焦点左移


AD_RIGHT
KEY_EVENT_SUP (短按)
&#39;r&#39;
焦点右移


AD_PRESS
KEY_EVENT_SUP (短按)
&#39;e&#39;
确认 &#x2F; 进入预览


AD_PRESS
KEY_EVENT_LDOWN (长按)
&#39;q&#39;
退出相册



按键映射设计说明：

第一版中 AD_LEFT&#x3D;’q’(退出), AD_UP&#x3D;’n’(下一页), AD_DOWN&#x3D;’p’(上一页)
第二版重映射为方向键导航，长按 AD_PRESS 替代退出
按键码通过 (val &amp; 0xff) 获取事件类型，(val &gt;&gt; 8) 获取按键值


预览模式 (preview_self_key)


硬件按键
事件类型
映射值
操作



AD_PRESS
KEY_EVENT_SUP (短按)
0x1B (ESC)
退出预览


7.2 焦点导航算法焦点高亮 (album_apply_focus)album_apply_focus(ui_s, idx)  ├── 有效性检查 (idx &lt; 0 || idx &gt;= loaded_cnt → return)  │  ├── 清除旧焦点高亮  │     if focus_img != NULL:  │         border_color = #CCCCCC  (灰色)  │         border_width = 1  │  └── 设置新焦点高亮        img = imgs[idx]        if img != NULL:            border_color = #FF0000  (红色)            border_width = 3            focus_img = img            focus_idx = idx

四方向移动 + 自动翻页上移 (album_focus_up)：
new_idx = focus_idx - colsif new_idx &gt;= 0:    album_apply_focus(ui_s, new_idx)    // 正常上移else:    if cur_page &gt; 1:        cur_page--        album_load_page(ui_s)           // 已到第一行 → 上翻一页

下移 (album_focus_down)：
new_idx = focus_idx + colsif new_idx &lt; loaded_cnt:    album_apply_focus(ui_s, new_idx)    // 正常下移else:    total_pages = ceil(total_files / per_page)    if cur_page &lt; total_pages:        cur_page++        album_load_page(ui_s)           // 已到最后一行 → 下翻一页

左移 (album_focus_left)：
new_idx = focus_idx - 1if new_idx &gt;= 0:    album_apply_focus(ui_s, new_idx)    // 正常左移else:    if cur_page &gt; 1:        cur_page--        album_load_page(ui_s)           // 已在第一个 → 上翻一页

右移 (album_focus_right)：
new_idx = focus_idx + 1if new_idx &lt; loaded_cnt:    album_apply_focus(ui_s, new_idx)    // 正常右移else:    total_pages = ceil(total_files / per_page)    if cur_page &lt; total_pages:        cur_page++        album_load_page(ui_s)           // 已在最后一个 → 下翻一页

7.3 按键事件处理 (album_key_handler)album_key_handler(e)  ├── c = *(int32_t *)lv_event_get_param(e)  // 获取按键码  │  ├── switch(c):  │     case &#x27;q&#x27;: album_exit(ui_s)           // 退出相册  │     case &#x27;u&#x27;: album_focus_up(ui_s)       // 焦点上移  │     case &#x27;d&#x27;: album_focus_down(ui_s)     // 焦点下移  │     case &#x27;l&#x27;: album_focus_left(ui_s)     // 焦点左移  │     case &#x27;r&#x27;: album_focus_right(ui_s)    // 焦点右移  │     case &#x27;e&#x27;:                             // 确认  │         if (ui_s-&gt;focus_img):  │             lv_event_send(focus_img, LV_EVENT_SHORT_CLICKED, NULL)  │             // ★ 通过发送 LV_EVENT_SHORT_CLICKED 复用触摸进入预览的逻辑  │  └── default: break

7.4 手势翻页album_gesture_handler(e)  ├── lv_indev_get_act() → indev  ├── lv_indev_get_gesture_dir(indev) → dir  │  ├── switch(dir):  │     case LV_DIR_LEFT:   cur_page++; album_load_page()  // 左滑→下一页  │     case LV_DIR_RIGHT:  cur_page--; album_load_page()  // 右滑→上一页  │     default: break  │  └── lv_indev_wait_release(indev)  // 等待手势释放


设计要点：now_ui 设置了 LV_OBJ_FLAG_GESTURE_BUBBLE 清除，阻止手势冒泡。img_cont 设置了 LV_OBJ_FLAG_GESTURE_BUBBLE 让手势可以冒泡到 now_ui 处理。top_bar 也设置了 LV_OBJ_FLAG_GESTURE_BUBBLE。


八、全屏预览机制8.1 进入预览 (enter_photo_preview_ui)触发方式:  1. 触摸点击缩略图 → LV_EVENT_SHORT_CLICKED  2. 按键确认(&#x27;e&#x27;) → lv_event_send(focus_img, LV_EVENT_SHORT_CLICKED)enter_photo_preview_ui(e)  │  ├── 0. 同步焦点（触摸时同步更新焦点高亮）  │     for i = 0 to loaded_cnt:  │         if imgs[i] == img:  │             album_apply_focus(ui_s, i)  │             break  │  ├── 1. 获取文件名  │     filename = (char *)lv_obj_get_user_data(img)  │     name_copy = STREAM_LIBC_MALLOC(strlen+1)  // 拷贝文件名  │     strcpy(name_copy, filename)  │  ├── 2. 按键接管  │     set_lvgl_get_key_func(preview_self_key)  // 预览按键映射  │  ├── 3. 创建预览界面  │     preview = lv_obj_create(lv_scr_act())  │     ├── 全屏 (100% × 100%)  │     ├── 无边框, 无滚动条  │     ├── LV_EVENT_CLICKED → exit_photo_preview_ui  (触摸退出)  │     ├── LV_EVENT_KEY → exit_photo_preview_ui       (按键退出)  │     ├── lv_group_add_obj(now_group, preview)       // 加入按键组  │     ├── lv_group_focus_obj(preview)                 // 聚焦预览对象  │     └── ui_s-&gt;preview_obj = preview  │  ├── 4. MSI 通路切换  │     │  │     ├── msi_add_output(decode_s → R_VIDEO_P1)    // 解码输出→视频层  │     ├── msi_del_output(decode_s → R_CSC_MSI)      // 断开→CSC通路  │     │     // ★ 避免预览帧通过 CSC 干扰 P2（菜单）显示  │     │  │     └── msi_cmd(R_VIDEO_P1, MSI_CMD_LCD_VIDEO, MSI_VIDEO_ENABLE, 1)  │           // 开启 VIDEO_P1 硬件视频层显示  │  ├── 5. 设置预览分辨率 (320×240)  │     msi_do_cmd(other_s, MSI_CMD_DECODE_JPEG_MSG,  │                MSI_JPEG_DECODE_OUT_SIZE, 320 &lt;&lt; 16 | 240)  │  ├── 6. 执行预览  │     album_preview_show(name_copy, ui_s)  │     │   └── album_jpeg_file_load(filename, ui_s, FRAMEBUFF_SOURCE_JPG)  │     │         └── JPEG → decode_s → R_VIDEO_P1 → LCD 显示  │  └── 7. 释放文件名副本        STREAM_LIBC_FREE(name_copy)

8.2 退出预览 (exit_photo_preview_ui)触发方式:  1. 触摸任意位置 → LV_EVENT_CLICKED  2. 按键 AD_PRESS(短按) → 0x1B(ESC) → LV_EVENT_KEYexit_photo_preview_ui(e)  │  ├── 1. 恢复相册按键  │     set_lvgl_get_key_func(album_self_key)  │  ├── 2. 删除预览界面  │     lv_obj_del(preview_obj)  │     preview_obj = NULL  │  ├── 3. 刷新 LVGL 显示  │     lv_refr_now(NULL)  │  ├── 4. 关闭 VIDEO_P1  │     msi_cmd(R_VIDEO_P1, MSI_CMD_LCD_VIDEO, MSI_VIDEO_ENABLE, 0)  │  ├── 5. 恢复 decode→CSC 通路（恢复菜单显示）  │     msi_add_output(decode_s, NULL, R_CSC_MSI)  │  ├── 6. 将输出分辨率调回缩略图参数  │     msi_do_cmd(other_s, MSI_CMD_DECODE_JPEG_MSG,  │                MSI_JPEG_DECODE_OUT_SIZE, 160 &lt;&lt; 16 | 120)  │  └── 7. 删除 decode→VIDEO_P1 通路        msi_del_output(decode_s, NULL, R_VIDEO_P1)

8.3 预览模式 MSI 通路切换图示┌───────── 缩略图模式 ─────────┐│                              ││  decode_s ──→ R_CSC_MSI ──→ rgb_s ──→ LVGL显示  ││       (连接)                 ││                              ││  decode_s     R_VIDEO_P1     ││       (断开)                 │└──────────────────────────────┘┌───────── 预览模式 ───────────┐│                              ││  decode_s     R_CSC_MSI     ││       (断开)                 ││                              ││  decode_s ──→ R_VIDEO_P1 ──→ LCD显示  ││       (连接)                 │└──────────────────────────────┘


关键设计：解码后的 JPEG 帧在缩略图模式下经过 CSC 转换为 RGB565 供 LVGL 显示，在预览模式下直接输出到 VIDEO_P1 硬件视频层（不经 CSC），实现全屏显示。


九、CSC 动态格式配置9.1 改造背景原 video_app_csc_msi.c 固定将输入 RGB565 转换为输出 YUV420P，用于 VIDEO2 通路（LVGL 菜单→CSC→YUV→LCD P2 层）。相册功能需要反向转换（JPEG 解码的 YUV420P → RGB565），因此需要对 CSC 驱动进行泛化改造。
9.2 新增辅助函数csc_is_rgb_format() — 判断是否为 RGB 格式static uint8_t csc_is_rgb_format(uint32_t fmt)

支持的 RGB 格式：CSC_BGR565, CSC_RGB565, CSC_BGR888, CSC_RGB888, CSC_RGB888P。
csc_get_convert_type() — 自动推导转换类型static uint8_t csc_get_convert_type(uint32_t input_fmt, uint32_t output_fmt)




输入
输出
返回值
含义



YUV
RGB
0
YUV→RGB


RGB
RGB 或 YUV→YUV
1
同色域


RGB
YUV
2
RGB→YUV


csc_get_plane_offsets() — 计算平面偏移static void csc_get_plane_offsets(uint32_t fmt, uint32_t w, uint32_t h,                                   uint32_t *off1, uint32_t *off2)

不同的颜色格式有不同的内存布局：



格式
平面1 (Y&#x2F;luma)
平面2 (U&#x2F;Cb)
平面3 (V&#x2F;Cr)



RGB565 &#x2F; YUYV422 &#x2F; YUV444 (packed)
基址 + 0
0
0


YUV420P
基址 + 0
+ w×h
+ w×h + w×h&#x2F;4


YUV422P
基址 + 0
+ w×h
+ w×h + w×h&#x2F;2


YUV422SP
基址 + 0
+ w×h
0 (interleaved UV)


RGB888P &#x2F; YUV444P
基址 + 0
+ w×h
+ w×h×2


csc_set_input_addr_by_fmt() &#x2F; csc_set_output_addr_by_fmt()根据格式自动计算三个平面地址并调用 csc_set_input_addr(dev, y_addr, u_addr, v_addr)。
csc_output_buf_size() — 计算缓冲区大小static uint32_t csc_output_buf_size(uint32_t w, uint32_t h, uint32_t fmt)




格式类
计算公式



16-bit packed (RGB565, YUYV422 等)
w × h × 2


YUV422 planar&#x2F;semi-planar
w × h × 2


YUV420P
w × h × 3 &#x2F; 2


24-bit packed (RGB888, YUV444 等)
w × h × 3


24-bit planar (RGB888P, YUV444P 等)
w × h × 3


9.3 CSC 工作线程动态分支video_app_csc_msi_work() 中根据 fb-&gt;srcID 分支：
switch (csc_priv-&gt;current_rx_fb-&gt;srcID)&#123;    case FRAMEBUFF_SOURCE_JPG:        // 相册缩略图路径        in_fmt  = CSC_YUV420P;     // JPEG 解码输出 YUV420P        out_fmt = CSC_RGB565;      // LVGL 需要 RGB565        csc_w   = 160;             // 缩略图宽        csc_h   = 120;             // 缩略图高        // 输出 fb 类型: F_RGB / LVGL_RGB        break;    default:        // VIDEO2 默认路径（菜单显示）        in_fmt  = CSC_RGB565;      // LVGL 输出 RGB565        out_fmt = CSC_YUV420P;     // LCD YUV 层需要 YUV420P        csc_w   = csc_priv-&gt;width;  // 全屏宽        csc_h   = csc_priv-&gt;height; // 全屏高        // 输出 fb 类型: F_YUV / FSTYPE_YUV_P0        break;&#125;

9.4 CSC 初始化改进改造前（固定 YUV420P 输出缓冲区大小）：
uint8_t *csc_output_addr = STREAM_MALLOC(width * height * 3 / 2);FBPOOL_SET_INFO(&amp;tx_pool, i, csc_output_addr, width * height * 3 / 2, yuv_msg);

改造后（根据输出格式计算）：
uint32_t out_buf_size = csc_output_buf_size(width, height, output_format);uint8_t *csc_output_addr = STREAM_MALLOC(out_buf_size);FBPOOL_SET_INFO(&amp;tx_pool, i, csc_output_addr, out_buf_size, yuv_msg);

9.5 MSI_CMD_TRANS_FB 增加对 JPG 源的支持case MSI_CMD_TRANS_FB:&#123;    struct framebuff *fb = (struct framebuff *)param1;    switch (fb-&gt;srcID)    &#123;    case FRAMEBUFF_SOURCE_CSC:    case FRAMEBUFF_SOURCE_JPG:  // ★ 新增：允许 JPG 源通过 CSC        ret = RET_OK;        break;    default:        ret = RET_OK + 1;       // 拒绝其他来源        break;    &#125;&#125;


十、JPEG 解码器扩展10.1 新增 MSI 命令在 jpg_decode_msg_msi.c 中新增两个 MSI 命令处理：
// 配置输出分辨率case MSI_JPEG_DECODE_OUT_SIZE:    decode_msg-&gt;out_w = arg &gt;&gt; 16;     // 高16位 = 宽度    decode_msg-&gt;out_h = arg &amp; 0xffff;   // 低16位 = 高度    break;// 配置步进（解码时的步进尺寸）case MSI_JPEG_DECODE_STEP:    decode_msg-&gt;step_w = arg &gt;&gt; 16;    // 高16位 = 步进宽度    decode_msg-&gt;step_h = arg &amp; 0xffff;  // 低16位 = 步进高度    break;

10.2 stream_define.h 新增定义// 新增 MSI 资源名称#define R_RGB_MSI  &quot;rgb_msi&quot;// 新增 JPEG 解码 MSI 命令枚举enum MSI_JPEG_DECODE_MSG&#123;    MSI_JPEG_DECODE_X_Y,    MSI_JPEG_DECODE_FORCE_TYPE,    MSI_JPEG_DECODE_MAGIC,    MSI_JPEG_DECODE_OUT_SIZE,   // ★ 新增    MSI_JPEG_DECODE_STEP,       // ★ 新增&#125;;

10.3 使用方式// 设置缩略图解码尺寸 (160×120)msi_do_cmd(other_s, MSI_CMD_DECODE_JPEG_MSG,           MSI_JPEG_DECODE_OUT_SIZE, 160 &lt;&lt; 16 | 120);// 设置预览解码尺寸 (320×240)msi_do_cmd(other_s, MSI_CMD_DECODE_JPEG_MSG,           MSI_JPEG_DECODE_OUT_SIZE, 320 &lt;&lt; 16 | 240);


参数编码：宽高编码在单个 uint32_t 中，高16位&#x3D;宽度，低16位&#x3D;高度。


十一、内存管理策略11.1 内存分区PSRAM (大容量, 稍慢)          SRAM (小容量, 快速)────────────────────────      ────────────────────────JPEG 文件数据 (photo_buf)     lv_img_dsc_t 结构体RGB565 像素数据 (dsc-&gt;data)   文件名副本 (name_copy)CSC 输出缓冲池                 ui_s 结构体                              yuv_arg_s 结构体

11.2 各资源生命周期


资源
分配函数
分配位置
释放时机
释放函数



ui_s 结构体
STREAM_LIBC_ZALLOC
album_ui()
退出相册 (album_exit)
（遗漏）


JPEG 文件 buffer
STREAM_MALLOC
album_jpeg_file_load()
MSI FreeFB 回调
STREAM_FREE


LVGL 图像描述符
STREAM_LIBC_MALLOC
album_thumbnail_create()
翻页&#x2F;退出
STREAM_LIBC_FREE


描述符内 RGB 数据
STREAM_MALLOC
album_thumbnail_create()
翻页&#x2F;退出
STREAM_FREE


文件名副本 (user_data)
STREAM_LIBC_MALLOC
album_show()
LV_EVENT_DELETE → album_img_cleanup
STREAM_LIBC_FREE


预览文件名副本
STREAM_LIBC_MALLOC
enter_photo_preview_ui()
函数末尾立即释放
STREAM_LIBC_FREE


CSC tx_pool buffer
STREAM_MALLOC
video_app_csc_msi_init()
MSI POST_DESTROY
STREAM_FREE


CSC yuv_arg_s
STREAM_LIBC_ZALLOC
video_app_csc_msi_init()
MSI POST_DESTROY
STREAM_LIBC_FREE


CSC priv 结构体
STREAM_LIBC_ZALLOC
video_app_csc_msi_init()
MSI POST_DESTROY
STREAM_LIBC_FREE


11.3 关键内存管理设计1. 深拷贝 RGB 数据
// album_thumbnail_create 中dsc-&gt;data = STREAM_MALLOC(IMG_W * IMG_H * 2);memcpy((void *)dsc-&gt;data, rgb_fb-&gt;data, IMG_W * IMG_H * 2);


不从 CSC 输出 buffer 直接引用数据，而是拷贝一份
原因：CSC 输出 buffer 会被后续帧覆盖，深拷贝保证缩略图数据持久有效

2. LV_EVENT_DELETE 自动释放
// album_show 中注册lv_obj_add_event_cb(img, album_img_cleanup, LV_EVENT_DELETE, NULL);// album_img_cleanup 回调static void album_img_cleanup(lv_event_t *e)&#123;    lv_obj_t *img = lv_event_get_target(e);    void *user_data = lv_obj_get_user_data(img);    if (user_data)    &#123;        lv_obj_set_user_data(img, NULL);        STREAM_LIBC_FREE(user_data);  // 释放文件名副本    &#125;&#125;


翻页时 lv_obj_clean(img_cont) 批量删除 img 对象
LVGL 在删除对象时会发送 LV_EVENT_DELETE，触发自动释放
防止翻页时文件名泄漏

3. photo_s MSI_CMD_FREE_FB 回调
case MSI_CMD_FREE_FB:&#123;    struct framebuff *fb = (struct framebuff *)param1;    if (fb-&gt;data)    &#123;        STREAM_FREE(fb-&gt;data);   // 释放 JPEG 文件数据 (PSRAM)        fb-&gt;data = NULL;    &#125;    fbpool_put(&amp;ui_s-&gt;tx_pool, fb);  // 归还 fb 到池中复用    ret = RET_OK + 1;  // 阻止框架层再次释放&#125;


RET_OK + 1 阻止 MSI 框架层重复释放 fb-&gt;data
fb 归还到 tx_pool 供下次使用

4. 翻页时内存清理顺序
lv_obj_clean(img_cont)       // 第1步：删除 LVGL 对象（触发 DELETE 回调释放文件名）    ↓手动释放 img_dsc[]           // 第2步：释放图像描述符和数据    ↓重置计数器                   // 第3步：img_loaded_cnt = 0


重要：必须先删除 LVGL 对象再释放 dsc，因为 LVGL 对象还在引用 dsc。如果先释放 dsc 再删除对象，会导致 LVGL 访问已释放的内存。

5. 退出相册时的完整清理
album_exit() 中：
// 1. 还原 UI 状态lv_indev_set_group(indev_keypad, last_group);lv_group_del(now_group);lv_obj_del(now_ui);// 2. 销毁 MSI 流水线 (包括 CSC 输出 buffer)album_msi_destroy(ui_s);// 3. 释放缩略图数据for (i = 0; i &lt; img_loaded_cnt; i++)&#123;    if (img_dsc[i])    &#123;        if (img_dsc[i]-&gt;data) STREAM_FREE(data);        STREAM_LIBC_FREE(img_dsc[i]);    &#125;&#125;

11.4 已知问题
ui_s 结构体泄漏：album_ui() 中分配的 struct album_ui_s 在 album_exit() 中没有释放。虽然整个结构体很小（SRAM 中几十字节），但理论上存在内存泄漏。修复方法：在 album_exit() 末尾增加 STREAM_LIBC_FREE(ui_s)。


十二、数据流完整跟踪12.1 缩略图加载数据流以加载 IMG/photo001.jpg 为例：
Step 1: album_load_page()  └── album_show(&quot;photo001.jpg&quot;, ui_s)        └── album_thumbnail_create(&quot;photo001.jpg&quot;, ui_s, img)Step 2: album_jpeg_file_load(&quot;photo001.jpg&quot;, ui_s, FRAMEBUFF_SOURCE_JPG)  ├── 打开文件: osal_fopen(&quot;0:IMG/photo001.jpg&quot;, &quot;rb&quot;)  ├── 获取大小: osal_fsize() → 例如 25600 bytes  ├── 分配内存: STREAM_MALLOC(25600) → photo_buf (PSRAM地址 0x6xxxxxxx)  ├── 读取文件: osal_fread(photo_buf, 25600)  ├── 获取 fb:  fbpool_get(&amp;tx_pool, 0, photo_s) → data_s  ├── 填充属性:  │     data_s-&gt;data  = photo_buf       (0x6xxxxxxx)  │     data_s-&gt;mtype = F_JPG  │     data_s-&gt;stype = FSTYPE_YUV_P1  │     data_s-&gt;len   = 25600  │     data_s-&gt;srcID = FRAMEBUFF_SOURCE_JPG  │  └── 喂入流水线: msi_output_fb(photo_s, data_s)Step 3: MSI 流水线处理 (异步)  photo_s → SR_OTHER_JPG → S_JPG_DECODE → R_CSC_MSI → R_RGB_MSI  │         │              │              │              │  │         │              │              │              └─ rgb_s 收到 RGB565 帧  │         │              │              │                  (160×120×2 = 38400 bytes)  │         │              │              │  │         │              │              └─ CSC 硬件: YUV420P → RGB565  │         │              │                  输入: JPEG 解码的 YUV420P (160×120×1.5)  │         │              │                  输出: RGB565 (160×120×2)  │         │              │  │         │              └─ jpg_decode_msi: 硬件解码 JPEG → YUV420P  │         │                  输入: JPEG 文件数据 (25600 bytes)  │         │                  输出: YUV420P 图像 (160×120×1.5 = 28800 bytes)  │         │  │         └─ jpg_decode_msg_msi: 配置解码参数  │              输出宽高: 160×120  │              步进宽高: 160×120  │              过滤器: FSTYPE_YUV_P1  │  └─ photo_s: 数据已送出, 等待 FreeFB 回调释放 photo_bufStep 4: msi_get_fb(rgb_s, 100) → rgb_fb (等待 RGB 输出)  ├── rgb_fb != NULL &amp;&amp; rgb_fb-&gt;data != NULL  │  ├── STREAM_LIBC_MALLOC(sizeof(lv_img_dsc_t)) → dsc (SRAM 0x1xxxxxxx)  ├── dsc-&gt;header = &#123; w:160, h:120, cf:LV_IMG_CF_TRUE_COLOR &#125;  ├── dsc-&gt;data_size = 38400  ├── STREAM_MALLOC(38400) → dsc-&gt;data (PSRAM 0x6xxxxxxx)  │  ├── sys_dcache_invalid_range(rgb_fb-&gt;data, 38400)  // cache 刷新  ├── memcpy(dsc-&gt;data, rgb_fb-&gt;data, 38400)          // 深拷贝  │  ├── lv_img_set_src(img, dsc)   // 设置 LVGL 图像源  │  └── msi_delete_fb(rgb_s, rgb_fb)  // 归还 rgb fbStep 5: 返回 album_show  ├── ui_s-&gt;img_dsc[idx] = dsc  ├── ui_s-&gt;imgs[idx] = img  └── return 0Step 6: 异步释放 JPEG buffer  MSI 框架调用 album_photo_msi_action(MSI_CMD_FREE_FB)  └── STREAM_FREE(photo_buf)    // 释放 PSRAM  └── fbpool_put(&amp;tx_pool, fb)  // 归还 fb

12.2 全屏预览数据流enter_photo_preview_ui(e)  │  ├── 通路切换  │     decode_s → R_VIDEO_P1  (连接)  │     decode_s → R_CSC_MSI   (断开)  │     VIDEO_P1 ENABLE = 1  │  ├── 分辨率配置: OUT_SIZE = 320×240  │  └── album_preview_show(&quot;photo001.jpg&quot;, ui_s)        │        └── album_jpeg_file_load(&quot;photo001.jpg&quot;, ui_s, FRAMEBUFF_SOURCE_JPG)              │              └── msi_output_fb(photo_s, data_s)  // srcID = FRAMEBUFF_SOURCE_JPG                    │                    └── photo_s → other_s → decode_s → R_VIDEO_P1 → LCD                          (配置320×240)   (硬件解码)    (视频层显示)

12.3 CSC 格式选择流程图msi_get_fb(csc_s) 触发 CSC 工作线程  │  └── csc_priv-&gt;current_rx_fb-&gt;srcID        │        ├── FRAMEBUFF_SOURCE_JPG ──→ in_fmt = YUV420P        │                            out_fmt = RGB565        │                            size = 160×120        │                            tx_fb-&gt;mtype = F_RGB        │                            tx_fb-&gt;stype = LVGL_RGB        │        └── default ──→ in_fmt = RGB565                         out_fmt = YUV420P                         size = csc_priv-&gt;width × csc_priv-&gt;height                         tx_fb-&gt;mtype = F_YUV                         tx_fb-&gt;stype = FSTYPE_YUV_P0


十三、操作说明13.1 缩略图浏览模式


操作方式
操作
功能



按键
↑ (AD_UP)
焦点上移；已在第一行则上翻一页


按键
↓ (AD_DOWN)
焦点下移；已在最后一行则下翻一页


按键
← (AD_LEFT)
焦点左移；已在第一个则上翻一页


按键
→ (AD_RIGHT)
焦点右移；已在最后一个则下翻一页


按键
短按确认 (AD_PRESS)
进入当前聚焦缩略图的预览


按键
长按确认 (AD_PRESS)
退出相册返回主菜单


触摸
点击缩略图
进入该图片预览


触摸
点按 “&lt;” 按钮
上一页


触摸
点按 “&gt;” 按钮
下一页


触摸
左右滑动
左滑→下一页，右滑→上一页


13.2 全屏预览模式


操作方式
操作
功能



按键
短按确认 (AD_PRESS)
退出预览返回缩略图


触摸
点击任意位置
退出预览返回缩略图



十四、内存泄漏修复回顾
以下记录来自 /memories/repo/album_ui_leaks.md，是相册功能开发过程中的历史修复记录。

14.1 view_photo_ctx 双重释放 &#x2F; Use-After-Free
症状: 多次点击同一缩略图后系统卡死
根因: view_photo() 中释放了 view_photo_ctx，但 img 对象的点击回调仍持有悬空指针。再次点击触发 use-after-free + double-free，堆损坏后 av_free&#x2F;av_malloc 死锁
修复: 移除 view_photo_ctx 结构体。文件名直接挂在 img user_data 上，事件 user_data 传 ui_s

14.2 翻页时文件名泄漏
根因: album_load_page → lv_obj_clean(img_cont) 删除 img 对象时，LVGL 不会自动释放 user_data
修复: 注册 LV_EVENT_DELETE 回调 (album_img_cleanup)，img 被删除时自动释放文件名副本

14.3 rgb_fb 泄漏
根因: msi_get_fb 返回的 rgb_fb 非 NULL 但 rgb_fb-&gt;data 为 NULL 时，跳过了 msi_delete_fb 调用
修复: 确保 msi_delete_fb 在任何分支都能被执行到

14.4 缺少防重入保护
根因: 快速重复点击 “album” 按钮可能重复创建 MSI pipeline
修复: 在 enter_album_ui 开头增加防重入检查 (if (now_ui) return;)

14.5 退出清理不彻底
修复: lv_obj_del(now_ui) 后增加 now_ui = NULL


附录A. commit 信息commit 72ce4df71923913fe6b60683ce1f61fdbbdb3529Author: zhongxu &lt;z18568601031@gmail.com&gt;Date:   Mon Jun 29 15:54:16 2026 +0800    更新相册功能Files: 7 files changed, 1000 insertions(+), 20 deletions(-)commit eab804e36ea504b5cb5760e6b46fb8978108aae6Author: zhongxu &lt;z18568601031@gmail.com&gt;Date:   Mon Jun 29 20:23:14 2026 +0800    添加照片选中功能Files: 1 file changed, 260 insertions(+), 66 deletions(-)

B. 关键文件路径


文件
路径



相册 UI
sdk/app/ui/album_ui.c


CSC 驱动
sdk/app/video_app/video_app_csc_msi.c


JPEG 解码消息
sdk/app/decode/jpg_decode_msg_msi.c


流定义
sdk/app/algorithm/stream_frame/stream_define.h


Framebuff 定义
sdk/include/lib/multimedia/framebuff.h


LVGL UI 头文件
sdk/app/ui/lvgl_ui.h


主菜单 UI
sdk/app/ui/main_ui.c


]]></content>
      <categories>
        <category>项目文档</category>
        <category>TXW82x 相册</category>
      </categories>
      <tags>
        <tag>LVGL</tag>
        <tag>嵌入式</tag>
        <tag>C语言</tag>
        <tag>MSI</tag>
        <tag>硬件解码</tag>
        <tag>UI组件</tag>
        <tag>TXW82x</tag>
        <tag>JPEG</tag>
      </tags>
  </entry>
  <entry>
    <title>TXW81x 视频对讲开发实战：JPEG over UDP 传输、双通道显示与 LVGL 页面联动</title>
    <url>/2026/07/10/TXW81x%E8%A7%86%E9%A2%91%E5%AF%B9%E8%AE%B2%E5%BC%80%E5%8F%91%E6%96%87%E6%A1%A3/</url>
    <content><![CDATA[1. 项目概述本文面向 81x_intercom_ui 工程，重点说明以下两个模块：

sdk/app/video_intercom/：负责 JPEG 视频帧的获取、UDP 分片发送、网络接收、帧重组和远端 JPEG 投递。
sdk/lib/gui/81x_demo_ui/：负责启动画面、按键翻页、本地&#x2F;远端画面模式切换，以及 LCD P0&#x2F;P1 双视频通道布局。

当前方案采用双端对等结构。每台设备既是发送端，也是接收端：本地摄像头画面编码成 JPEG 后通过 UDP 发给对端，同时接收对端 JPEG 并解码显示。
系统中的两路显示通道约定如下：



通道
数据来源
用途



LCD P0 &#x2F; R_VIDEO_P0
本地摄像头缩放流
本地预览


LCD P1 &#x2F; R_VIDEO_P1
网络接收后解码的远端 JPEG
远端画面


2. 总体架构本机摄像头    │    ▼DVP / VPP_DATA1    │    ▼JPEG0 编码器    │    ▼视频流框架 R_RTP_JPEG    │    ▼bbm_video_tx_thread    │  JPEG 提取、1300 Byte 分片、UDP:11011    ▼======================== Wi-Fi ========================    ▼bbm_video_rx_thread    │  包校验、乱序重组、超时丢帧    ▼S_BBM_REMOTE_JPG_SRC    │    ▼SR_OTHER_JPG_REMOTE    │  设置远端输出尺寸    ▼S_JPG_DECODE    │    ▼LCD R_VIDEO_P1 ─────┐                    ├── LCDC 合成 ── 屏幕本地缩放流 P0 ──────┘LVGL OSD：页面名称、启动 Logo、按键事件

这里的 LVGL 主要承担 OSD 和交互控制，不直接绘制实时视频像素。视频由 LCDC 的 P0&#x2F;P1 硬件通道输出，LVGL 页面使用透明背景叠加在视频层之上。
3. 目录与文件职责3.1 视频传输模块


文件
职责



sdk/app/video_intercom/video_intercom.c
UDP socket、发送&#x2F;接收任务、协议封包、JPEG 分片与重组、远端解码流初始化


sdk/app/video_intercom/video_intercom.h
视频传输控制 API，与 sdk/include/bbm_protocol.h 暴露的接口一致


sdk/include/bbm_protocol.h
UI、音频对讲等模块使用的公共协议头文件


3.2 LVGL UI 模块


文件
职责



sdk/lib/gui/81x_demo_ui/81x_demo_ui.c
UI 主控制器、页面状态、视频布局、按键组、启动 Logo


sdk/lib/gui/81x_demo_ui/81x_demo_ui.h
页面枚举、视频模式枚举、对外切换 API


generated/gui_guider.c
GUI Guider 生成的页面初始化、屏幕加载和动画辅助函数


generated/setup_scr_screen.c
创建 320×240 透明主屏幕


generated/events_init.c
GUI Guider 事件初始化入口，当前无业务事件


custom/custom.c
用户自定义初始化入口，当前为空实现


ui_res/ui_bgLogo.c
320×240 JPEG 启动 Logo 数据


3.3 关联模块


文件
与本功能的关系



project/main.c
初始化摄像头、JPEG、LCD&#x2F;LVGL，并在网络就绪后调用 user_protocol()


sdk/app/video_app/video_app.c
将 JPEG 编码结果绑定到 R_RTP_JPEG


sdk/app/other_jpg_show/other_jpg_show_stream.c
接收远端 JPEG，配置解码输出尺寸


sdk/app/app_lcd/app_lcd.c
控制 P0&#x2F;P1 使能、坐标和上下层关系


sdk/app/interface_management/interface_mgnt.c
初始化 LVGL 并调用 ui_81x_demo_display()


sdk/app/intercom/intercom_adpcm.c
音频对讲复用已学习到的对端 IP


4. 系统启动调用链4.1 硬件与视频源初始化project/main.c::hardware_init() 完成流框架、按键、JPEG、摄像头和 LCD&#x2F;LVGL 初始化。
当 BBM_DEMO == 1 时，JPEG0 使用 VPP_DATA1：
jpg_cfg(HG_JPG0_DEVID, VPP_DATA1);

对应的编码尺寸也必须读取 photo_msg.out1_w/out1_h。工程已经在以下两处做了配套处理：

video_app.c 中使用 get_jpg_default_dpi(1)。
jpg_v2.c::jpg_start() 中使用 photo_msg.out1_h/out1_w。

该约束非常重要。如果数据源使用 VPP_DATA1，却按 VPP_DATA0 的 640×480 尺寸读取一个实际为 320×240 的缓冲区，会出现 2×2 重复画面和绿色色度块。
4.2 UI 初始化UI 启动链如下：
hardware_init()  └─ lvgl_init(w, h, rotate)      ├─ lv_init()      ├─ lv_port_disp_init()      ├─ lv_port_indev_init()      ├─ ui_81x_demo_display()      │   ├─ ui_81x_demo_show_boot_logo()      │   └─ 创建 3000 ms 单次定时器      └─ 创建 gui_thread，运行 lvgl_run()定时器到期  └─ ui_81x_demo_boot_timer_cb()      └─ ui_81x_demo_start_main_ui()          ├─ setup_ui(&amp;guider_ui)          ├─ events_init(&amp;guider_ui)          ├─ custom_init(&amp;guider_ui)          ├─ ui_81x_demo_create_osd()          ├─ ui_81x_demo_init_keypad()          └─ ui_81x_demo_switch_page(LOCAL)

主屏幕在 setup_scr_screen.c 中创建，尺寸为 320×240，背景透明。ui_81x_demo_apply_osd_key_bg() 又将显示器和屏幕底色设置为黑色，用于视频通道暂时无帧或关闭时的背景填充。
4.3 网络协议初始化网络服务初始化完成后，project/main.c::app_network_init() 调用 user_protocol()。本模块提供强符号实现：
user_protocol()  └─ bbm_protocol_start()      ├─ bbm_video_stream_init()      ├─ usr_protocol_create(11011)      ├─ bind(INADDR_ANY:11011)      ├─ 创建 video2_rx 任务      └─ 创建 video2_tx 任务

protocol_client_init() 和 protocol_server_init() 最终也调用同一个 bbm_protocol_start()。bbm_protocol_started 保证初始化幂等，因此 AP 和 STA 两种角色共用同一套传输代码。
5. 本地视频发送链路5.1 JPEG 数据来源video_app.c 创建 JPEG 视频流后，将编码结果绑定到多个目的流，其中包括：
streamSrc_bind_streamDest(s, R_RTP_JPEG);

发送任务通过以下调用订阅该流：
open_stream_available(R_RTP_JPEG, 0, 8, bbm_tx_stream_op, NULL);start_jpeg();

这里的 8 是目的端缓存节点数量。任务循环调用 recv_real_data() 获取编码帧，处理完成后必须调用 free_data(frame) 归还流框架。
5.2 兼容两种 JPEG 存储形式bbm_copy_encoded_jpeg() 同时支持：

JPEG_FULL：完整 JPEG 位于连续内存，直接复制。
分段 JPEG：数据由 stream_jpeg_data_s 链表保存，逐节点复制到连续发送缓冲区。

复制完成后，bbm_find_jpeg() 搜索 JPEG SOI FF D8 和 EOI FF D9，只发送有效 JPEG 区间，避免把编码缓冲区中的前后填充数据发到网络。
5.3 分片发送单帧最大值和 UDP 负载定义如下：
#define BBM_FRAGMENT_PAYLOAD  1300#define BBM_JPEG_MAX_SIZE     (30 * 1024)

每帧最多 24 个分片。bbm_send_frame() 逐片调用 bbm_send_packet()，每包最多重试 3 次，重试和相邻分片之间均有 1 ms 让步。
发送主调用链：
bbm_video_tx_thread()  ├─ recv_real_data(R_RTP_JPEG)  ├─ bbm_copy_encoded_jpeg()  ├─ bbm_find_jpeg()  ├─ bbm_send_frame()  │   └─ bbm_send_packet()  │       └─ sendto(MSG_DONTWAIT)  └─ free_data(frame)

发送条件包括：

bbm_tx_enable == 1；
STA 模式下 Wi-Fi 已连接；
能够取得有效对端 IP。

6. UDP 私有协议协议版本为 2，端口为 UDP 11011，协议头固定 28 字节。多字节字段统一使用网络字节序。



偏移
字段
类型
说明



0
magic
uint32_t
固定值 0x42424D32，ASCII 近似为 BBM2


4
frame_id
uint32_t
帧序号，发送端逐帧递增，允许 32 位回绕


8
frame_len
uint32_t
完整 JPEG 长度，最大 30 KiB


12
frame_time
uint32_t
发送端 os_jiffies() 时间戳


16
version
uint16_t
当前版本 2


18
header_size
uint16_t
当前固定为 28


20
fragment_index
uint16_t
当前分片编号，从 0 开始


22
fragment_count
uint16_t
本帧分片总数，最大 24


24
payload_len
uint16_t
当前 UDP 负载长度，最大 1300


26
flags
uint16_t
bit0 为首片，bit1 为尾片


协议启动时会检查 sizeof(struct bbm_wire_header) == 28，用于发现编译器对齐策略变化。但结构体没有显式 packed 属性，因此移植到不同 ABI 或编译器时，仍应优先改为字段级序列化，而不是直接依赖结构体布局。
7. 远端接收与帧重组7.1 接收任务bbm_video_rx_thread() 在同一个 UDP socket 上调用 recvfrom()，接收超时为 20 ms。收到任意有效长度的数据包后，首先通过源地址学习对端 IP，再根据 bbm_rx_enable 决定是否解析视频包。
接收调用链：
bbm_video_rx_thread()  ├─ recvfrom()  ├─ bbm_learn_peer()  ├─ bbm_parse_packet()  │   └─ bbm_rx_accept_packet()  │       ├─ bbm_rx_start_frame()  │       ├─ 将 payload 复制到 bbm_rx_jpeg[offset]  │       ├─ received_map 标记分片  │       ├─ bbm_find_jpeg()  │       └─ bbm_submit_remote_jpeg()  ├─ 检查 150 ms 帧超时  └─ 检查 2000 ms 对端超时

7.2 数据包校验bbm_parse_packet() 校验：

包长度至少为 28 字节；
magic 和 version 正确；
header_size 等于本地协议头大小；
payload_len 位于 1～1300；
UDP 实际长度等于协议头长度加负载长度。

bbm_rx_accept_packet() 进一步校验：

分片索引小于分片总数；
分片总数不超过 24；
offset 未越过帧长度；
每片长度与它在整帧中的位置严格匹配；
写入范围不超过 30 KiB 接收缓冲区。

7.3 乱序、重复和新旧帧处理bbm_rx_assembler 保存当前正在组装的帧：
struct bbm_rx_assembler &#123;    uint32_t frame_id;    uint32_t frame_len;    uint32_t frame_time;    uint32_t received_map;    uint32_t last_packet_time;    uint16_t fragment_count;    uint16_t received_count;    uint8_t active;&#125;;

实现使用 32 位位图记录分片，能够接收乱序包并忽略重复包。新帧到达时：

若帧号比当前帧旧，记为 old_packet 并忽略；
若帧号更新，丢弃未完成旧帧并开始组装新帧；
帧号比较使用有符号差值，可正确处理 uint32_t 回绕附近的常见情况。

一帧超过 150 ms 未收齐时，接收任务复位组帧状态并增加 frame_timeout。
7.4 投递到解码链路完整 JPEG 通过固定槽位源流进入解码系统：
S_BBM_REMOTE_JPG_SRC  └─ SR_OTHER_JPG_REMOTE      └─ S_JPG_DECODE          └─ R_VIDEO_P1

初始化顺序位于 bbm_video_stream_init()：

创建 JPEG 解码流，绑定到 LCD P1。
创建远端 other_jpg 流，绑定到 JPEG 解码器。
创建 4 槽位远端 JPEG 源流。

每个源槽位按需申请 30 KiB PSRAM，并在槽位归还时保留内存复用，只在 STREAM_DATA_DESTORY 时释放。因此稳定运行后的源槽位池最多占用约 120 KiB PSRAM。
bbm_submit_remote_jpeg() 的所有权规则是：

get_src_data_f() 取得一个空闲 data_structure。
将 JPEG 复制到该槽位自己的 PSRAM 缓冲区。
设置 JPEG_FULL、真实长度和帧时间。
send_data_to_stream() 成功后，所有权交给流框架。
发送失败时调用 force_del_data() 回收槽位。

8. LVGL 页面与视频布局UI 定义四个可循环切换的页面：



页面
页面文字
P0 本地
P1 远端
顶层通道
远端解码



UI_81X_DEMO_PAGE_LOCAL
LOCAL
320×240，坐标 (0,0)
关闭
P0
关闭


UI_81X_DEMO_PAGE_REMOTE
REMOTE
关闭
320×240，坐标 (0,0)
P1
开启


UI_81X_DEMO_PAGE_LOCAL_BIG_REMOTE_SMALL
LOCAL BIG
320×240，坐标 (0,0)
120×90，右上角
P1
开启


UI_81X_DEMO_PAGE_REMOTE_BIG_LOCAL_SMALL
REMOTE BIG
120×90，右上角
320×240，坐标 (0,0)
P0
开启


右上角小窗横坐标计算公式：
SCALE_WIDTH - 120 - 6

在 320×240 屏幕上即为 (194, 6)。
8.1 页面切换调用链LVGL 按键事件  ├─ LV_KEY_PREV ──────────────┐  ├─ LV_KEY_NEXT / ENTER ──────┤  └─ group edge callback ──────┘                 │                 ▼ui_81x_demo_next_page() / prev_page()                 │                 ▼ui_81x_demo_switch_page()                 │                 ▼ui_81x_demo_apply_page()                 │                 ▼ui_81x_demo_set_video_mode()                 ├─ lcd_stream_set_video_enable()                 ├─ lcd_stream_set_video_layout()                 ├─ bbm_protocol_set_remote_display_size()                 ├─ ui_81x_demo_open/close_local_stream()                 └─ bbm_protocol_set_remote_display_enable()

8.2 本地预览流本地画面不是直接使用编码 JPEG，而是创建缩放流：
scale3_stream_not_bind(    &quot;S_81X_LOCAL_DISPLAY&quot;,    input_w, input_h,    output_w, output_h,    YUV_P0);

随后绑定到 R_VIDEO_P0。每次改变本地显示尺寸时，代码会关闭旧流并按新尺寸重新创建，确保全屏和小窗模式使用正确的缩放参数。
8.3 远端显示尺寸UI 通过以下接口设置远端 JPEG 解码输出尺寸：
bbm_protocol_set_remote_display_size(width, height);

该函数把宽高编码到一个 32 位参数中：
((uint32_t)width &lt;&lt; 16) | height

然后通过 SET_LVGL_VIDEO_ARG 发给 other_jpg 流。other_jpg_cmd_func() 在关中断区间更新 out_w/out_h/step_w/step_h，避免解码工作与页面切换同时读写尺寸参数。
8.4 LCD 布局更新lcd_stream_set_video_enable() 和 lcd_stream_set_video_layout() 都在关中断区间更新 app_lcd_s 状态：

video_p0_enable、video_p1_enable：控制两路视频是否输出；
p0_x/y、p1_x/y：控制两路画面坐标；
p0_up：控制 P0&#x2F;P1 的叠放顺序；
layout_pending：通知 LCD 线程应用新布局；
clear_p0_pending/clear_p1_pending：关闭通道时请求清理旧画面。

9. 对端 IP 学习与音视频联动默认对端地址由 Wi-Fi 角色决定：



本机模式
默认对端 IP



STA
192.168.1.1


AP
192.168.1.100


接收任务收到 UDP 包后，以 recvfrom() 返回的源 IP 更新 bbm_peer_ip，同时设置 bbm_peer_ready。音频对讲模块 intercom_adpcm.c 也调用 bbm_protocol_get_peer_ip()，因此视频链路学到的 IP 可以被音频链路复用；反过来，音频 ACK 或 UDP 包也会调用 bbm_protocol_set_peer_ip() 更新同一个对端地址。
连续 2000 ms 未收到视频包时，接收任务清除：

bbm_peer_ready；
bbm_remote_frame_ready；
动态学习到的 bbm_peer_ip。

之后发送端会重新退回 AP&#x2F;STA 对应的默认地址。
10. 任务、并发与内存10.1 任务配置


任务
入口
优先级
栈大小
主要阻塞点



video2_rx
bbm_video_rx_thread()
15
2048
recvfrom()，20 ms 超时


video2_tx
bbm_video_tx_thread()
15
2048
轮询视频流，失败时短暂 sleep


gui_thread
lvgl_run()
NORMAL
4096
LVGL 主循环


lcd_thread
lcd_thread()
NORMAL
1024
LCD 输出与布局更新


视频任务均为永久循环，目前没有停止和销毁路径。
10.2 主要缓冲区


缓冲区
大小
存储区域
用途



bbm_tx_packet
1328 B
静态内存，4 字节对齐
UDP 发送包


bbm_rx_packet
1328 B
静态内存，4 字节对齐
UDP 接收包


bbm_tx_jpeg
30 KiB
.psram.src
连续化后的本地 JPEG


bbm_rx_jpeg
30 KiB
.psram.src
远端帧重组


远端源槽位池
最多 4 × 30 KiB
动态 PSRAM
向解码流投递完整 JPEG


仅视频对讲模块稳定运行时，JPEG 大缓冲区的 PSRAM 上限约为 180 KiB，未包含流框架节点、JPEG 解码器和 LCD 帧缓冲。
10.3 并发模型
TX 任务独占 bbm_tx_packet 和 bbm_tx_jpeg。
RX 任务独占 bbm_rx_packet、bbm_rx_jpeg 和 bbm_rx_frame。
UI&#x2F;LVGL 任务写 bbm_tx_enable、bbm_rx_enable、bbm_displaydecode_run。
音频、RX 和 TX 任务可能同时访问对端状态变量。
这些简单状态使用 volatile，但没有互斥锁或原子操作。当前目标平台对自然对齐的 8&#x2F;32 位访问通常是单次操作，但 volatile 不提供跨核同步、复合操作原子性或内存屏障保证。

11. 对外 API11.1 视频协议控制 API


API
作用



bbm_protocol_set_tx_enable(enable)
开关本机 JPEG 网络发送


bbm_protocol_get_tx_enable()
查询发送状态


bbm_protocol_set_rx_enable(enable)
开关收到包后的协议解析


bbm_protocol_get_rx_enable()
查询接收状态


bbm_protocol_set_remote_display_enable(enable)
开关完整远端帧向解码流投递


bbm_protocol_get_remote_display_enable()
查询远端显示门控


bbm_protocol_set_remote_display_size(w, h)
设置远端解码输出尺寸


bbm_protocol_set_peer_ip(ip)
设置&#x2F;学习对端 IPv4 地址，参数使用网络字节序


bbm_protocol_get_peer_ip()
获取动态地址；未学习时返回默认地址


bbm_protocol_is_peer_ready()
是否已收到对端数据并进入 ready 状态


bbm_protocol_has_remote_frame()
是否至少成功投递过一帧远端 JPEG


bbm_protocol_clear_peer_ready()
清除对端和远端首帧状态


11.2 UI 控制 API


API
作用



ui_81x_demo_display()
显示启动 Logo，并在 3 秒后进入主界面


ui_81x_demo_set_video_mode(mode)
直接设置视频显示模式


ui_81x_demo_switch_page(page)
切换到指定页面


ui_81x_demo_next_page()
循环切换到下一页


ui_81x_demo_prev_page()
循环切换到上一页


ui_81x_demo_show_remote_full()
只显示远端全屏


ui_81x_demo_show_local_full()
只显示本地全屏


ui_81x_demo_show_local_big_remote_small()
本地全屏、远端右上小窗


ui_81x_demo_show_remote_big_local_small()
远端全屏、本地右上小窗


ui_81x_demo_hide_video()
关闭 P0&#x2F;P1 视频显示


12. 调试日志说明发送端每秒打印一次：
video2_tx peer:... frame:... drop:... pkt:... fail:... empty:... speed:...KB/s fd:...




字段
含义



frame
成功发送的完整 JPEG 帧数


drop
JPEG 提取失败或任一分片发送失败的帧数


pkt
成功发送的 UDP 分片数


fail
经过 3 次重试仍失败的分片数


empty
本次轮询未取得 JPEG 帧的次数


speed
本秒发送协议数据量，单位 KiB&#x2F;s


接收端每秒打印一次：
video2_rx en:... pkt:... invalid:... old:... dup:...frame:start/complete drop:... timeout:... gated:...stream:... busy:... mem:... route:... speed:... state:... id:... got:.../...

重点字段：



字段
含义及排查方向



invalid
协议头、长度或分片参数非法；检查版本、端口和数据损坏


old
收到比当前组装帧更旧的分片；通常由网络乱序或延迟引起


dup
重复 UDP 分片


drop
新帧到达时旧帧仍未完成


timeout
150 ms 内未收齐一帧


gated
已收齐，但当前 UI 不需要显示远端画面


stream
成功投递到远端 JPEG 解码流的帧数


busy
4 个远端源槽位全部占用，解码链路消费不及时


mem
源槽位 PSRAM 申请失败


route
send_data_to_stream() 失败


got x/y
当前帧已收到分片数&#x2F;总分片数


13. 常见问题定位13.1 本地画面正常，远端黑屏按以下顺序检查：

video2_rx pkt 是否增长。
invalid 是否持续增长。
frame_complete 是否增长。
当前页面的 en 是否为 1；本地全屏页会主动关闭远端投递。
stream 是否增长，busy/mem/route 是否异常。
lcd_stream_set_video_enable() 是否已开启 P1。
SET_LVGL_VIDEO_ARG 的输出尺寸是否与当前页面一致。

13.2 远端画面卡顿或马赛克
timeout/drop 高：Wi-Fi 丢包、单帧分片过多或 150 ms 超时太短。
fail 高：发送 socket 繁忙，当前非阻塞发送重试仍不足。
busy 高：JPEG 解码或 LCD 消费速度低于网络输入速度。
JPEG 超过 30 KiB：发送端会整帧丢弃，可降低分辨率&#x2F;质量或提高上限。

当前协议没有校验和、FEC、NACK 或关键帧重传。UDP 包丢失会导致整帧在超时或新帧到达时被丢弃，但不会污染下一帧。
13.3 画面出现 2×2 重复或绿色块优先检查 JPEG0 的源和尺寸是否匹配：

jpg_cfg(HG_JPG0_DEVID, VPP_DATA1)；
get_jpg_default_dpi(1)；
jpg_start() 使用 photo_msg.out1_w/out1_h。

这三处必须保持一致。
13.4 页面切换后残留旧画面检查 lcd_stream_set_video_enable() 是否触发 clear_p0_pending 或 clear_p1_pending，以及 LCD 线程是否消费这些标志。布局函数只更新坐标与层级，不负责关闭旧通道。
14. 二次开发指南14.1 新增一种布局
在 ui_81x_demo_video_mode_t 中增加模式。
如需成为可翻页页面，在 ui_81x_demo_page_t 中增加页面。
在 ui_81x_demo_page_name() 中增加 OSD 名称。
编写布局函数，设置 P0&#x2F;P1 使能、远端尺寸、坐标和本地缩放尺寸。
在 ui_81x_demo_set_video_mode() 中增加分支。
在 ui_81x_demo_apply_page() 中建立页面到视频模式的映射。
验证 PIP 坐标没有超出 SCALE_WIDTH/SCALE_HIGH。

14.2 修改分辨率需要同步检查：

摄像头输入 photo_msg.in_w/in_h；
VPP DATA0&#x2F;DATA1 输出尺寸；
JPEG0 编码源和默认 DPI；
BBM_JPEG_MAX_SIZE 是否容纳新 JPEG；
LCD P0 本地缩放输出；
P1 远端解码输出；
UI 主屏幕和 PIP 坐标；
PSRAM 总占用。

14.3 修改 UDP 分片大小双方必须同时修改 BBM_FRAGMENT_PAYLOAD。还要同步评估：

bbm_tx_packet、bbm_rx_packet 大小；
BBM_MAX_FRAGMENTS；
received_map 位数；
Wi-Fi MTU，避免 IP 层再次分片；
发送间隔和帧超时。

当前 1300 字节负载加 28 字节私有头、8 字节 UDP 头和 20 字节 IPv4 头，总计约 1356 字节，低于常见 1500 字节 MTU。
14.4 增加协议可靠性推荐按兼容性从低到高逐步扩展：

在协议头尾部增加 CRC32，并升级 BBM_WIRE_VERSION。
增加会话 ID，避免设备重启后旧包与新帧号混淆。
增加接收端丢片统计和周期性反馈。
对关键帧或低分片数帧增加选择性重传。
网络较差时动态降低 JPEG 质量或帧率。

15. 已知风险与改进建议以下项目不会阻止当前功能运行，但在产品化前值得处理：

任务创建返回值未检查。 csi_kernel_task_new() 失败后仍会把协议标记为已启动，且没有回滚 socket 和流资源。
部分初始化失败没有统一清理。 解码流成功而后续源流失败时，已创建资源保持打开。
没有停止&#x2F;重启接口。 两个任务永久循环，socket、流和 PSRAM 槽位仅适合一次性启动模型。
协议结构依赖 ABI。 虽然检查了 28 字节大小，但更稳妥的做法是显式序列化各字段。
共享状态只使用 volatile。 多任务并发更新对端状态时缺少明确同步机制。
对端学习接受任意 UDP 源。 在解析协议头之前就更新 peer IP，局域网内发往 11011 端口的任意数据都可能改变发送目标。建议至少在 magic&#x2F;version 校验后再学习，产品环境可增加配对标识或鉴权。
未校验 FIRST&#x2F;LAST 标志。 当前主要依赖 index&#x2F;count，标志字段被发送但接收端没有验证。
无数据完整性校验。 UDP 校验和之外没有帧级 CRC，错误载荷可能进入 JPEG 解码器。
UI 重入清理不完整。 ui_81x_demo_display() 若被多次调用，旧定时器、按键组和页面对象的生命周期需要额外确认。
公共声明存在重复。 video_intercom.h 与 sdk/include/bbm_protocol.h 维护同一组 API，后续容易发生声明不一致，建议保留单一公共头文件。

16. 构建与集成检查清单工程的 project/fpv_app_umac4.mk 已包含：

sdk/app/video_intercom/video_intercom.c；
sdk/lib/gui/81x_demo_ui/81x_demo_ui.c；
generated/ 下的 GUI Guider 源文件；
custom/custom.c；
ui_res/ui_bgLogo.c。

编译前确认：

BBM_DEMO == 1；
JPG_EN == 1；
DVP_EN == 1；
LCD_EN == 1；
LVGL_STREAM_ENABLE 与目标 LVGL 显示路径一致；
KEY_MODULE_EN == 1 时按键输入已注册；
工程中只保留一个强符号 user_protocol() 实现，避免与旧版 babyprotocol.c 同时链接产生冲突或选错协议实现；
链接脚本存在 .psram.src 段，并且 PSRAM 容量足够。

17. 核心流程总结这套视频对讲方案的关键设计可以概括为三层：

媒体层：JPEG0 从 VPP_DATA1 编码，本地 YUV 通过 P0 预览，远端 JPEG 通过 P1 解码显示。
传输层：单帧 JPEG 按 1300 字节切分，通过 UDP 11011 双向传输；接收端支持乱序、重复包过滤、新旧帧判断和 150 ms 超时丢帧。
交互层：LVGL 提供启动 Logo、页面名称和按键事件，页面状态机统一控制本地&#x2F;远端通道使能、尺寸、位置和层级。

开发和调试时应始终沿着以下顺序定位问题：
摄像头/VPP → JPEG 编码 → R_RTP_JPEG → UDP 发送→ UDP 接收 → 分片重组 → other_jpg → JPEG 解码→ LCD P1 → P0/P1 布局 → LVGL 页面控制

只要明确每一层的数据格式、缓冲区所有权和开关状态，就能快速区分问题究竟发生在编码、网络、解码、LCD 合成还是 UI 控制环节。
]]></content>
      <categories>
        <category>项目文档</category>
        <category>TXW81x 可视对讲</category>
      </categories>
      <tags>
        <tag>LVGL</tag>
        <tag>嵌入式</tag>
        <tag>C语言</tag>
        <tag>JPEG</tag>
        <tag>流媒体</tag>
        <tag>UDP</tag>
        <tag>TXW81x</tag>
        <tag>视频对讲</tag>
        <tag>双画面</tag>
      </tags>
  </entry>
  <entry>
    <title>Blog 代码主题与目录锚点修复开发文档</title>
    <url>/2026/07/12/Blog%E4%BB%A3%E7%A0%81%E4%B8%BB%E9%A2%98%E4%B8%8E%E7%9B%AE%E5%BD%95%E9%94%9A%E7%82%B9%E4%BF%AE%E5%A4%8D%E5%BC%80%E5%8F%91%E6%96%87%E6%A1%A3/</url>
    <content><![CDATA[
项目: Blog-Hexo (Butterfly 主题定制)阶段: 代码阅读与目录跳转修复提交范围: 18d27c5 至 10e4a3a关键文件: code-highlight.css &#x2F; typography.css &#x2F; responsive.css &#x2F; themes/butterfly/source/js/main.js


目录
提交范围
优化目标
代码主题对齐
目录跳转偏移
实时目录锚点映射
影响范围
验证要点
后续建议


提交范围


提交
日期
说明



18d27c5
2026-07-12
对齐 VS Code Dark+ 代码主题与字体


4f77f3c
2026-07-12
统一目录跳转与标题定位偏移


10e4a3a
2026-07-12
改用实时目录锚点映射机制



优化目标技术博客的核心阅读对象不是普通段落，而是：

代码块。
文件路径。
函数名。
表格。
长文目录。

本阶段主要解决：

代码块颜色和本地 VS Code 阅读习惯不一致。
目录点击后标题定位被固定导航遮挡。
Butterfly 目录锚点在页面结构变化后映射不稳定。


代码主题对齐涉及提交：

18d27c5

涉及文件：

_config.butterfly.yml
source/css/modules/code-highlight.css

1. 目标风格暗色代码块对齐本地常用编辑器体验：

VS Code Dark+
Cascadia Code
16px 左右阅读字号
清晰 token 色彩

2. 关键调整code-highlight.css 调整内容：

代码块背景。
工具栏背景。
行号颜色。
token 颜色。
字体族。
行高。
行内代码样式。

核心字体栈：
font-family: &quot;Cascadia Code&quot;, &quot;Cascadia Mono&quot;, Consolas, &quot;Courier New&quot;, monospace;

3. 设计原因博客文章包含大量：

C 语言代码。
SDK 路径。
宏定义。
函数名。
架构片段。

代码块越接近本地 IDE 阅读体验，迁移到网页后的认知成本越低。

目录跳转偏移涉及提交：

4f77f3c

涉及文件：

source/css/modules/responsive.css
source/css/modules/typography.css
themes/butterfly/source/js/main.js

1. 问题表现点击文章目录后，标题定位容易被固定导航遮住。
原因：

页面存在固定导航栏。
标题锚点默认滚动到视口顶部。
移动端和桌面端导航高度不同。

2. CSS 侧调整通过 scroll-padding-top 和标题定位策略，让跳转位置预留导航高度。
相关文件：
source/css/modules/typography.csssource/css/modules/responsive.css

3. JS 侧调整Butterfly 的目录点击逻辑位于：
themes/butterfly/source/js/main.js

提交 4f77f3c 对目录跳转逻辑做了第一次统一，避免不同入口计算偏移不一致。

实时目录锚点映射涉及提交：

10e4a3a

涉及文件：

themes/butterfly/source/js/main.js

1. 为什么需要实时映射文章内容较长，且标题结构可能包含：

H1&#x2F;H2&#x2F;H3 多级标题。
自动生成的 anchor。
中文标题。
代码、表格、长段落导致的滚动高度变化。

如果目录只在初始化时缓存锚点映射，页面后续变化可能导致：

当前目录高亮不准确。
点击目录跳到错误位置。
标题偏移和实际 DOM 不一致。

2. 实时映射机制提交 10e4a3a 将目录锚点逻辑改为实时查询：

点击时根据当前 DOM 查找目标标题。
滚动时根据当前标题位置更新目录状态。
避免依赖过早缓存的锚点映射。

3. 改动收益
长文目录稳定性更好。
中文标题锚点更可靠。
页面加载后发生布局变化时，目录仍能工作。
与固定导航偏移策略配合更自然。


影响范围


页面
影响



文章页
代码块主题、目录跳转、标题定位


移动端文章页
目录抽屉与标题定位


长文页面
当前目录高亮和滚动定位


技术文档
代码可读性提升



验证要点1. 代码块检查：

暗黑模式代码块是否接近 VS Code Dark+。
字体是否为 Cascadia Code 优先。
行号是否清晰。
选中文本是否可读。
行内代码是否和正文区分明显。

2. 目录点击检查：

点击目录一级标题。
点击目录二级标题。
点击目录三级标题。
标题是否被导航遮挡。
URL hash 是否更新正常。

3. 滚动高亮检查：

手动滚动文章。
当前目录项是否跟随变化。
长文中段和末尾是否仍准确。


后续建议1. 减少主题源码改动themes/butterfly/source/js/main.js 已被本地修改，后续升级 Butterfly 时需要重点合并。
建议后续记录：
themes/butterfly/source/js/main.js

中的本地补丁点。
2. 增加目录视觉层级目录功能已稳定，视觉上还可以继续：

当前项左侧加细线。
H2&#x2F;H3 缩进更明显。
长标题截断更平滑。

3. 优化代码块复制反馈后续可以增加：

复制成功提示。
复制按钮 hover 状态。
移动端复制按钮更大点击区域。


结论本阶段把技术博客最关键的两个阅读组件继续打磨：

代码块更接近本地开发体验。
长文目录跳转和高亮更可靠。

这类优化不一定第一眼显眼，但会显著影响读者阅读长篇工程文档时的稳定感。
]]></content>
      <categories>
        <category>博客开发</category>
      </categories>
      <tags>
        <tag>CSS</tag>
        <tag>Butterfly</tag>
        <tag>Hexo</tag>
        <tag>文档</tag>
        <tag>JavaScript</tag>
        <tag>代码高亮</tag>
        <tag>目录</tag>
      </tags>
  </entry>
  <entry>
    <title>Blog 全站视觉系统重构开发文档</title>
    <url>/2026/07/11/Blog%E5%85%A8%E7%AB%99%E8%A7%86%E8%A7%89%E7%B3%BB%E7%BB%9F%E9%87%8D%E6%9E%84%E5%BC%80%E5%8F%91%E6%96%87%E6%A1%A3/</url>
    <content><![CDATA[
项目: Blog-Hexo (Butterfly 主题定制)阶段: 全站视觉系统重构提交范围: 00973e8 至 26adced样式入口: source/css/custom.css模块目录: source/css/modules/


目录
提交范围
重构目标
文件分类
主题变量与全局基础
首页首屏、导航与页脚
文章列表、侧栏与卡片
文章正文、代码块与目录
移动端与打印样式
主题配置统一
验证要点


提交范围


提交
日期
说明



00973e8
2026-07-11
重构全站视觉基础与主题变量


40e2962
2026-07-11
优化首页首屏导航与页脚体验


6103719
2026-07-11
重塑文章列表侧栏与内容卡片


ad6a1bb
2026-07-11
提升文章排版代码块与目录可读性


610d322
2026-07-11
完善移动端适配与打印体验


26adced
2026-07-11
统一主题配色文案与内容密度



重构目标本阶段将站点从 Butterfly 默认视觉逐步调整为更适合技术博客的界面系统。
主要目标：

建立统一设计变量，减少零散颜色和阴影。
优化首页 Hero、导航、页脚和列表区域。
提升文章页长文阅读体验。
强化代码块、表格、目录等技术内容组件。
完善移动端与打印场景。
用 _config.butterfly.yml 统一站点文案和内容密度。


文件分类


分类
文件
作用



主题系统
variables.css
明暗主题变量、颜色、阴影、圆角


全局基础
base.css &#x2F; background.css &#x2F; scrollbar.css
基础字体、背景、滚动条


布局组件
header-text.css &#x2F; navigation.css &#x2F; footer.css
首屏、导航、页脚


卡片系统
cards.css &#x2F; about-page.css
首页卡片、侧栏、关于页


内容排版
typography.css &#x2F; tables.css
正文、标题、列表、表格


代码阅读
code-highlight.css
代码块、行内代码、语法颜色


文章元信息
post-meta.css
文章头图、目录、版权、标签


响应式
responsive.css
移动端、窄屏、打印


主题配置
_config.butterfly.yml
站点文案、侧栏、文章密度、配色



主题变量与全局基础涉及提交：

00973e8

涉及文件：

variables.css
base.css
background.css
scrollbar.css

1. 主题变量体系variables.css 建立统一变量：
--ui-accent--ui-accent-strong--ui-accent-secondary--ui-text--ui-text-muted--ui-heading--ui-surface--ui-border--ui-shadow-sm--ui-shadow-md--ui-shadow-lg

这些变量覆盖：

明亮模式。
暗黑模式。
Butterfly 主题变量。
代码块、引用块、目录链接等组件颜色。

2. 全局基础样式base.css 统一了：

字体栈。
行高。
选中文本颜色。
焦点态。
页面宽度。
卡片基础背景和阴影。
prefers-reduced-motion 降级规则。

3. 背景与滚动条background.css 将页面背景从装饰性背景转为安静底色。
scrollbar.css 统一滚动条：

更细。
主题感更弱。
明暗模式颜色不同。

设计原则：
技术博客的背景应服务阅读，不喧宾夺主。

首页首屏、导航与页脚涉及提交：

40e2962

涉及文件：

header-text.css
navigation.css
footer.css

1. 首页 Heroheader-text.css 负责：

首页全屏头图。
站点标题。
副标题。
社交图标。
滚动按钮。
文章页头图。

优化目标：

首页有更强个人识别。
内页头图更克制。
文案在图片上保持对比度。

2. 导航栏navigation.css 负责：

顶部导航尺寸。
白色导航在 Hero 上的显示。
固定导航时的毛玻璃背景。
移动端侧边栏菜单。
右侧工具栏。

3. 页脚footer.css 做了页脚视觉统一：

弱化页脚装饰。
统一链接悬停状态。
与页面背景保持一致。


文章列表、侧栏与卡片涉及提交：

6103719

涉及文件：

cards.css
about-page.css

1. 首页文章卡片cards.css 重塑首页文章卡片：

增加左侧强调线。
添加文章序号背景。
统一圆角、边框、阴影。
标题 hover 下划线动画。
摘要使用多行截断。

2. 侧栏卡片侧栏卡片统一为同一套表面系统：

作者卡片。
公告卡片。
最近文章。
分类。
标签云。
归档。

所有卡片共享：

var(--ui-surface)
var(--ui-border)
var(--ui-shadow-sm)

3. 关于页about-page.css 用于 About 页面：

头像。
Hero。
信息网格。
响应式布局。


文章正文、代码块与目录涉及提交：

ad6a1bb

涉及文件：

typography.css
code-highlight.css
post-meta.css
tables.css

1. 正文排版typography.css 优化：

正文字号和行高。
H1&#x2F;H2&#x2F;H3 层级。
标题锚点。
链接样式。
引用块。
列表。
图片与说明文字。
details&#x2F;summary。
嵌入视频。

目标是让长篇技术文档能稳定阅读。
2. 代码块code-highlight.css 优化：

代码块背景。
工具栏。
代码字体。
行号。
hover 行。
token 颜色。
行内代码。

3. 目录与文章元信息post-meta.css 优化：

文章页标题。
文章元信息。
TOC 链接。
当前目录项。
标签。
版权信息。
上下篇导航。

4. 表格tables.css 优化：

横向滚动。
表头背景。
行 hover。
单元格边框。

技术文档中表格频繁出现，因此表格需要比默认样式更清楚。

移动端与打印样式涉及提交：

610d322

涉及文件：

responsive.css

1. 移动端布局优化内容：

窄屏下主布局变为单列。
侧栏卡片隐藏或切换为目录抽屉。
正文字号和行高调整。
导航栏高度和站点名尺寸调整。
代码块、表格、引用块避免撑破屏幕。

2. 打印样式打印模式隐藏：

头图。
侧栏。
右侧工具栏。
页脚。
分享和上下篇组件。

打印保留：

正文。
链接。
代码块。
表格。
引用块。

目的：
让技术文章可以被导出或打印为相对干净的文档。

主题配置统一涉及提交：

26adced

涉及文件：

_config.butterfly.yml

配置统一内容：

主题色。
首页文案。
作者卡片描述。
侧栏卡片显示。
首页文章摘要长度。
目录显示策略。
分类和标签页 UI。

这一步让视觉 CSS 与 Butterfly 配置保持一致，避免 CSS 已经优化但配置仍停留在默认状态。

验证要点构建验证npm run build

需要确认：

CSS 模块正常生成。
首页、文章页、分类、标签、归档页面正常生成。
没有 YAML 配置错误。

视觉验证重点检查：

首页 Hero 是否完整。
导航栏在首屏和滚动后是否清晰。
首页卡片是否对齐。
侧栏卡片是否统一。
文章正文层级是否清楚。
代码块是否可读。
移动端是否无横向溢出。


结论本阶段是 Butterfly 主题接入后的第一次系统性视觉重构。它建立了后续所有优化的基础：

变量统一。
模块边界清楚。
首页和文章页都形成明确风格。
长文阅读组件得到强化。
移动端和打印场景补齐。

后续的素材压缩、全屏透明首页、代码主题对齐和目录锚点修复，都是在这个视觉系统基础上继续推进。
]]></content>
      <categories>
        <category>博客开发</category>
      </categories>
      <tags>
        <tag>CSS</tag>
        <tag>样式</tag>
        <tag>主题定制</tag>
        <tag>Butterfly</tag>
        <tag>Hexo</tag>
        <tag>前端</tag>
        <tag>文档</tag>
      </tags>
  </entry>
  <entry>
    <title>Blog 视觉与阅读体验追加优化开发文档</title>
    <url>/2026/07/12/Blog%E8%A7%86%E8%A7%89%E4%B8%8E%E9%98%85%E8%AF%BB%E4%BD%93%E9%AA%8C%E4%BC%98%E5%8C%96%E5%BC%80%E5%8F%91%E6%96%87%E6%A1%A3/</url>
    <content><![CDATA[
项目: Blog-Hexo (Butterfly 主题定制)阶段: 工作区追加优化基准提交: 10e4a3a 之后优化周期: 2026-07-11 至 2026-07-12样式入口: source/css/custom.css模块目录: source/css/modules/主要目标: 提升首页视觉表现、文章阅读体验、长文目录可用性与移动端可读性。


目录
优化目标
文件分类
优化总览
模块详解
首页 Hero 与头图
导航栏
首页文章卡片
侧栏与目录
文章正文排版
代码块与行内代码
移动端适配
主题变量


验证记录
后续优化建议


优化目标本次优化围绕博客的实际内容形态展开。站点以嵌入式开发、系统设计、工程文档和问题记录为主，因此视觉风格需要同时满足两个方向：

首页保持个人主页的沉浸感，保留全屏壁纸与明确的个人识别。
文章页优先服务长文阅读，目录、代码块、正文层级和移动端换行必须清晰稳定。

最终取向是：少一点装饰性干扰，多一点信息层级、阅读节奏和交互反馈。

文件分类


分类
标签
涉及文件



站点配置
config &#x2F; subtitle &#x2F; home
_config.butterfly.yml


主题系统
theme &#x2F; variable &#x2F; radius
variables.css


布局组件
hero &#x2F; header &#x2F; navigation
header-text.css &#x2F; navigation.css


卡片与侧栏
card &#x2F; sidebar &#x2F; toc &#x2F; surface
cards.css &#x2F; post-meta.css


内容排版
typography &#x2F; article &#x2F; heading
typography.css


代码阅读
code &#x2F; highlight &#x2F; inline-code
code-highlight.css


响应式设计
responsive &#x2F; mobile
responsive.css



优化总览


优化项
目标
结果



首页滚动文字英文化
统一首页 Hero 的视觉语言
首页打字文案改为英文短句


全屏 Hero 保留
保留个人主页沉浸感
full_page 维持 100vh / 100svh


Hero 动效
增加轻量生命感
标题、字幕、社交图标、滚动按钮加入入场与呼吸动画


导航栏对比度
避免白字压在亮图上不清楚
首屏顶部添加暗色渐变，滚动后恢复浅色毛玻璃


首页文章卡片
降低信息密度
摘要缩短，圆角收敛，hover 反馈更清楚


目录正常显示
去掉目录虚化&#x2F;磨砂感
#card-toc 和 .toc-content 改为实底显示


文章正文层级
减弱重复标题压迫感
正文首个 H1 降级为更克制的视觉尺寸


移动端行内代码
避免长路径撑破布局
行内代码允许正常折行


代码交互反馈
提升技术文章细节体验
行内代码 hover 增加轻量边框与阴影反馈



模块详解首页 Hero 与头图涉及文件:

source/css/modules/header-text.css
_config.butterfly.yml

1. 首页英文滚动文案将首页 subtitle.sub 从中文短句调整为英文短句：
sub:  - Turn complex systems into clear notes  - Embedded development, system design, and engineering practice  - Build carefully, document honestly, improve continuously

目的:

首页 Hero 中英文更适合短句打字效果。
减少中文长句在移动端换行时的拥挤感。
强化站点“工程记录 &#x2F; 系统设计 &#x2F; 持续构建”的定位。

2. 保留全屏壁纸首页 Hero 最终保持全屏：
#page-header.full_page &#123;    min-height: 100vh;    min-height: 100svh;    height: 100vh;    height: 100svh;&#125;

说明:

曾尝试将 Hero 调整为 92svh，让首屏露出文章区。
最终确认首页应保留完整壁纸沉浸感，因此恢复为 100svh。

3. Hero 入场动画新增动画:

hero-copy-in
hero-title-in
hero-subtitle-in
hero-social-in
scroll-button-float

对应元素:

#site-info
#site-title
#site-subtitle
#site_social_icons
#scroll-down .scroll-down-effects

设计原则:

动画时间短，避免影响阅读。
不使用大幅度移动。
不对内容做持续闪烁。
滚动按钮只保留轻微呼吸&#x2F;浮动提示。


导航栏涉及文件:

source/css/modules/navigation.css

1. 首屏导航增强对比度首屏 Hero 背景图局部较亮，白色导航文字容易丢失，因此增加顶部暗色渐变：
#page-header.full_page:not(.nav-fixed) #nav &#123;    background: linear-gradient(180deg, rgba(2, 6, 23, 0.42), rgba(2, 6, 23, 0.08)) !important;&#125;

2. 滚动后恢复正常导航选择器限制在 :not(.nav-fixed)，避免滚动到文章列表后仍保留 Hero 的暗色渐变。
验证结果:

首屏导航文字对比度更稳。
滚动后导航恢复浅色实用状态。
未产生横向溢出。


首页文章卡片涉及文件:

source/css/modules/cards.css
source/css/modules/variables.css

1. 圆角收敛将全局大圆角变量从 24px 调整为 18px：
--ui-radius-lg: 18px;

目的:

降低卡片的装饰感。
让技术博客界面更偏工具型、文档型。

2. 摘要密度调整桌面端文章摘要从 3 行改为 2 行：
#recent-posts .recent-post-item .content &#123;    line-height: 1.78;    -webkit-line-clamp: 2;&#125;

移动端保留 3 行：
@media screen and (max-width: 768px) &#123;    #recent-posts .recent-post-item .content &#123;        -webkit-line-clamp: 3;    &#125;&#125;

3. 卡片交互增加轻量 hover：
#recent-posts &gt; .recent-post-items &gt; .recent-post-item:hover &#123;    border-color: var(--ui-border-strong);    box-shadow: var(--ui-shadow-md);    transform: translateY(-6px);&#125;

滚动出现动画只保留透明度变化，避免虚化：
@keyframes surface-reveal &#123;    from &#123;        opacity: 0;    &#125;    to &#123;        opacity: 1;    &#125;&#125;


侧栏与目录涉及文件:

source/css/modules/cards.css
source/css/modules/post-meta.css

1. 目录关闭虚化目录卡片最终改为正常实底显示：
#aside-content .sticky_layout:has(#card-toc),#aside-content #card-toc,#aside-content #card-toc .toc-content &#123;    background: var(--ui-surface-solid) !important;    backdrop-filter: none !important;    -webkit-backdrop-filter: none !important;&#125;

目录内容区也单独覆盖：
#card-toc .toc-content &#123;    background: var(--ui-surface-solid) !important;    backdrop-filter: none !important;    -webkit-backdrop-filter: none !important;&#125;

目的:

目录作为长文导航工具，必须清晰。
避免半透明&#x2F;磨砂效果影响条目识别。
让目录和正文形成稳定阅读辅助关系。

2. 目录内部滚动长文目录内容很多，因此限制目录高度并允许内部滚动：
#card-toc .toc-content &#123;    overflow-y: auto;    max-height: calc(100vh - 180px);    scrollbar-width: thin;&#125;

3. 目录当前项反馈当前项与 hover 项增加轻微横向反馈：
#card-toc .toc-link:hover &#123;    transform: translateX(2px);&#125;#card-toc .toc-link.active &#123;    transform: translateX(3px);&#125;


文章正文排版涉及文件:

source/css/modules/typography.css

1. 正文首个 H1 降级文章页顶部已经有 Hero 标题，正文再次出现同名 H1 时容易显得重复。因此正文首个 H1 调整为更克制的尺寸：
#article-container &gt; h1:first-child &#123;    margin-top: 0;    font-size: clamp(1.55rem, 3vw, 1.95rem);&#125;

2. 正文换行策略全局正文从 overflow-wrap: anywhere 调整为 break-word：
#article-container &#123;    overflow-wrap: break-word;&#125;

目的:

避免普通中文、英文段落过度碎裂。
行内代码在移动端由单独规则处理。


代码块与行内代码涉及文件:

source/css/modules/code-highlight.css
source/css/modules/responsive.css

1. 行内代码 hover 反馈行内代码增加轻量过渡与 hover 状态：
#article-container :not(pre) &gt; code &#123;    transition: background-color 0.18s ease, border-color 0.18s ease, color 0.18s ease,        box-shadow 0.18s ease;&#125;#article-container :not(pre) &gt; code:hover &#123;    border-color: var(--ui-border-strong);    box-shadow: 0 0 0 3px var(--ui-accent-soft);&#125;

2. 移动端长路径折行移动端行内代码允许正常折行：
@media screen and (max-width: 768px) &#123;    #article-container :not(pre) &gt; code &#123;        max-width: 100%;        white-space: normal;        overflow-wrap: anywhere;        word-break: break-word;    &#125;&#125;

解决的问题:

sdk/app/intercom/intercom_adpcm.c 这类路径过长。
移动端正文宽度有限。
不处理会导致行内代码撑破或换行很难看。


移动端适配涉及文件:

source/css/modules/header-text.css
source/css/modules/responsive.css

1. 移动端 Hero 取景移动端调整背景位置：
@media screen and (max-width: 768px) &#123;    #page-header.full_page &#123;        background-position: 47% center !important;    &#125;&#125;

2. 移动端 Hero 信息位置移动端将站点信息下移：
#page-header.full_page #site-info &#123;    top: 70%;&#125;

目的:

避免标题压在人物眼部区域。
保留壁纸主体。
社交图标以底条形式出现，视觉更稳定。

3. 移动端文章页内边距文章页移动端内边距调整：
#post &#123;    padding: 26px 20px;&#125;


主题变量涉及文件:

source/css/modules/variables.css

本次主要调整：
--ui-radius-lg: 18px;

影响范围:

首页文章卡片
侧栏卡片
页面容器
相关文章卡片

设计原因:

原 24px 更偏柔和、装饰性。
18px 更适合技术文档站点，保留现代感但不过度圆润。


验证记录构建验证执行命令：
npm run build

结果：

Hexo 构建通过。
CSS 模块正常生成到 public/css/modules/。
文章、分类、标签、归档页面正常生成。

Chrome 视觉验证检查页面：

http://localhost:4000/
http://localhost:4000/2026/07/03/ADPCM对讲模块详细设计文档/

检查项：

首页 Hero 是否保持全屏。
首页英文打字文案是否正常。
滚动后导航栏是否恢复正常浅色状态。
首页卡片是否无横向溢出。
文章页目录是否正常实底显示。
移动端文章页行内代码是否正常折行。

截图记录目录：
.codex-screenshots/


后续优化建议1. 目录层级继续增强当前目录已经可用，后续可以优化：

二级&#x2F;三级标题缩进更明确。
当前章节左侧增加细线。
长标题截断策略更稳定。

2. 代码块工具栏优化技术文章代码较多，后续可继续优化：

复制按钮成功状态。
代码语言标签位置。
横向滚动条样式。
移动端代码块阅读宽度。

3. 首页卡片元信息结构化当前文章卡片已经收敛摘要，但还可以继续：

日期、分类、标签视觉弱化。
标题和摘要之间拉开层级。
分类标签改为更小的 metadata 样式。

4. 首屏资源优化首页壁纸是核心视觉资源，后续可考虑：

为 Hero 图增加 preload。
针对移动端使用更小尺寸图片。
避免弱网下首屏空白时间过长。


结论本轮优化完成后，站点视觉方向更加明确：

首页负责第一印象，保留全屏壁纸与轻量动画。
首页列表负责内容入口，降低摘要密度并增强卡片反馈。
文章页负责长文阅读，目录正常实底显示，正文层级更克制。
移动端优先保证可读性，尤其是技术路径和行内代码换行。

整体风格从“偏装饰的主题定制”进一步收敛到“适合工程文档阅读的个人技术博客”。
]]></content>
      <categories>
        <category>博客开发</category>
      </categories>
      <tags>
        <tag>CSS</tag>
        <tag>样式</tag>
        <tag>主题定制</tag>
        <tag>Butterfly</tag>
        <tag>Hexo</tag>
        <tag>前端</tag>
        <tag>文档</tag>
      </tags>
  </entry>
  <entry>
    <title>Butterfly 主题接入与仓库清理开发文档</title>
    <url>/2026/07/03/Butterfly%E4%B8%BB%E9%A2%98%E6%8E%A5%E5%85%A5%E4%B8%8E%E4%BB%93%E5%BA%93%E6%B8%85%E7%90%86%E5%BC%80%E5%8F%91%E6%96%87%E6%A1%A3/</url>
    <content><![CDATA[
项目: Blog-Hexo阶段: Butterfly 主题接入提交范围: 196591f 至 daedcd4核心目录: themes/butterfly/目标: 将 Butterfly 主题纳入主仓库，并清理不应进入业务仓库的主题 Git 缓存。


目录
提交范围
背景目标
主题文件接入
Git 缓存清理
目录结构说明
后续定制策略
风险与注意事项


提交范围


提交
日期
说明



196591f
2026-07-03
添加 Butterfly 主题文件


daedcd4
2026-07-03
移除主题目录中的 Git 缓存



背景目标博客从 Hexo 基础站点切换到 Butterfly 主题后，需要将主题文件纳入当前仓库，便于：

本地完整构建，不依赖外部主题目录。
后续可以基于 Butterfly 做配置和样式覆盖。
保证部署环境能够直接读取 themes/butterfly/。

主题接入后又发现主题目录中包含 .git_disabled/ 缓存文件，这类文件属于主题上游仓库的 Git 元数据，不应该进入当前博客仓库，因此随后做了清理。

主题文件接入1. 新增主题主体提交 196591f 添加了完整的 Butterfly 主题目录：
themes/butterfly/├── _config.yml├── languages/├── layout/├── scripts/├── source/├── package.json├── plugins.yml├── README.md└── README_CN.md

2. 主要能力来源Butterfly 主题提供以下基础能力：



能力
对应目录



页面模板
layout/


侧栏卡片
layout/includes/widget/


文章布局
layout/post.pug


首页列表
layout/includes/mixins/indexPostUI.pug


目录组件
layout/includes/widget/card_post_toc.pug


主题脚本
source/js/


Stylus 样式源码
source/css/


配置默认值
scripts/common/default_config.js


3. 对项目的影响主题接入后，博客的视觉和交互能力不再只依赖 Hexo 默认主题，而是由 Butterfly 提供：

首页 Hero 和文章列表。
文章页标题、元信息、目录。
侧栏作者卡片、最近文章、分类、标签。
右侧工具栏、搜索、返回顶部。
代码高亮、暗黑模式、响应式布局。


Git 缓存清理1. 问题来源主题目录中出现了 .git_disabled/：
themes/butterfly/.git_disabled/

该目录包含：

HEAD
config
objects/
refs/
logs/
Git hooks 示例文件

这些内容是主题上游仓库的 Git 元数据，不属于博客项目源码。
2. 清理提交提交 daedcd4 删除了 .git_disabled/ 下的缓存内容，并更新 .gitignore。
清理目标：

避免把上游主题仓库历史带入博客仓库。
减少仓库体积。
避免后续 Git 操作产生混淆。

3. 清理原则主题代码可以进入仓库，但主题自己的 Git 元数据不应该进入仓库。
保留：

themes/butterfly/layout/
themes/butterfly/source/
themes/butterfly/scripts/
themes/butterfly/_config.yml

删除：

themes/butterfly/.git_disabled/
主题上游仓库内部对象缓存


目录结构说明layout&#x2F;负责页面结构：
layout/├── index.pug├── post.pug├── page.pug├── archive.pug├── category.pug└── includes/

后续目录跳转修复、侧栏卡片和文章页结构都依赖这套模板输出的 DOM。
source&#x2F;css&#x2F;Butterfly 原始 Stylus 样式目录：
source/css/├── _layout/├── _page/├── _highlight/├── _mode/└── index.styl

当前项目没有直接大量修改主题 Stylus，而是在 source/css/modules/ 里做自定义 CSS 覆盖。
source&#x2F;js&#x2F;主题交互脚本目录：
source/js/├── main.js├── utils.js└── search/

后续目录锚点映射修复涉及 themes/butterfly/source/js/main.js。

后续定制策略主题接入后，项目采用“少改主题源码，多用自定义 CSS 覆盖”的策略：



类型
策略



视觉主题
优先改 source/css/modules/


站点配置
优先改 _config.butterfly.yml


文章脚手架
改 scaffolds/post.md


主题 JS Bug
必要时改 themes/butterfly/source/js/main.js


上游主题模板
尽量少改，避免升级困难


该策略的好处：

主题升级时冲突更少。
自定义样式边界清楚。
视觉改动可以通过模块化 CSS 管理。


风险与注意事项1. 主题源码升级风险如果后续重新拉取 Butterfly 上游版本，需要注意：

themes/butterfly/source/js/main.js 已有本地修复。
主题默认配置可能与 _config.butterfly.yml 不一致。
主题 DOM 结构变化可能影响自定义 CSS 选择器。

2. 自定义覆盖顺序自定义样式入口为：
source/css/custom.css

它通过 @import 引入模块，因此模块顺序会影响覆盖结果。
3. 不建议修改的内容除非必要，不建议直接修改：

themes/butterfly/layout/
themes/butterfly/source/css/
themes/butterfly/scripts/common/default_config.js

这些文件属于主题核心，升级时最容易冲突。

结论该阶段完成了 Butterfly 主题接入，并清理了主题目录中的 Git 缓存。后续所有视觉、排版、目录、代码块和移动端优化都建立在这个主题基础之上。
本阶段的核心价值不是视觉改造，而是建立主题运行基础和仓库边界。
]]></content>
      <categories>
        <category>博客开发</category>
      </categories>
      <tags>
        <tag>主题定制</tag>
        <tag>Butterfly</tag>
        <tag>Hexo</tag>
        <tag>文档</tag>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>Blog 素材性能与全屏头图配置开发文档</title>
    <url>/2026/07/11/Blog%E7%B4%A0%E6%9D%90%E6%80%A7%E8%83%BD%E4%B8%8E%E5%85%A8%E5%B1%8F%E5%A4%B4%E5%9B%BE%E9%85%8D%E7%BD%AE%E5%BC%80%E5%8F%91%E6%96%87%E6%A1%A3/</url>
    <content><![CDATA[
项目: Blog-Hexo (Butterfly 主题定制)阶段: 素材性能与头图体验提交范围: 4b68bdb 至 3f39d93关键文件: _config.butterfly.yml &#x2F; header-text.css &#x2F; scaffolds/post.md &#x2F; source/img/


目录
提交范围
优化目标
视觉素材压缩
首页全屏透明头图
文章自定义头图
脚手架更新
文档同步
验证要点


提交范围


提交
日期
说明



4b68bdb
2026-07-11
压缩视觉素材并完成界面细节收尾


3f39d93
2026-07-12
全屏透明首页并支持文章自定义头图



优化目标本阶段主要解决两个问题：

首页和侧栏使用的视觉素材体积较大，影响加载速度。
首页头图和文章头图需要更清楚地区分，首页负责沉浸感，文章页负责阅读入口。


视觉素材压缩涉及提交：

4b68bdb

涉及文件：

source/img/avatar.jpeg
source/img/avatar.webp
source/img/dark-bg.png
source/img/dark-bg.webp
source/img/light-bg.png
source/img/light-bg.webp

1. 图片格式迁移原图片：
source/img/avatar.jpegsource/img/dark-bg.pngsource/img/light-bg.png

新图片：
source/img/avatar.webpsource/img/dark-bg.webpsource/img/light-bg.webp

2. 迁移原因WebP 相比 PNG&#x2F;JPEG 更适合网页场景：

文件体积更小。
视觉质量保持较好。
浏览器支持度足够。
首页背景图和头像加载更快。

3. 配套引用更新图片格式变更后，同步更新：

_config.butterfly.yml
source/about/index.html
source/css/modules/header-text.css
source/css/README.md
source/_posts/样式文档.md

确保页面和文档中不再指向旧图片。

首页全屏透明头图涉及提交：

3f39d93

涉及文件：

_config.butterfly.yml
source/css/modules/header-text.css

1. 首页头图透明配置配置中将首页头图交给自定义 CSS 控制：
index_img: transparent

这样可以避免 Butterfly 默认头图干扰自定义背景图。
2. 自定义 CSS 接管首页 Heroheader-text.css 负责首页背景：
#page-header.full_page &#123;    background-image:        linear-gradient(...),        url(&#x27;/img/light-bg.webp&#x27;) !important;&#125;

暗黑模式使用：
html[data-theme=&quot;dark&quot;] #page-header.full_page &#123;    background-image:        linear-gradient(...),        url(&#x27;/img/dark-bg.webp&#x27;) !important;&#125;

3. 首页与内页分离首页：

使用个人壁纸。
保持全屏。
用站点名和副标题建立第一印象。

文章页：

使用文章头图或深色渐变。
强化标题和文章元信息。
不抢正文阅读注意力。


文章自定义头图涉及提交：

3f39d93

1. Front Matter 支持文章可以通过 Front Matter 设置头图：
top_img: /img/your-image.webp

2. 使用场景适合：

项目复盘文章。
架构文档。
带有明确视觉素材的专题文章。

不适合：

纯代码记录。
Bug 修复短文。
无明确视觉主题的文档。


脚手架更新涉及文件：

scaffolds/post.md

新增字段：
top_img:

目的：

新建文章时自动保留头图配置位。
避免每次手动补 Front Matter。
让文章头图能力成为标准写作流程的一部分。


文档同步涉及文件：

source/css/README.md
source/_posts/样式文档.md

同步内容：

图片路径从 PNG&#x2F;JPEG 更新到 WebP。
补充首页 Hero 图片说明。
补充文章自定义头图说明。
更新模块说明，避免文档落后于实现。


验证要点1. 图片路径需要确认：

/img/avatar.webp 正常显示。
/img/light-bg.webp 正常显示。
/img/dark-bg.webp 在暗黑模式下正常显示。

2. 首页 Hero需要确认：

首页保持全屏。
背景由 CSS 控制。
标题和副标题可读。

3. 文章头图需要确认：

未设置 top_img 的文章仍有默认头图表现。
设置 top_img 的文章可以覆盖默认头图。


结论本阶段把视觉素材从“能显示”推进到“适合网页加载”，同时建立了首页头图和文章头图的职责边界。
首页继续承担品牌和氛围，文章页则支持按内容配置头图。后续视觉动画和阅读体验优化，都是在这套资源与头图策略上继续展开。
]]></content>
      <categories>
        <category>博客开发</category>
      </categories>
      <tags>
        <tag>主题定制</tag>
        <tag>Butterfly</tag>
        <tag>Hexo</tag>
        <tag>前端</tag>
        <tag>文档</tag>
        <tag>性能优化</tag>
        <tag>图片</tag>
      </tags>
  </entry>
  <entry>
    <title>TXW81x 微信消息模块总体架构开发文档</title>
    <url>/2026/07/17/TXW81x%E5%BE%AE%E4%BF%A1%E6%B6%88%E6%81%AF%E6%A8%A1%E5%9D%97%E6%80%BB%E4%BD%93%E6%9E%B6%E6%9E%84%E5%BC%80%E5%8F%91%E6%96%87%E6%A1%A3/</url>
    <content><![CDATA[
项目: TXW81x 可视对讲 UI模块: 微信式语音与表情消息阶段: UI、业务服务、UDP 与 Opus 链路整合提交范围: e9a2fb5 至 2440a8e分析基线: 2440a8e（2026-07-17）


1. 文档目标本文给后续开发者建立模块级心智模型，重点说明以下问题：

微信入口从哪里创建，页面由谁管理；
按键事件如何转换为录音、发送、播放等业务动作；
UDP 收发线程、服务线程和音频线程如何协作；
Opus 在本项目中承担什么职责；
哪些路径已经实现，哪些仍是预留能力；
维护时需要优先关注哪些并发、队列和协议风险。

配套文档：

TXW81x微信消息UDP-v2协议开发文档.md：UDP v2 包格式与语音文件传输协议；
TXW81x微信语音Opus与WCA音频链路开发文档.md：Opus 参数、WCA 文件和录放音任务；
TXW81x微信UI控制层与业务服务开发文档.md：LVGL 页面、控制层、服务状态与消息队列。

2. 代码范围


层级
主要文件
职责



应用入口
sdk/app/ui/main_ui.c
调用 wechat_service_create_ui() 创建微信入口


UI 展示
sdk/lib/gui/intercom_ui/wechat_ui.c
创建聊天页面、气泡、表情面板和 30 ms 消息轮询定时器


输入控制
sdk/lib/gui/intercom_ui/wechat_ui_control.c
将 LVGL 键值转换为语音按下&#x2F;松开、表情选择等动作


业务服务
sdk/lib/gui/intercom_ui/wechat_service.c
初始化资源、串行化业务事件、调度录音&#x2F;播放、桥接 UI 与网络


音频适配
sdk/lib/gui/intercom_ui/wechat_app/wechat_audio.c
音频流、Opus 编解码、WCA 文件和异步作业状态机


UDP 传输
sdk/app/wechat_udp/wechat_udp.c
UDP socket、协议封包、接收解析、文件分片发送


Opus 库
sdk/lib/opus/
Opus 1.5.1 编解码实现，固定点配置


sdk/app/intercom/intercom_opus.c 是项目原有的实时对讲链路。微信语音模块没有直接复用它的实时网络抖动缓冲和重传逻辑，而是复用了底层音频 stream 与 Opus API，形成“先录制为文件，再通过 UDP 发送”的异步语音消息链路。
基线版本的关键入口：

sdk/app/ui/main_ui.c:85：创建微信服务 UI；
sdk/lib/gui/intercom_ui/wechat_ui.c:818：通用 UI 创建接口；
sdk/lib/gui/intercom_ui/wechat_service.c:642：业务服务初始化；
sdk/app/wechat_udp/wechat_udp.c:365：UDP 初始化；
sdk/lib/gui/intercom_ui/wechat_app/wechat_audio.c:925：音频与 Opus 初始化。

3. 总体分层main_ui.c    |    vwechat_ui.c  &lt;---- 30 ms 拉取 UI 消息 ----  wechat_service.c    |                                          |       |    | 按键动作                                 |       +--&gt; wechat_udp.c    v                                          |              |wechat_ui_control.c                            |              +--&gt; 对端 UDP 5010                                               |                                               +--&gt; wechat_audio.c                                                       |                                                       +--&gt; sdk/lib/opus/                                                       +--&gt; 音频 stream / DAC                                                       +--&gt; FLASH:/OPUS/*.WCA

设计上的核心边界如下：

UI 层不直接操作 socket、文件或 Opus。
UDP 接收任务不操作 LVGL 对象，只回调服务层。
音频工作任务不操作 LVGL，只向服务队列投递完成事件。
服务任务负责把跨线程事件串行化，并保证录音和播放互斥。
UI 通过独立 UI 队列获取可显示的消息，不直接读取服务内部状态。

4. 运行实体与资源


运行实体
创建位置
优先级
栈&#x2F;队列
主要工作



LVGL 线程&#x2F;定时器
GUI 框架
GUI 上下文
30 ms timer
页面操作、消息气泡渲染


wechat_service
wechat_service_init()
Normal
栈 2048 B；事件队列 16
业务状态编排


wechat_udp_tx
wechat_udp_init()
Normal
栈 1024 B；发送队列 16
异步发送表情


wechat_udp_rx
wechat_udp_init()
Normal
栈 1024 B
收包、校验、回调服务层


wechat_audio
wechat_audio_init()
Normal
PSRAM 栈 30 KiB；作业队列 4
录音、编码、解码、播放


重要静态&#x2F;动态资源：

UDP 上下文包含 1209 B 接收缓冲区和 1208 B 发送缓冲区；
音频播放时从 PSRAM 申请 4 × 1024 B 节点数据区；
音频任务栈从 PSRAM 申请 30 KiB；
Opus encoder、decoder 在服务打开时创建，在页面关闭时释放；
语音文件统一保存在 FLASH:/OPUS。

5. 初始化与退出顺序5.1 打开页面
main_ui.c 创建微信菜单入口。
用户短按入口，wechat_ui_open() 调用服务层 open 回调。
wechat_service_init() 依次创建互斥锁、停止信号量、UI 队列和服务队列。
初始化 wechat_audio，创建 Opus encoder、decoder 和音频工作任务。
初始化 wechat_udp，绑定本地 UDP 端口 5010，创建收发任务。
根据 Wi-Fi 模式配置固定对端：
AP 模式：192.168.1.100:5010；
STA 模式：192.168.1.1:5010。


创建 wechat_service 任务。
服务初始化成功后，UI 才隐藏主页面并创建聊天页面、焦点组和消息 timer。

5.2 关闭页面
删除 UI 消息 timer，停用输入控制。
调用服务层 close 回调。
服务任务收到 STOP 消息后发出停止信号。
停止 UDP 收发任务。
中止并销毁音频任务，释放 Opus 和 PSRAM 资源。
清理未完成的接收文件、队列、锁和信号量。
删除聊天页面，恢复原 LVGL group 和主页面。

6. 本地语音消息链路按住语音键  -&gt; UI action: VOICE_PRESS  -&gt; service queue: VOICE_START  -&gt; 创建 record job_id  -&gt; audio worker 打开 R_WECHAT_AUDIO  -&gt; 每 160 个采样点编码一个 20 ms Opus 帧  -&gt; 写入 FLASH:/OPUS/MSG&lt;n&gt;.WCA松开语音键  -&gt; UI action: VOICE_RELEASE  -&gt; service queue: VOICE_STOP  -&gt; audio worker 协作停止并回报 RECORD_FINISHED  -&gt; service 读取真实录音结果  -&gt; UDP BEGIN + DATA... + END 发送整个 WCA 文件  -&gt; 发送成功后通知 UI 添加本地语音气泡

注意：UI 在松键时计算的秒数被放入 VOICE_STOP 事件，但当前服务逻辑只把它当作附带数据，并不用于最终发送。最终语音时长来自 WCA 文件的实际 Opus 帧数，即 wechat_audio_result_t.duration_sec。
7. 远端语音消息链路
UDP RX 任务收到 WECHAT_UDP_MSG_VOICE。
服务回调按 BEGIN / DATA / END 子类型处理。
BEGIN 创建 FLASH:/OPUS/RX&lt;transfer_id&gt;.TMP。
DATA 要求 offset 严格等于当前已接收长度，并边写文件边计算 CRC32。
END 校验 transfer ID、文件长度和 CRC32。
校验通过后将临时文件重命名为 .WCA。
服务任务把它转为待播放语音并通知 UI 添加远端气泡。
若当前没有录音或播放任务，立即创建播放 job。
音频任务逐帧 Opus 解码，将 PCM 节点送往 speaker stream。

录音优先级高于远端播放：用户在播放期间按下语音键时，服务先停止当前播放，待播放完成事件返回后再开始录音。
8. 表情消息链路8.1 本地发送
用户打开表情面板并选择索引 0..7。
UI 调用 emoji_selected 回调。
服务把 SEND_EMOJI 投递到服务队列。
UI 在投递成功后立即显示本地表情气泡。
服务任务调用 wechat_udp_send_emoji()。
UDP TX 任务异步发送一个 1 B payload 的表情包。

这里采用乐观 UI：本地气泡表示业务事件成功入队，不表示 UDP 已确认送达。
8.2 远端接收
UDP RX 校验协议头和表情索引。
服务回调投递 REMOTE_EMOJI 事件。
服务任务写入 UI 队列。
LVGL timer 拉取消息并显示远端表情气泡。

9. 当前功能边界已实现：

8 个固定表情的双向 UDP 消息；
8 kHz、单声道、20 ms 帧的 Opus 语音录制；
WCA 文件保存、UDP 分片发送、CRC32 校验；
远端语音自动播放；
录音优先于播放的半双工业务调度；
UI 与业务服务解耦；
语音、表情编译期开关。

尚未实现或仅预留：

拍照按钮动作为空；
UDP 的 PHOTO_BEGIN、PHOTO_DATA、PHOTO_END 只有消息类型定义；
ACK 类型尚未形成确认与重传机制；
UI 语音气泡可点击但没有绑定重播事件；
聊天记录未持久化，重开页面不会恢复历史消息；
对端 IP 为固定配置，不支持发现、配对或动态切换。

10. 维护风险摘要高优先级
音频完成回调使用非阻塞方式投递服务队列，且当前忽略投递失败。若 16 深度的服务队列已满，服务层可能收不到完成事件，导致 record_job_id 或 play_job_id 无法清零。
UDP 协议没有 ACK、重传或乱序重组。任意一个语音 DATA 包丢失或乱序都会使接收端中止整个文件。

中优先级
UDP RX 回调内直接执行 Flash 文件创建、写入、重命名和 CRC 计算。Flash 延迟过高时可能拖慢后续收包。
语音接收超时只在收到下一次 BEGIN 时检查。单次传输中途停止后，临时文件可能一直保持打开，直到新 BEGIN 或模块退出。
UI 队列深度为 8，写入采用非阻塞方式；突发远端消息可能被丢弃。
接收端只校验来源 IP，没有校验来源 UDP 端口。

兼容性注意
UDP 多字节字段使用大端序，跨平台可解析。
WCA 文件头和每帧 packet_len 使用目标机本地字节序。当前文件以整体二进制形式传输并在同类设备上播放没有问题，但不应直接假定其跨端序兼容。
Opus 库配置为 fixed-point，且启用了 C99 VLA；音频工作任务的大栈需求需要持续关注。

11. 建议的后续演进顺序
为服务队列完成事件增加可靠投递或兜底状态恢复。
给语音协议增加 ACK、超时清理和重传策略。
将 UDP RX 的文件写入移到专用接收任务或缓存队列。
为消息增加 message ID、发送状态和失败提示，替代完全乐观的 UI。
实现语音气泡重播与聊天记录索引。
再扩展照片分片协议，复用 transfer ID、offset、size、CRC 的通用文件传输模型。

]]></content>
      <categories>
        <category>项目文档</category>
        <category>TXW81x 可视对讲</category>
      </categories>
      <tags>
        <tag>LVGL</tag>
        <tag>UDP</tag>
        <tag>TXW81x</tag>
        <tag>微信消息</tag>
        <tag>可视对讲</tag>
        <tag>Opus</tag>
        <tag>RTOS</tag>
        <tag>软件架构</tag>
      </tags>
  </entry>
  <entry>
    <title>TXW81x 微信 UI、控制层与业务服务开发文档</title>
    <url>/2026/07/17/TXW81x%E5%BE%AE%E4%BF%A1UI%E6%8E%A7%E5%88%B6%E5%B1%82%E4%B8%8E%E4%B8%9A%E5%8A%A1%E6%9C%8D%E5%8A%A1%E5%BC%80%E5%8F%91%E6%96%87%E6%A1%A3/</url>
    <content><![CDATA[
项目: TXW81x 可视对讲 UIUI: wechat_ui.c控制层: wechat_ui_control.c服务层: wechat_service.c提交范围: e9a2fb5 至 2440a8e分析基线: 2440a8e（2026-07-17）


1. 三层职责wechat_ui.c
创建入口按钮和聊天页面；
管理 LVGL 对象、group、焦点和 timer；
绘制本地&#x2F;远端语音与表情气泡；
把控制动作转交 callbacks；
从 callback 拉取可显示消息。

wechat_ui_control.c
识别 ESC、ENTER、语音按下和语音松开键；
根据当前焦点对象生成语义化 action；
记录 speak_pressed，过滤重复按下&#x2F;松开；
不直接调用服务、网络和音频 API。

wechat_service.c
实现 UI callbacks；
创建和销毁音频、UDP、服务任务；
把 UDP&#x2F;音频回调转换为服务事件；
串行调度录音、播放和发送；
把业务结果转换为 UI 消息。

这种拆分使 wechat_ui_create() 可以注入其他 callbacks，UI 本身不依赖具体网络或音频实现。
基线版本的关键源码定位：

sdk/lib/gui/intercom_ui/wechat_ui_control.c:73：按键事件映射；
sdk/lib/gui/intercom_ui/wechat_ui.c:350：UI 消息 timer；
sdk/lib/gui/intercom_ui/wechat_ui.c:519：UI action 分发；
sdk/lib/gui/intercom_ui/wechat_ui.c:687：聊天页面打开；
sdk/lib/gui/intercom_ui/wechat_service.c:392：UDP 回调桥接；
sdk/lib/gui/intercom_ui/wechat_service.c:415：音频回调桥接；
sdk/lib/gui/intercom_ui/wechat_service.c:559：服务任务事件循环；
sdk/lib/gui/intercom_ui/wechat_service.c:894：服务 callbacks 绑定。

2. 页面入口和生命周期main_ui.c 通过以下调用创建入口：
wechat_service_create_ui(group, base_ui);

服务函数构造静态 callbacks，再调用 wechat_ui_create()。UI 为入口分配 wechat_ui_s 上下文，并向主列表加入 wechat 按钮。
用户进入页面时：

先调用 callbacks.open，即 wechat_service_init()；
服务成功后初始化控制层；
隐藏 base_ui；
创建全屏聊天 UI；
创建独立 group 并接管 keypad；
创建 30 ms UI 消息 timer。

用户退出页面时：

ESC 在表情面板打开时只关闭面板；
ESC 在正常聊天页中触发完整退出；
删除 timer 和控制状态；
调用 service deinit；
恢复原 group 和主页面。

3. UI 布局页面使用纵向 flex：
+----------------------------------+|           We Chat               | 20 px+----------------------------------+|                                  ||       可滚动聊天消息列表          | flex grow|                                  |+----------------------------------+| 隐藏/显示的 8 个表情选择按钮      | 40 px+----------------------------------+|    语音       拍照       表情     | 40 px+----------------------------------+

语音气泡宽度按秒数增长：
bubble_width = 65 + min(duration_sec, 30) * 3

本地消息右对齐，远端消息左对齐，并使用不同颜色的圆形 marker 区分双方。
4. 按键映射


键值
焦点对象
Action
行为



LV_KEY_ESC
任意
exit
关闭表情面板或退出页面


LV_KEY_ENTER
拍照按钮
PHOTO_SELECTED
当前无业务实现


LV_KEY_ENTER
表情按钮
EMOJI_PANEL_TOGGLE
打开&#x2F;关闭表情面板


LV_KEY_ENTER
表情项
EMOJI_SELECTED
发送并显示表情


INTERCOM_KEY_SPEAK_PRESS
语音按钮
VOICE_PRESS
请求开始录音


INTERCOM_KEY_SPEAK_RELEASE
语音按钮
VOICE_RELEASE
请求停止录音


语音键只有在焦点位于语音按钮时生效。
5. 焦点组切换正常工具栏状态下，group 包含：
语音 -&gt; 拍照 -&gt; 表情

打开表情面板时：

从 group 移除三个工具按钮；
加入 8 个表情按钮；
聚焦第一个表情。

关闭表情面板时执行反向操作，并将焦点恢复到表情工具按钮。
6. UI callback 接口


Callback
服务实现
线程上下文
作用



open
wechat_service_ui_open
LVGL
初始化全部业务资源


close
wechat_service_ui_close
LVGL
停止并释放全部业务资源


voice_start
wechat_service_ui_voice_start
LVGL
非阻塞投递 VOICE_START


voice_stop
wechat_service_ui_voice_stop
LVGL
非阻塞投递 VOICE_STOP


emoji_selected
wechat_service_ui_emoji_selected
LVGL
非阻塞投递 SEND_EMOJI


get_message
wechat_service_ui_get_message
LVGL timer
非阻塞读取 UI 队列


UI 只在 callback 返回 RET_OK 后更新本地交互状态。例如表情事件成功进入服务队列后，立即创建本地表情气泡。
7. 两级消息队列7.1 服务事件队列深度 16，方向为“各生产者 -&gt; 服务任务”。
生产者包括：

LVGL：VOICE_START、VOICE_STOP、SEND_EMOJI；
UDP RX：REMOTE_VOICE、REMOTE_EMOJI；
音频 worker：AUDIO_RECORD_FINISHED、AUDIO_PLAY_FINISHED。

每条消息压缩为 32 bit：
uint16 cmd + uint16 data

7.2 UI 消息队列深度 8，方向为“服务任务 -&gt; LVGL timer”。
当前消息：

添加远端语音；
添加远端表情；
添加本地语音。

30 ms timer 每次触发时用 while 循环排空队列。
8. 服务任务事件处理


服务事件
核心处理



VOICE_START
标记录音请求；若正在播放则先停止播放，否则开始录音


VOICE_STOP
清除持续录音请求；若正在录音则发停止事件


SEND_EMOJI
调用 UDP 异步表情发送


REMOTE_VOICE
接纳已完成的接收文件，通知 UI，并尝试播放


REMOTE_EMOJI
写入 UI 队列


AUDIO_RECORD_FINISHED
读取结果、发送 WCA、通知 UI，再尝试播放待播语音


AUDIO_PLAY_FINISHED
清除播放 job；优先满足录音请求，否则播放下一条待播语音


9. 录音与播放仲裁服务层使用以下字段表达业务状态：



字段
含义



record_requested
用户当前希望录音


record_job_id
正在启动或运行的录音作业


play_job_id
正在启动或运行的播放作业


pending_voice_valid
有一条远端语音等待播放


pending_voice_path
待播 WCA 路径


主要规则：

录音和播放不能同时运行。
用户按下语音键时，录音优先。
若正在播放，先请求停止；收到 PLAY_FINISHED 后再启动录音。
录音完成并发送后，才尝试播放远端待播语音。
当前只保存一条 pending voice，无法积压多条远端语音。

10. 语音接收文件状态wechat_service_voice_rx_s 管理一个活动传输：



字段
含义



file
当前 .TMP 文件句柄


transfer_id
当前传输 ID


expected_size
BEGIN 声明大小


received_size
已按序写入大小


crc32
增量 CRC 状态


last_packet_tick
最近数据时间


active
正在接收


ready
.WCA 已校验并等待服务接纳


状态简图：
idle  -&gt; BEGIN 合法，创建 TMPactive  -&gt; DATA 连续写入  -&gt; 错误：abort + 删除 TMP  -&gt; END 校验成功：renameready  -&gt; REMOTE_VOICE 事件pending/play

11. 编译期开关wechat_service.h 提供：
WECHAT_SERVICE_VOICE_ENABLEWECHAT_SERVICE_EMOJI_ENABLE

默认均为 1。关闭语音后，音频模块初始化、语音接收状态和相关 UI 消息映射会被条件编译移除；关闭表情后，表情网络事件和 callbacks 绑定会被移除。
UI 视觉控件本身目前仍在 wechat_ui.c 中创建，因此若希望彻底裁剪资源和交互入口，还需要同步在 UI 层增加条件编译或运行时隐藏。
12. 已确认的行为差异12.1 本地表情是乐观显示表情在服务事件入队成功后立即显示。后续 UDP TX 队列满或 sendto() 失败不会回滚气泡，也没有失败标记。
12.2 本地语音是发送成功后显示语音气泡只有在录音成功且 wechat_udp_send_voice_file() 返回成功后才加入 UI 队列。这里的成功仍然只是本机 UDP 发送完成，不代表远端已确认接收。
12.3 远端语音气泡先于实际播放结果接收文件通过 CRC 校验后就通知 UI 添加气泡，随后才尝试播放。播放失败不会移除气泡。
13. 并发与队列风险高优先级：音频完成事件可能丢失wechat_service_audio_event() 在音频 worker 中非阻塞投递完成事件，但没有处理返回失败。如果服务队列已满：
audio 已回到 IDLEservice 仍保留 record_job_id/play_job_id后续录音或播放可能一直无法启动

建议至少对完成事件采用可靠投递，或在服务任务中定期对比 wechat_audio_get_state() 做状态自愈。
中优先级：UI 消息丢失UI 队列写入为非阻塞，队列满时消息会丢失。远端文件可能已经播放，但气泡没有显示；本地语音可能已经发送，但 UI 没有记录。
中优先级：只支持一个 pending voice当 pending_voice_valid 已经为 1 时，新的 BEGIN 不被接受。远端连续发送多条语音时，必须等待上一条被服务接纳并开始播放后才能接收下一条。
低优先级：UI 上下文生命周期wechat_ui_s 在创建入口时分配，并随入口长期存在；退出聊天页只删除 now_ui，不释放入口上下文。这符合入口常驻的当前用法，但如果未来动态删除和重建菜单入口，需要补充入口对象删除回调来释放上下文。
14. 扩展功能的推荐接入点增加照片消息
在 WECHAT_UI_ACTION_PHOTO_SELECTED 中调用新 callback；
在 callbacks 和服务事件枚举中增加 photo 事件；
复用语音的 transfer ID、offset、size、CRC 模型；
文件落盘完成后再通知 UI；
不要在 UDP RX 回调中做图片解码或 LVGL 操作。

增加语音气泡重播
UI 消息不能只携带 duration，需要增加稳定的 message ID；
服务维护 message ID 到 WCA path 的映射；
气泡点击事件向服务投递 PLAY_MESSAGE；
服务继续使用现有录音优先仲裁规则。

增加发送状态建议引入：queued、sending、sent、delivered、failed。表情和语音统一由服务生成 message ID，UI 根据状态消息更新气泡，而不是仅在创建时决定结果。
15. 调试入口优先观察以下日志：

wechat udp initialized &#x2F; deinitialized；
wechat udp peer configured；
wechat voice receive begin；
wechat voice receive sequence error；
wechat voice receive validation failed；
wechat voice receive complete；
wechat audio record start failed；
wechat voice send failed；
wechat audio play failed。

出现“按键无反应”时，按以下顺序检查：

焦点是否位于语音按钮；
PRESS&#x2F;RELEASE 日志是否成对；
服务事件队列是否成功入队；
audio state 是否为 IDLE；
R_WECHAT_AUDIO stream 是否有输入；
Flash 是否可创建 WCA；
对端 IP、端口和 Wi-Fi 模式是否匹配。

]]></content>
      <categories>
        <category>项目文档</category>
        <category>TXW81x 可视对讲</category>
      </categories>
      <tags>
        <tag>LVGL</tag>
        <tag>UI架构</tag>
        <tag>TXW81x</tag>
        <tag>微信消息</tag>
        <tag>RTOS</tag>
        <tag>业务服务</tag>
        <tag>状态机</tag>
        <tag>消息队列</tag>
      </tags>
  </entry>
  <entry>
    <title>TXW81x 微信语音 Opus 与 WCA 音频链路开发文档</title>
    <url>/2026/07/17/TXW81x%E5%BE%AE%E4%BF%A1%E8%AF%AD%E9%9F%B3Opus%E4%B8%8EWCA%E9%9F%B3%E9%A2%91%E9%93%BE%E8%B7%AF%E5%BC%80%E5%8F%91%E6%96%87%E6%A1%A3/</url>
    <content><![CDATA[
项目: TXW81x 可视对讲 UI音频适配: sdk/lib/gui/intercom_ui/wechat_app/wechat_audio.c编解码库: sdk/lib/opus/Opus 版本: 1.5.1提交范围: 6c55dda 至 2440a8e分析基线: 2440a8e（2026-07-17）


1. 模块定位wechat_audio 将项目音频 stream、Flash 文件和 Opus API 封装为单任务异步作业接口：

wechat_audio_record_start()：开始录音并写入 WCA 文件；
wechat_audio_record_stop()：协作停止录音；
wechat_audio_play_start()：播放一个 WCA 文件；
wechat_audio_play_stop()：协作停止播放；
wechat_audio_get_result()：读取最近完成作业的结果。

同一时刻只允许一个录音或播放作业。业务服务持有 job ID，用它匹配命令、完成回调和结果。
基线版本的关键源码定位：

sdk/lib/gui/intercom_ui/wechat_app/wechat_audio.c:58：WCA 文件头；
sdk/lib/gui/intercom_ui/wechat_app/wechat_audio.c:375：单帧 Opus 编码；
sdk/lib/gui/intercom_ui/wechat_app/wechat_audio.c:401：录音作业；
sdk/lib/gui/intercom_ui/wechat_app/wechat_audio.c:653：播放作业；
sdk/lib/gui/intercom_ui/wechat_app/wechat_audio.c:902：encoder 参数；
sdk/lib/gui/intercom_ui/wechat_app/wechat_audio.c:925：模块初始化；
sdk/lib/opus/opus_config.h:50：fixed-point 配置；
sdk/lib/opus/opus_config.h:203：Opus 版本。

2. Opus 编译配置sdk/lib/opus/opus_config.h 的关键配置：



配置
值
含义



PACKAGE_VERSION
1.5.1
Opus 源码版本


FIXED_POINT
1
使用固定点实现


VAR_ARRAYS
1
允许 C99 变长数组


OPUS_BUILD
enabled
编译 Opus 库本体


ARM&#x2F;NEON 优化
未启用
当前以通用 C 路径为主


ENABLE_HARDENING
未启用
未开启额外 hardening


Deep PLC &#x2F; DRED &#x2F; OSCE
未启用
新增增强能力未启用


固定点配置适合无高性能 FPU 的 MCU&#x2F;SoC，但 Opus 内部调用深度和 VLA 会增加栈压力。本模块为音频任务单独分配了 30 KiB PSRAM 栈。
3. 微信语音编码参数


参数
当前值



采样率
8000 Hz


声道
单声道


PCM 位深
16 bit


每帧采样点
160


每帧时长
20 ms


每帧 PCM 大小
320 B


目标码率
8000 bit&#x2F;s


应用模式
OPUS_APPLICATION_VOIP


带宽
OPUS_BANDWIDTH_NARROWBAND


VBR
关闭，CBR


Complexity
0


In-band FEC
关闭


DTX
关闭


Packet loss percent
0


最大 Opus 包缓存
256 B


最大录音时长
60 s


这是一套偏低算力、窄带语音的参数，目标是降低 CPU、存储和网络开销，而不是追求宽带语音质量。
理论上 8 kbit&#x2F;s 的 20 ms CBR 帧约为 20 B 编码数据；WCA 每帧额外保存 2 B 长度字段。
4. WCA 文件格式文件扩展名为 .WCA，magic 为 WCA1。文件由固定头和连续 Opus 帧组成。
+-----------------------------+| WCA header                  |+-----------------------------+| uint16 packet_len           || packet_len bytes Opus data  |+-----------------------------+| uint16 packet_len           || packet_len bytes Opus data  |+-----------------------------+| ...                         |+-----------------------------+

4.1 文件头wechat_audio_file_header_s 使用 packed 布局，共 28 B。



字段
类型
当前值&#x2F;含义



magic
uint32
0x31414357，在当前小端目标上对应字节串 WCA1


version
uint16
1


header_size
uint16
sizeof(header)


sample_rate
uint32
8000


channels
uint16
1


frame_samples
uint16
160


frame_count
uint32
Opus 帧数


duration_ms
uint32
frame_count × 20


payload_size
uint32
所有长度字段和 Opus 包的总字节数


录音开始时先写一个全零占位头；结束后回到文件起点写入最终元数据。
4.2 字节序WCA 头和 packet_len 直接写入目标机本地二进制表示，没有转换为网络字节序。当前 UDP 层发送的是整个 WCA 文件原始字节，因此同构 TXW81x 设备之间可以使用；若未来由 PC、手机或不同端序芯片解析，应先明确并固定 WCA 的标准字节序。
5. 录音编码流程R_WECHAT_AUDIO stream  -&gt; recv_real_data()  -&gt; 聚合为 320 B / 160 samples  -&gt; opus_encode(..., 160, ..., 256)  -&gt; 写 uint16 packet_len  -&gt; 写 Opus packet  -&gt; 重复直到 STOP / ABORT / 60 s timeout  -&gt; 回写 WCA header

关键行为：

录音 stream 名为 R_WECHAT_AUDIO，定义值为 wechat-record-audio。
输入节点长度向下对齐到偶数，避免半个 16 bit 采样。
输入节点可以大于或小于一帧，模块使用 320 B 栈内 PCM 帧进行聚合。
STOP、ABORT 和最大录音时长在循环顶部检查。
少于一帧的尾部 PCM 不编码；至少要成功编码一帧，文件才有效。
成功文件命名为 FLASH:/OPUS/MSG&lt;n&gt;.WCA。
&lt;n&gt; 通过扫描现有文件的最大索引再加一得到。

录音结果时长来自实际写入帧数，而不是按键按住时间：
duration_ms  = frame_count * 20duration_sec = ceil(duration_ms / 1000)

6. 播放解码流程打开 WCA  -&gt; 校验 header  -&gt; reset Opus decoder  -&gt; 创建 4 个播放节点，每个 1024 B  -&gt; 绑定 speaker stream  -&gt; DAC 切换到 8 kHz / SOUND_FILE  -&gt; 逐帧读取 packet_len 和 packet  -&gt; opus_decode(..., max 160 samples, FEC=0)  -&gt; 标记 16 bit PCM 长度并送入 stream  -&gt; 关闭 stream，恢复 DAC 配置

播放缓冲总大小为 4 KiB，分配在 PSRAM。单帧解码 PCM 为 320 B，小于单节点 1024 B。
播放前会检查：

magic、version 和 header size；
采样率、声道和 frame samples 必须与当前固件一致；
frame count 非零；
payload size 不越过文件大小；
frame count 与 payload 最小长度关系合理；
每帧 packet length 为 1..256。

7. 作业状态机UNINITIALIZED    |    vIDLE  | record_start          | play_start  v                       vRECORD_STARTING         PLAY_STARTING  | stream ready           | stream ready  v                       vRECORDING               PLAYING  | record_stop            | play_stop  v                       vRECORD_STOPPING         PLAY_STOPPING  | finished               | finished  +-----------+-----------+              v             IDLE

状态、当前 job ID、播放路径和最近结果由 wechat_audio.lock 保护。STOP&#x2F;ABORT 使用 event flags，使停止 API 不必等待整个音频循环结束。
8. 完成原因与错误码完成原因用于描述正常结束方式：

completed；
user stopped；
timeout；
aborted；
error。

错误码用于描述具体失败点，例如：

文件打开或文件 I&#x2F;O；
stream 打开失败；
encoder&#x2F;decoder 初始化失败；
Opus 编码&#x2F;解码失败；
文件格式非法；
内存不足；
内部错误。

完成回调在音频工作任务上下文执行。回调不得操作 LVGL，也不应执行阻塞工作；当前服务回调只把 (event, job_id) 投递到服务队列。
9. 初始化与资源生命周期初始化顺序：

初始化 mutex；
初始化 control event；
初始化深度为 4 的 job queue；
创建并配置 Opus encoder；
创建 Opus decoder；
从 PSRAM 申请 30 KiB task stack；
创建常驻音频 worker；
状态切换为 IDLE。

退出顺序：

设置 STOP 与 ABORT；
反复尝试投递 EXIT，直到 job queue 接受；
等待 worker 设置 WORKER_STOPPED；
销毁任务并释放 PSRAM 栈；
销毁 decoder、encoder；
删除 queue、event、mutex。

禁止在音频回调自身调用 deinit，否则会形成任务等待自身退出的问题；代码已显式拒绝这种调用。
10. 与实时对讲 Opus 链路的区别


维度
微信语音
intercom_opus.c 实时对讲



业务形态
录完后发送文件
边采集边发送、边收边播


网络对象
WCA 文件分片
实时编码帧&#x2F;环形缓冲


抖动处理
无
有缓存、丢包检查等逻辑


PLC&#x2F;FEC
FEC 关闭，不做丢包补偿
原链路包含 PLC 相关逻辑


存储
Flash 持久文件
以内存流为主


调度
单 worker，录放互斥
多任务并发实时处理


因此，实时对讲中的丢包补偿、重传和同步策略不会自动作用于微信语音文件传输。
11. 风险与待确认项11.1 完成事件可靠性音频模块只保存最近一个完成结果。当前服务严格串行启动作业，因此正常情况下不会被覆盖；但如果 AUDIO_*_FINISHED 事件因服务队列已满而丢失，服务状态可能与音频模块的 IDLE 状态脱节。
11.2 播放尾部是否完全排空最后一帧送入 stream 后代码立即进入 cleanup 并关闭 stream。是否会等待 speaker 端消费所有已排队节点，取决于 close_stream() 的底层语义，需要结合 stream 框架验证。若关闭不带 drain，可能截断尾部音频。
11.3 WCA 完整性WCA 自身没有 CRC。网络接收时由外层语音传输 CRC32 保证整个文件完整，但本地 Flash 文件损坏只能通过头字段和逐帧长度发现，不能检测所有静默位翻转。
11.4 文件索引与容量管理每次录音扫描目录并使用最大序号加一，没有回收策略、容量上限处理和序号溢出处理。长期运行时需要增加消息索引与存储清理策略。
11.5 Opus 栈与性能当前使用通用 fixed-point C 路径、complexity 0 和 30 KiB PSRAM 栈。修改 Opus complexity、启用新算法、切换 frame size 或打开额外增强功能后，应重新测量：

worker 最大栈深；
单帧 encode&#x2F;decode 耗时；
PSRAM 访问对实时性的影响；
录音 stream 是否出现积压；
播放节点是否出现欠载。

12. 参数修改检查表如果修改采样率或 frame size，必须同步检查：

encoder&#x2F;decoder create 参数；
WECHAT_AUDIO_FRAME_SAMPLES；
WECHAT_AUDIO_FRAME_BYTES；
Opus expert frame duration；
WCA header 校验规则；
duration_ms 计算，当前写死为每帧 20 ms；
DAC 采样率切换；
record stream 实际 PCM 格式；
play node 容量；
对端固件兼容性。

]]></content>
      <categories>
        <category>项目文档</category>
        <category>TXW81x 可视对讲</category>
      </categories>
      <tags>
        <tag>TXW81x</tag>
        <tag>Opus</tag>
        <tag>RTOS</tag>
        <tag>微信语音</tag>
        <tag>WCA</tag>
        <tag>音频编解码</tag>
        <tag>PSRAM</tag>
      </tags>
  </entry>
  <entry>
    <title>TXW81x 微信消息 UDP v2 协议开发文档</title>
    <url>/2026/07/17/TXW81x%E5%BE%AE%E4%BF%A1%E6%B6%88%E6%81%AFUDP-v2%E5%8D%8F%E8%AE%AE%E5%BC%80%E5%8F%91%E6%96%87%E6%A1%A3/</url>
    <content><![CDATA[
项目: TXW81x 可视对讲 UI模块: sdk/app/wechat_udp/协议版本: 2默认端口: 5010/UDP提交范围: febc4f7 至 2440a8e分析基线: 2440a8e（2026-07-17）


1. 模块定位wechat_udp 是微信消息功能的网络传输层，当前负责：

创建并绑定 UDP socket；
管理固定对端地址；
生成和解析统一 v2 包头；
异步发送表情消息；
同步分片发送 WCA 语音文件；
在 RX 任务中校验数据包并回调上层服务。

模块不负责 UI、Opus 编解码、业务状态和远端语音文件落盘。
基线版本的关键源码定位：

sdk/app/wechat_udp/wechat_udp.c:145：生成通用包头；
sdk/app/wechat_udp/wechat_udp.c:255：解析通用包头；
sdk/app/wechat_udp/wechat_udp.c:309：UDP RX 任务；
sdk/app/wechat_udp/wechat_udp.c:365：socket 与任务初始化；
sdk/app/wechat_udp/wechat_udp.c:579：语音文件分片发送；
sdk/lib/gui/intercom_ui/wechat_service.c:206：语音 BEGIN 接收；
sdk/lib/gui/intercom_ui/wechat_service.c:274：语音 DATA 接收；
sdk/lib/gui/intercom_ui/wechat_service.c:308：语音 END 校验。

2. 通用数据包格式固定包头长度为 8 B，最大 payload 为 1200 B，最大 UDP 应用数据长度为 1208 B。



偏移
长度
字段
编码
说明



0
1
Magic 0
ASCII
固定为 W


1
1
Magic 1
ASCII
固定为 X


2
1
Version
uint8
当前为 2


3
1
Type
uint8
消息类型


4
2
Sequence
uint16 BE
发送包序号，自然回绕


6
2
Payload length
uint16 BE
0..1200


8
N
Payload
bytes
长度必须与包头完全一致


接收端拒绝以下数据包：

长度小于 8 B 或大于 1208 B；
Magic 或版本不匹配；
payload 长度大于 1200 B；
UDP 实际长度不等于 8 + payload_len。

3. 通用消息类型


Type
枚举
当前状态



1
WECHAT_UDP_MSG_EMOJI
已实现收发


2
WECHAT_UDP_MSG_VOICE
已实现 WCA 文件收发


3
WECHAT_UDP_MSG_PHOTO_BEGIN
预留


4
WECHAT_UDP_MSG_PHOTO_DATA
预留


5
WECHAT_UDP_MSG_PHOTO_END
预留


6
WECHAT_UDP_MSG_ACK
预留，尚未使用


sequence 当前只由发送端递增并传给接收回调，上层没有据此执行去重、丢包判断或重排。
4. 表情消息表情 payload 固定为 1 B：



偏移
长度
字段
约束



0
1
emoji_index
0..7


发送 API wechat_udp_send_emoji() 只负责把索引投递到深度为 16 的 TX 队列。真正的 sendto() 在 wechat_udp_tx 任务中执行，因此 API 返回成功仅表示消息已入队。
5. 语音文件传输语音使用一个通用消息类型 WECHAT_UDP_MSG_VOICE，payload 第 1 B 再区分子类型。
5.1 BEGINpayload 固定 9 B。



偏移
长度
字段
编码
说明



0
1
subtype
uint8
WECHAT_UDP_VOICE_BEGIN = 1


1
2
transfer ID
uint16 BE
0 为非法值


3
4
file size
uint32 BE
当前限制 1..128 KiB


7
2
duration
uint16 BE
秒，必须大于 0


5.2 DATA固定头 7 B，后接最多 1193 B 文件内容。



偏移
长度
字段
编码
说明



0
1
subtype
uint8
WECHAT_UDP_VOICE_DATA = 2


1
2
transfer ID
uint16 BE
必须与 BEGIN 一致


3
4
offset
uint32 BE
当前数据在文件内的字节偏移


7
N
data
bytes
1..1193 B


接收端要求：
transfer_id == active transfer_idoffset      == received_sizedata_len    &lt;= expected_size - received_size

这意味着协议当前只接受严格有序、无重复、无缺口的数据流。
5.3 ENDpayload 固定 11 B。



偏移
长度
字段
编码
说明



0
1
subtype
uint8
WECHAT_UDP_VOICE_END = 3


1
2
transfer ID
uint16 BE
必须与 BEGIN 一致


3
4
file size
uint32 BE
必须与 BEGIN 和累计长度一致


7
4
CRC32
uint32 BE
整个 WCA 文件的 CRC32


CRC 参数：

初始值：0xffffffff；
多项式反射形式：0xedb88320；
最终异或：0xffffffff；
覆盖范围：所有 DATA 中的文件内容，不含 UDP 和语音子协议头。

6. 发送流程wechat_udp_send_voice_file() 在调用者线程中同步完成整个文件发送：

校验模块状态、对端、路径和时长。
打开文件并检查大小不超过 128 KiB。
持有 tx_lock，防止表情包插入语音文件传输中间。
分配非零 transfer ID。
发送 BEGIN。
每次读取最多 1193 B，生成 offset 并发送 DATA。
每个 DATA 后等待 5 ms，降低突发发送压力。
完成 CRC32 后发送 END。
释放 tx_lock 并关闭文件。

在当前业务中，这个函数由 wechat_service 任务在录音完成后调用，因此语音发送期间服务事件处理会暂停。
7. 接收流程wechat_udp_rx 使用 100 ms socket 接收超时，以便退出时能够观察 rx_running = 0。
recvfrom  -&gt; 可选来源 IP 过滤  -&gt; v2 包头解析  -&gt; 表情合法性检查 / 通用消息分发  -&gt; recv_cb(type, payload, length, sequence, user_data)  -&gt; wechat_service_udp_recv()

回调中的 payload 指向 UDP 模块内部 RX 缓冲区，只在本次回调期间有效。上层若需要延后处理，必须复制数据。
8. 对端配置wechat_udp_set_peer() 保存 IPv4 地址与端口。服务层当前按 Wi-Fi 模式使用固定地址：



本机模式
对端地址



AP
192.168.1.100:5010


STA
192.168.1.1:5010


RX 过滤只比较来源 IPv4 地址，没有比较来源端口。
9. 线程与锁


资源
访问者
保护方式



tx_buffer、发送 sequence
TX 任务、服务任务
tx_lock


TX 消息
UI&#x2F;服务调用者、TX 任务
tx_msgq


RX buffer
RX 任务
单任务独占，回调期间借用


socket
TX、RX、deinit
生命周期状态 + 退出信号量


peer 地址
初始化流程、TX&#x2F;RX
当前假定运行期间不动态修改


TX 与 RX 任务在退出点先发信号量，然后停留在休眠循环，最终由 deinit 调用 os_task_destroy() 回收。
10. 错误和丢包行为


情况
当前行为



表情 TX 队列满
wechat_udp_send_emoji() 返回失败


sendto() 失败
打印日志，不重试


非法包头&#x2F;长度
丢弃并打印日志


未知消息类型
丢弃并打印日志


来源 IP 不匹配
静默丢弃


DATA 丢失、重复或乱序
服务层中止接收并删除临时文件


END 长度或 CRC 错误
中止并删除临时文件


传输中途停止
保持 active，直到新 BEGIN 触发超时检查或模块退出


11. 已确认的限制与改进建议11.1 没有可靠传输闭环虽然定义了 ACK 类型和 sequence 字段，但尚无确认、重传、窗口或去重实现。语音消息的成功发送日志只代表本机所有 sendto() 调用成功，不代表对端完整落盘。
建议最小升级方案：

END 校验成功后返回包含 transfer ID、size、CRC 的 ACK；
发送端等待 ACK 超时，至少支持整文件重发；
再逐步增加分片位图或缺失 offset 列表，实现选择性重传。

11.2 RX 回调执行 Flash I&#x2F;O当前 DATA 在 UDP RX 任务回调内直接写 Flash。若单次写入延迟接近或超过包到达间隔，后续 UDP 包可能因 socket 缓冲不足而丢失。
建议将 RX 任务限定为“解析 + 复制 + 入队”，由文件接收任务负责落盘和 CRC。
11.3 超时清理不主动WECHAT_SERVICE_VOICE_RX_TIMEOUT_MS = 3000 只在处理新的 BEGIN 时生效。建议增加周期 timer 或在 RX receive timeout 分支中驱动接收状态超时清理。
11.4 安全边界协议没有鉴权和加密，只依赖固定 IP 过滤。它适合受控局域网和开发联调，不应直接暴露到不可信网络。
12. 联调检查表
两端协议版本均为 2；
AP&#x2F;STA 地址与端口配置方向正确；
表情索引严格为 0..7；
抓包确认多字节字段采用大端序；
DATA offset 连续递增，单片不超过 1193 B；
BEGIN&#x2F;END 的 file size 一致；
CRC32 覆盖原始 WCA 全文件；
语音文件大于 0 且不超过 128 KiB；
丢一个 DATA 包时，预期结果应为整段语音接收失败；
退出页面时 RX 最长约等待一个 100 ms socket timeout。

]]></content>
      <categories>
        <category>项目文档</category>
        <category>TXW81x 可视对讲</category>
      </categories>
      <tags>
        <tag>UDP</tag>
        <tag>TXW81x</tag>
        <tag>微信消息</tag>
        <tag>网络协议</tag>
        <tag>语音传输</tag>
        <tag>CRC32</tag>
      </tags>
  </entry>
</search>
